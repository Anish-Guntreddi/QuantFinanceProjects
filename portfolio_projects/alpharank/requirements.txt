# AlphaRank project dependencies
# Note: qbacktest must be installed first from ../qbacktest via:
#   pip install -e portfolio_projects/qbacktest
# All other deps are installed automatically via pyproject.toml
numpy>=2.0
pandas>=2.1,<3.0
scipy>=1.10
scikit-learn>=1.5
lightgbm>=4.0
skfolio>=0.20
statsmodels>=0.14
matplotlib>=3.8
seaborn>=0.13
pyyaml>=6.0
qbacktest>=0.1

# Optional: real market data (not required for offline tests)
# yfinance>=0.2
