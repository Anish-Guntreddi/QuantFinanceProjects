---
phase: 03-macroregime
plan: 09
type: execute
wave: 5
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05", "03-06", "03-07", "03-08"]
files_modified:
  - portfolio_projects/macroregime/src/macroregime/__init__.py
autonomous: true
requirements: [QUAL-01, QUAL-04]

must_haves:
  truths:
    - "Full strict suite passes twice in a row with identical results (deterministic, offline, FutureWarning-as-error)"
    - "macroregime exposes a frozen public API via __all__ (pipeline, loaders, detector, allocation, benchmarks, report)"
    - "Codex read-only audit passes with no unresolved findings on the three audit targets: causal-HMM signal path, point-in-time masking order, benchmark cost parity"
  artifacts:
    - path: "portfolio_projects/macroregime/src/macroregime/__init__.py"
      provides: "public API freeze with __all__"
      contains: "__all__"
  key_links:
    - from: "codex audit"
      to: "src/macroregime/regime/causal.py + data/loader_base.py + benchmarks/benchmarks.py"
      via: "codex exec --sandbox read-only"
      pattern: "codex exec"
---

<objective>
Phase quality gate: freeze the public API, prove the suite is deterministic and offline under strict warnings, then run the codex read-only leakage audit focused on the two phase-defining correctness properties (point-in-time masking, causal regime detection) plus benchmark cost parity — and resolve every finding before the phase is marked complete.

Purpose: QUAL-04 is a locked project-wide gate (Phases 1 and 2 each ended with this plan shape). Phase cannot complete without it.
Output: green strict suite x2, frozen API, codex audit clean.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-macroregime/03-RESEARCH.md
@.planning/phases/03-macroregime/03-VALIDATION.md
@portfolio_projects/macroregime/src/macroregime/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: API freeze + strict deterministic suite x2</name>
  <files>portfolio_projects/macroregime/src/macroregime/__init__.py</files>
  <action>
Finalize macroregime/__init__.py with explicit `__all__` covering the public surface: MacroRegimePipeline, PipelineResults, SyntheticMacroGenerator, SyntheticMacroLoader, FredMacroLoader, MacroLoaderBase, apply_release_lag, as_of_view, CausalRegimeDetector, align_regime_labels, transition_matrix, dwell_times, TargetWeightPortfolio, TargetWeightStrategy, run_strategy_backtest, build_60_40_weights, build_equal_weight_weights, build_risk_parity_weights, ReportBuilder, __version__. Keep matplotlib-importing modules (report) lazy via module __getattr__ if importing them at package init pulls pyplot (mirror qbacktest's TearsheetRenderer lazy-export pattern).

Run the full strict suite TWICE and diff results: `../../quant/bin/python -m pytest tests/ -v -W error::FutureWarning --tb=short` — both runs must pass with the same test count and zero skips (all Wave-0 stubs must be gone by now; if any skip remains, fail this task and fix the owning module). Confirm offline: no FRED_API_KEY in env during the run, no socket use (fredapi never imported — assert via the existing module-scope test).
  </action>
  <verify>
    <automated>cd portfolio_projects/macroregime && ../../quant/bin/python -m pytest tests/ -q -W error::FutureWarning && ../../quant/bin/python -m pytest tests/ -q -W error::FutureWarning && ../../quant/bin/python -c "import macroregime; assert len(macroregime.__all__) >= 15"</automated>
  </verify>
  <done>Suite green twice consecutively under strict warnings; zero skipped tests; __all__ frozen.</done>
</task>

<task type="auto">
  <name>Task 2: Codex read-only leakage audit + finding triage</name>
  <files>portfolio_projects/macroregime/src/macroregime/__init__.py</files>
  <action>
Run the codex read-only review (locked project gate):
`codex exec --sandbox read-only "Audit portfolio_projects/macroregime for look-ahead bias and correctness. Focus on: (1) CAUSAL REGIME PATH — find any call to model.predict / predict_proba / score_samples on a feature matrix extending beyond the current bar t in any signal-generating code path (smoothed HMM states are forbidden; only fit-on-X[:t] then predict(X[:t+1])[-1] is allowed); also check expanding-window standardization for full-sample leakage. (2) POINT-IN-TIME MACRO PATH — verify apply_release_lag is applied before any resample/ffill/join of macro data to asset returns, and that no fred get_series (revised vintage) calls exist. (3) BENCHMARK COST PARITY — verify the regime strategy and all three benchmarks construct slippage/commission/engine config from the same parameters via run_strategy_backtest. (4) General: seeded determinism, no network in tests, no hyperparameter selection by Sharpe."`

Triage every finding: fix real issues in the implicated module (and rerun the strict suite), or document false positives with justification in the SUMMARY. Re-run the codex audit after fixes until no unresolved findings remain. If codex CLI is unavailable or requires login, this becomes an authentication gate: surface the exact command for the user and pause — do not skip the audit.
  </action>
  <verify>
    <automated>cd portfolio_projects/macroregime && ../../quant/bin/python -m pytest tests/ -q -W error::FutureWarning</automated>
  </verify>
  <done>Codex audit completed with zero unresolved findings; any fixes covered by the still-green strict suite; triage recorded in SUMMARY.</done>
</task>

</tasks>

<verification>
- Strict suite green twice; codex audit transcript shows no unresolved findings on the three focus areas.
- `grep -rn "predict_proba\|score_samples" portfolio_projects/macroregime/src/` clean (signal path).
</verification>

<success_criteria>
- QUAL-01: deterministic offline suite proven. QUAL-04: codex read-only review (point-in-time + causal-HMM + cost-parity focus) passed with findings resolved — phase completion gate satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/03-macroregime/03-09-SUMMARY.md`
</output>
