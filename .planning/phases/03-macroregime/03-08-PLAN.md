---
phase: 03-macroregime
plan: 08
type: execute
wave: 4
depends_on: ["03-06", "03-07"]
files_modified:
  - portfolio_projects/macroregime/src/macroregime/report/builder.py
  - portfolio_projects/macroregime/src/macroregime/report/__init__.py
  - portfolio_projects/macroregime/run_macroregime.py
  - portfolio_projects/macroregime/README.md
  - portfolio_projects/macroregime/tests/test_integration.py
autonomous: true
requirements: [MCR-08, QUAL-02, QUAL-03]

must_haves:
  truths:
    - "`python run_macroregime.py --quick` exits 0 offline and writes figures under reports/figures/ plus a summary table"
    - "The report shows regime strategy vs 60/40, equal weight, and risk parity over identical periods: gross AND net Sharpe with bootstrap CIs for all four"
    - "Regime diagnostics appear in the report: transition matrix, mean dwell times per regime, and the label-alignment rule (states ordered by economic observable) documented"
    - "README is a research report: question, data (incl. release-lag table and synthetic DGP), methodology (causal re-fit pattern), results, robustness (K sensitivity, HMM-vs-GMM stability), limitations"
  artifacts:
    - path: "portfolio_projects/macroregime/run_macroregime.py"
      provides: "one-command runner with --quick, --seed, --k, --backend flags"
      contains: "--quick"
    - path: "portfolio_projects/macroregime/src/macroregime/report/builder.py"
      provides: "ReportBuilder — figures + markdown summary tables"
      min_lines: 100
    - path: "portfolio_projects/macroregime/README.md"
      provides: "research report"
      min_lines: 80
  key_links:
    - from: "portfolio_projects/macroregime/run_macroregime.py"
      to: "macroregime.MacroRegimePipeline"
      via: "in-process pipeline call (PipelineResults), argparse wrapper only"
      pattern: "MacroRegimePipeline"
    - from: "portfolio_projects/macroregime/src/macroregime/report/builder.py"
      to: "matplotlib Agg backend"
      via: "matplotlib.use('Agg') before pyplot import (headless safety, Phase 1 locked pattern)"
      pattern: "matplotlib.use\\([\"']Agg[\"']\\)"
---

<objective>
Ship the one-command runner (`run_macroregime.py --quick`), the ReportBuilder (regime timeline, transition-matrix heatmap, dwell-time chart, four-strategy OOS equity comparison, gross/net Sharpe + bootstrap CI table, K-sensitivity and HMM-vs-GMM stability tables), and the README research report.

Purpose: MCR-08 runner + QUAL-02/03. This is the user-facing deliverable: one command → honest research output.
Output: runner, figures under reports/figures/, README; runner integration test green.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-macroregime/03-RESEARCH.md
@portfolio_projects/macroregime/src/macroregime/pipeline.py
@portfolio_projects/macroregime/src/macroregime/evaluation.py
@portfolio_projects/alpharank/run_pipeline.py

<interfaces>
From plan 03-07:
```python
from macroregime import MacroRegimePipeline, PipelineResults
# PipelineResults: macro_regimes, market_regimes, combined_regimes, weight_schedule,
#                  regime_backtest (BacktestResults), diagnostics, config
from macroregime.evaluation import run_walk_forward, regime_stability_report, k_sensitivity
```
From plan 03-06: `run_strategy_backtest`, `build_60_40_weights`, `build_equal_weight_weights`, `build_risk_parity_weights`.
From qbacktest MetricsReport (verified): gross/net Sharpe fields plus `sharpe_ci_low`, `sharpe_ci_high` (95% bootstrap CI on net Sharpe) — use these directly for the comparison table; call `bootstrap_sharpe_ci` from qbacktest.metrics.performance for gross CIs if needed.
Phase 1 locked pattern: `matplotlib.use("Agg")` at tearsheet/report module import before pyplot.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReportBuilder — figures and comparison tables</name>
  <files>portfolio_projects/macroregime/src/macroregime/report/builder.py, portfolio_projects/macroregime/src/macroregime/report/__init__.py</files>
  <action>
Implement ReportBuilder(output_dir="reports/figures") with `matplotlib.use("Agg")` before any pyplot import. Methods, each saving a PNG and returning its path:
- `regime_timeline(combined_regimes, asset_ohlcv)`: EQUITY close with regime-colored background shading + regime-name legend.
- `transition_heatmap(diagnostics)`: seaborn heatmap(s) of the combined-sequence transition matrix with regime names; annotate mean dwell times.
- `dwell_time_chart(diagnostics)`: bar chart of mean dwell per regime (macro vs market vs combined).
- `equity_comparison(results: dict[str, BacktestResults | WalkForwardResults])`: OOS equity curves for Regime, 60/40, EqualWeight, RiskParity on one axes (log scale), identical periods.
- `summary_table(results) -> str`: markdown table — rows = 4 strategies; columns = Gross Sharpe, Net Sharpe, Net Sharpe 95% CI [low, high], Sortino, MaxDD, Turnover. Net beside gross with CIs satisfies QUAL-03. Write to reports/summary.md and return the markdown string.
- `stability_table(stability: dict, ksens: dict) -> str`: HMM-vs-GMM agreement, distribution drift, and per-K dwell/transition summaries; appended to reports/summary.md.
- `build_all(pipeline_results, benchmark_results, wf_results, stability, ksens) -> dict[str, Path]` orchestrating everything.
Document the label-alignment rule (states sorted ascending by the economic observable; state 0 = lowest) directly in the summary output (MCR-05 reporting requirement).
  </action>
  <verify>
    <automated>cd portfolio_projects/macroregime && ../../quant/bin/python -c "import macroregime.report.builder as b; import matplotlib; assert matplotlib.get_backend().lower() == 'agg'; print('agg ok')"</automated>
  </verify>
  <done>All builder methods implemented headless-safe; summary table includes gross+net Sharpe and bootstrap CIs for all four strategies; diagnostics + alignment documentation rendered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run_macroregime.py runner + integration test</name>
  <files>portfolio_projects/macroregime/run_macroregime.py, portfolio_projects/macroregime/tests/test_integration.py</files>
  <behavior>
    - test_runner_quick: invoke the runner's main(["--quick", "--output-dir", tmp_path]) in-process; exit code 0; at least 4 PNG files and summary.md exist in the output dir; summary.md contains all four strategy names and a "Net Sharpe" column
  </behavior>
  <action>
run_macroregime.py: argparse with --quick (10-year panel, fast detector settings), --seed (default 42), --k (default 3), --backend (hmm|gmm, default hmm), --output-dir (default reports/figures). Flow: pipeline = MacroRegimePipeline(quick=...).run() → benchmarks via build_*_weights + run_strategy_backtest on the same asset_ohlcv and rebalance dates → run_walk_forward → regime_stability_report + k_sensitivity (skip k_sensitivity for --quick? No — run with reduced sizes; keep it, quick must still exercise every report section) → ReportBuilder.build_all → print summary table to stdout. Structure as `def main(argv=None) -> int` returning 0, with `if __name__ == "__main__": raise SystemExit(main())` so the test calls main() in-process (Phase 2 locked pattern — no subprocess). No sys.path hacks. Replace the test_runner_quick stub per <behavior>.
  </action>
  <verify>
    <automated>cd portfolio_projects/macroregime && ../../quant/bin/python run_macroregime.py --quick && ../../quant/bin/python -m pytest tests/test_integration.py -v</automated>
  </verify>
  <done>Runner exits 0 offline in quick mode writing figures + summary; integration test green.</done>
</task>

<task type="auto">
  <name>Task 3: README research report</name>
  <files>portfolio_projects/macroregime/README.md</files>
  <action>
Write README.md as a research report (alpharank README is the structural template): (1) Research question — does causal macro/market regime conditioning improve risk-adjusted multi-asset allocation vs static benchmarks net of costs? (2) Data — synthetic 4-state Markov-switching DGP description (persistence ~0.95, regime parameter table), release-lag calendar table (CPI 13d, UNRATE 7d, GDPC1 30d, T10Y2Y 1d, USREC evaluation-only), optional FRED path (fredapi first-release vintages, FRED_API_KEY). (3) Methodology — point-in-time masking (lag before ffill), the causal re-fit pattern `model.predict(X[:t+1])[-1]` with the smoothed-state pitfall explained (55% historical disagreement), label alignment by argsort of means, dual-frequency model separation, TargetWeightPortfolio injection, identical-cost benchmarking. (4) How to run — install + `python run_macroregime.py --quick`, test commands. (5) Results — embed reports/figures PNGs and the summary table from the quick run actually executed in Task 2 (paste real numbers, honest framing: synthetic data, illustrative allocation table, no parameter tuning). (6) Robustness — K=2/3/4 sensitivity, HMM vs GMM stability. (7) Limitations — synthetic DGP, monthly macro granularity, no regime-uncertainty sizing.
  </action>
  <verify>
    <automated>cd portfolio_projects/macroregime && grep -c "##" README.md && grep -q "predict(X\[:t+1\])\[-1\]\|causal" README.md && echo "readme ok"</automated>
  </verify>
  <done>README covers question/data/methodology/run/results/robustness/limitations with real quick-run figures referenced.</done>
</task>

</tasks>

<verification>
- `cd portfolio_projects/macroregime && ../../quant/bin/python run_macroregime.py --quick` exits 0; `ls reports/figures/*.png` non-empty; summary table printed.
- `../../quant/bin/python -m pytest tests/ -q` fully green — zero remaining skip stubs.
</verification>

<success_criteria>
- MCR-08: one-command runner produces the research report.
- QUAL-02: README research report complete. QUAL-03: net-of-cost beside gross with bootstrap CIs for all four strategies.
</success_criteria>

<output>
After completion, create `.planning/phases/03-macroregime/03-08-SUMMARY.md`
</output>
