---
phase: 03-macroregime
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - portfolio_projects/macroregime/src/macroregime/data/loader_base.py
  - portfolio_projects/macroregime/src/macroregime/data/fred_loader.py
  - portfolio_projects/macroregime/src/macroregime/data/__init__.py
  - portfolio_projects/macroregime/tests/test_macro_data.py
autonomous: true
requirements: [MCR-01, MCR-02]

must_haves:
  truths:
    - "A macro series value for observation month m is invisible at any as-of date earlier than m + lag_days, and visible on or after it"
    - "The strategy's data view at any date t contains only rows published on or before t (unit-tested across all series)"
    - "SyntheticMacroLoader and FredMacroLoader share the MacroLoaderBase interface; tests run fully offline with no fredapi import at module scope"
    - "Release lags are loaded from configs/release_calendar.yml, never hardcoded"
  artifacts:
    - path: "portfolio_projects/macroregime/src/macroregime/data/loader_base.py"
      provides: "MacroLoaderBase ABC, apply_release_lag, as_of_view, load_release_calendar"
      exports: ["MacroLoaderBase", "apply_release_lag", "as_of_view", "load_release_calendar", "SyntheticMacroLoader"]
    - path: "portfolio_projects/macroregime/src/macroregime/data/fred_loader.py"
      provides: "FredMacroLoader — fredapi first-release vintages + lag, behind API-key check"
      contains: "get_series_first_release"
    - path: "portfolio_projects/macroregime/tests/test_macro_data.py"
      provides: "point-in-time mask oracle tests"
      min_lines: 60
  key_links:
    - from: "portfolio_projects/macroregime/src/macroregime/data/loader_base.py"
      to: "configs/release_calendar.yml"
      via: "load_release_calendar(yaml.safe_load)"
      pattern: "release_calendar"
    - from: "portfolio_projects/macroregime/src/macroregime/data/fred_loader.py"
      to: "fredapi"
      via: "lazy import inside method body (never module scope)"
      pattern: "def .*:\\n.*import fredapi|from fredapi import"
---

<objective>
Build the point-in-time macro data layer: MacroLoaderBase ABC, release-lag application (observation index → publication index), as-of masking, the default SyntheticMacroLoader, and the optional FredMacroLoader (fredapi first-release vintages, gated on an API key, never touched by tests).

Purpose: MCR-02 point-in-time correctness is one of the two phase-defining leakage controls (the codex gate audits this path specifically). Release lag must be applied BEFORE any resample/ffill/join downstream.
Output: PIT data layer with passing mask oracle tests; both loaders behind one interface.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-macroregime/03-RESEARCH.md
@portfolio_projects/macroregime/src/macroregime/data/synthetic.py
@portfolio_projects/macroregime/configs/release_calendar.yml

<interfaces>
Verified PIT pattern (03-RESEARCH.md, executed live):
```python
def apply_release_lag(series: pd.Series, lag_days: int) -> pd.Series:
    new_index = series.index + pd.Timedelta(days=lag_days)
    return pd.Series(series.values, index=new_index, name=series.name)

def as_of_view(series_pit: pd.Series, as_of: pd.Timestamp) -> pd.Series:
    return series_pit[series_pit.index <= as_of]
```
fredapi 0.5.2 verified ALFRED methods: `get_series_first_release(series_id)` (most PIT-correct), `get_series_as_of_date`, `get_series_all_releases`, `get_series_vintage_dates`. `fredapi.Fred(api_key=...)` requires a key.

From plan 03-01 (SyntheticMacroGenerator):
```python
panel = SyntheticMacroGenerator(n_years=10, seed=42).generate()
panel.macro              # monthly DataFrame, OBSERVATION-date index, columns = series ids
panel.asset_ohlcv        # dict[str, DataFrame]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MacroLoaderBase + release-lag/as-of utilities + SyntheticMacroLoader</name>
  <files>portfolio_projects/macroregime/src/macroregime/data/loader_base.py, portfolio_projects/macroregime/src/macroregime/data/__init__.py, portfolio_projects/macroregime/tests/test_macro_data.py</files>
  <behavior>
    - test_point_in_time_mask: for a monthly series with lag_days=13, the value observed at month-end m is NOT in as_of_view(series_pit, m + 12 days) but IS in as_of_view(series_pit, m + 13 days). Parametrize over all series in release_calendar.yml using each one's own lag.
    - test_no_future_observation: for every date t in a sampled grid, every row of SyntheticMacroLoader.load_panel(as_of=t) has publication index <= t AND original observation date <= t - lag_days for its series
    - test_loader_interface: SyntheticMacroLoader is an instance of MacroLoaderBase; load_series returns a pd.Series with publication-date index and `.attrs['observation_dates']` preserved; FredMacroLoader subclasses the same ABC (class relationship only — no instantiation, no network)
  </behavior>
  <action>
In loader_base.py implement:
- `load_release_calendar(path) -> dict[str, int]` reading configs/release_calendar.yml via yaml.safe_load (resolve path relative to package: default to `Path(__file__).parents[3] / "configs" / "release_calendar.yml"`, overridable arg).
- `apply_release_lag(series, lag_days)` and `as_of_view(series_pit, as_of)` exactly per the verified research pattern. Preserve original observation dates in `series.attrs["observation_dates"]` (pd.DatetimeIndex) so tests can assert against observation dates after the index becomes publication dates.
- `MacroLoaderBase(ABC)` with abstract `load_series(series_id, start, end) -> pd.Series` (publication-date index, lag already applied) and concrete `load_panel(series_ids=None, start=None, end=None, as_of=None) -> pd.DataFrame` that loads each series, applies as_of_view when as_of is given, and outer-joins on publication dates (no ffill here — frequency handling is the pipeline's job in plan 03-07, and ffill must happen AFTER lag per Pitfall 5).
- `SyntheticMacroLoader(MacroLoaderBase)` wrapping a SyntheticMacroGenerator instance (accept generator or seed in __init__): load_series pulls panel.macro[series_id] (observation dates) then applies the series' configured lag.
Update data/__init__.py to export the public names. Write the three real tests in test_macro_data.py replacing the Wave-0 skips (keep test fixtures small: n_years=5).
  </action>
  <verify>
    <automated>cd portfolio_projects/macroregime && ../../quant/bin/python -m pytest tests/test_macro_data.py -v -x</automated>
  </verify>
  <done>All three PIT tests pass; lags come exclusively from release_calendar.yml; observation month m invisible before m + lag_days for every configured series.</done>
</task>

<task type="auto">
  <name>Task 2: FredMacroLoader — optional fredapi path, never in tests</name>
  <files>portfolio_projects/macroregime/src/macroregime/data/fred_loader.py, portfolio_projects/macroregime/tests/test_macro_data.py</files>
  <action>
Implement FredMacroLoader(MacroLoaderBase):
- `__init__(self, api_key: str | None = None, release_calendar_path=None)`: resolve key from arg or os.environ["FRED_API_KEY"]; raise a clear RuntimeError("FRED_API_KEY not set — use SyntheticMacroLoader (default offline path)") if absent. Do NOT import fredapi at module scope — lazy import inside `_client()` method (locked convention: optional real-data deps are lazy, mirroring alpharank's yfinance pattern). fredapi is an optional dependency (`pip install macroregime[real-data]`).
- `load_series(series_id, start, end)`: `raw = self._client().get_series_first_release(series_id)` (first-release vintage = most PIT-correct; plain get_series returns revised data — explicitly forbidden, add a comment citing the research), slice [start:end] on observation dates, then `apply_release_lag(raw_slice, lag_days_from_calendar)`. Store observation dates in attrs like the synthetic loader.
- Add a module docstring stating: NEVER used in tests; synthetic loader is the default everywhere.
Add two offline tests to test_macro_data.py: (1) `test_fred_loader_requires_key`: instantiating FredMacroLoader with no key and empty env raises RuntimeError (monkeypatch.delenv FRED_API_KEY) — no network touched; (2) `test_fredapi_not_imported_at_module_scope`: `import macroregime.data.fred_loader` succeeds and `"fredapi" not in sys.modules` afterwards (pop it first if present).
  </action>
  <verify>
    <automated>cd portfolio_projects/macroregime && ../../quant/bin/python -m pytest tests/test_macro_data.py -v</automated>
  </verify>
  <done>test_macro_data.py fully green offline; fred_loader importable without fredapi installed/used; key absence raises actionable error pointing to the synthetic path.</done>
</task>

</tasks>

<verification>
- `cd portfolio_projects/macroregime && ../../quant/bin/python -m pytest tests/ -q` green (no network, no FRED key in env).
- `grep -n "import fredapi\|from fredapi" src/macroregime/data/fred_loader.py` shows the import only inside a function body.
- `grep -rn "get_series(" src/` returns nothing except get_series_first_release (no revised-vintage calls).
</verification>

<success_criteria>
- MCR-02: per-series release lag config, point-in-time as-of masking unit-tested (the codex-gate audit target).
- MCR-01: synthetic default + fredapi optional behind key check, shared MacroLoaderBase ABC, tests fully offline.
</success_criteria>

<output>
After completion, create `.planning/phases/03-macroregime/03-02-SUMMARY.md`
</output>
