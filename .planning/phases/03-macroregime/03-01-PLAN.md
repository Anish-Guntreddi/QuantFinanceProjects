---
phase: 03-macroregime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - portfolio_projects/macroregime/pyproject.toml
  - portfolio_projects/macroregime/requirements.txt
  - portfolio_projects/macroregime/src/macroregime/__init__.py
  - portfolio_projects/macroregime/src/macroregime/data/__init__.py
  - portfolio_projects/macroregime/src/macroregime/data/synthetic.py
  - portfolio_projects/macroregime/src/macroregime/features/__init__.py
  - portfolio_projects/macroregime/src/macroregime/regime/__init__.py
  - portfolio_projects/macroregime/src/macroregime/allocation/__init__.py
  - portfolio_projects/macroregime/src/macroregime/benchmarks/__init__.py
  - portfolio_projects/macroregime/src/macroregime/report/__init__.py
  - portfolio_projects/macroregime/configs/release_calendar.yml
  - portfolio_projects/macroregime/configs/strategy_params.yml
  - portfolio_projects/macroregime/tests/conftest.py
  - portfolio_projects/macroregime/tests/test_synthetic_macro.py
  - portfolio_projects/macroregime/tests/test_macro_data.py
  - portfolio_projects/macroregime/tests/test_market_features.py
  - portfolio_projects/macroregime/tests/test_regimes.py
  - portfolio_projects/macroregime/tests/test_allocation.py
  - portfolio_projects/macroregime/tests/test_benchmarks.py
  - portfolio_projects/macroregime/tests/test_integration.py
autonomous: true
requirements: [MCR-01, QUAL-01, QUAL-05]

must_haves:
  truths:
    - "pip install -e portfolio_projects/macroregime succeeds in the quant/ venv and `import macroregime` works"
    - "pytest collects all 8 Wave-0 test files with zero collection errors"
    - "SyntheticMacroGenerator with the same seed produces byte-identical macro series and asset OHLCV twice"
    - "An HMM fit on the full synthetic sample recovers the planted Markov regimes above chance"
  artifacts:
    - path: "portfolio_projects/macroregime/pyproject.toml"
      provides: "hatchling src-layout package, strict pytest config (filterwarnings error::FutureWarning)"
      contains: "error::FutureWarning"
    - path: "portfolio_projects/macroregime/src/macroregime/data/synthetic.py"
      provides: "SyntheticMacroGenerator — 4-state Markov-switching DGP"
      min_lines: 80
    - path: "portfolio_projects/macroregime/configs/release_calendar.yml"
      provides: "per-series release-lag config (CPI 13d, UNRATE 7d, GDPC1 30d, T10Y2Y 1d)"
      contains: "lag_days"
    - path: "portfolio_projects/macroregime/tests/conftest.py"
      provides: "autouse seed fixture + session-scoped synthetic macro panel fixture"
  key_links:
    - from: "portfolio_projects/macroregime/tests/conftest.py"
      to: "macroregime.data.synthetic"
      via: "deferred import inside fixture (collection-safe before implementation)"
      pattern: "from macroregime\\.data\\.synthetic import"
    - from: "portfolio_projects/macroregime/pyproject.toml"
      to: "qbacktest"
      via: "dependency declaration"
      pattern: "qbacktest"
---

<objective>
Create the macroregime package skeleton (src layout + hatchling, editable-installed into the quant/ venv), the two YAML configs, the full Wave-0 test scaffold (conftest + 8 stub test files with the exact node IDs from the validation contract), and the SyntheticMacroGenerator — the deterministic 4-state Markov-switching DGP that every downstream plan and every test depends on.

Purpose: Wave 0 of the phase. Without the skeleton and the synthetic generator, no other plan can run a single test. The generator is the offline default data source for the whole phase (locked decision: tests NEVER touch FRED).
Output: installable macroregime package, configs, 8 collecting test files, working seeded generator with planted-regime recovery test passing.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-macroregime/03-RESEARCH.md
@.planning/phases/03-macroregime/03-VALIDATION.md
@portfolio_projects/alpharank/pyproject.toml
@portfolio_projects/alpharank/tests/conftest.py

<interfaces>
From portfolio_projects/alpharank/pyproject.toml (template to copy — change name/deps):
```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/alpharank"]          # -> ["src/macroregime"]

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["error::FutureWarning"]
```

From qbacktest (installed 0.1.0, verified importable in quant/ venv):
```python
from qbacktest import SyntheticOHLCVGenerator  # single-regime GBM — NOT sufficient for macro DGP
# SyntheticOHLCVGenerator.generate() -> dict[str, pd.DataFrame(open,high,low,close,volume)], bdate index
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package skeleton, configs, editable install, conftest + Wave-0 test stubs</name>
  <files>portfolio_projects/macroregime/pyproject.toml, portfolio_projects/macroregime/requirements.txt, portfolio_projects/macroregime/src/macroregime/__init__.py, portfolio_projects/macroregime/src/macroregime/{data,features,regime,allocation,benchmarks,report}/__init__.py, portfolio_projects/macroregime/configs/release_calendar.yml, portfolio_projects/macroregime/configs/strategy_params.yml, portfolio_projects/macroregime/tests/conftest.py, portfolio_projects/macroregime/tests/test_{synthetic_macro,macro_data,market_features,regimes,allocation,benchmarks,integration}.py</files>
  <action>
Create portfolio_projects/macroregime/ with src layout, copying the alpharank pyproject.toml template: name="macroregime", version="0.1.0", requires-python=">=3.11", dependencies: numpy>=2.0, pandas>=2.1,<3.0, scipy>=1.10, scikit-learn>=1.5, hmmlearn>=0.3, skfolio>=0.20, statsmodels>=0.14, matplotlib>=3.8, seaborn>=0.13, pyyaml>=6.0, qbacktest>=0.1. Optional-dependencies: real-data = ["fredapi>=0.5"]. pytest config: testpaths=["tests"], filterwarnings=["error::FutureWarning"]. requirements.txt mirrors dependencies. Subpackages: data, features, regime, allocation, benchmarks, report (empty __init__.py each; top-level __init__.py with __version__ = "0.1.0").

configs/release_calendar.yml — per locked user decision (CPI ~13d; research cross-referenced ~15d, user decision wins):
```yaml
CPIAUCSL: {lag_days: 13, description: "CPI; BLS release ~mid-following-month"}
UNRATE:   {lag_days: 7,  description: "Unemployment; BLS first Friday of following month"}
GDPC1:    {lag_days: 30, description: "Real GDP quarterly advance estimate"}
T10Y2Y:   {lag_days: 1,  description: "10Y-2Y spread; daily H.15, 1 business day"}
USREC:    {lag_days: 180, description: "NBER recession flag; EVALUATION ONLY — never a feature"}
```

configs/strategy_params.yml — regime→weights table (4 synthetic assets EQUITY/BONDS/COMMODITY/CASH; weights per regime sum to 1.0; document as illustrative defaults, NOT tuned):
regime 0 (Recession): {EQUITY: 0.10, BONDS: 0.55, COMMODITY: 0.05, CASH: 0.30}; regime 1 (Neutral/Recovery): {EQUITY: 0.45, BONDS: 0.35, COMMODITY: 0.10, CASH: 0.10}; regime 2 (Expansion): {EQUITY: 0.65, BONDS: 0.20, COMMODITY: 0.10, CASH: 0.05}. Also: n_regimes: 3, refit_every: 21, min_train: 60, rebalance: "monthly", costs: {spread_bps: 5.0, commission_rate: 0.001}, engine: {initial_capital: 1000000, max_gross_exposure: 1.05, max_position_weight: 0.70}.

tests/conftest.py — copy alpharank pattern: autouse fix_seeds fixture (np.random.seed(42), random.seed(42), PYTHONHASHSEED); session fixture `macro_panel` with deferred import `from macroregime.data.synthetic import SyntheticMacroGenerator` returning a small generated panel (n_years=10, seed=42); session fixture `asset_ohlcv` returning the generator's asset OHLCV dict.

Create the 8 Wave-0 stub test files. Each contains the exact node IDs from 03-VALIDATION.md decorated with @pytest.mark.skip(reason="Wave 0 stub — implemented in plan 03-NN"):
- test_synthetic_macro.py: test_determinism, test_regime_structure, test_hmm_recovers_planted_regimes (implemented in Task 2 of THIS plan — write stubs here, replace in Task 2)
- test_macro_data.py: test_point_in_time_mask, test_no_future_observation, test_loader_interface (plan 03-02)
- test_market_features.py: test_realized_vol_causal, test_features_append_future_invariant, test_feature_nan_warmup (plan 03-03)
- test_regimes.py: test_causality_future_data_does_not_change_past_regimes, test_label_alignment_stable_across_refits, test_hmm_convergence, test_transition_matrix_rows_sum_to_one, test_gmm_causal_sequence (plan 03-04)
- test_allocation.py: test_target_weight_portfolio_sizes_position, test_accounting_invariant_after_fills, test_weight_change_reemits_signal (plan 03-05)
- test_benchmarks.py: test_identical_costs_across_strategies, test_risk_parity_weights (plan 03-06)
- test_integration.py: test_walk_forward_oos (plan 03-07), test_runner_quick (plan 03-08)

Install editable: run `cd portfolio_projects/macroregime && ../../quant/bin/python -m pip install -e .` and confirm `../../quant/bin/python -c "import macroregime"` works. Do NOT add sys.path hacks anywhere (locked convention from Phase 2).
  </action>
  <verify>
    <automated>cd portfolio_projects/macroregime && ../../quant/bin/python -m pip install -e . -q && ../../quant/bin/python -m pytest tests/ --collect-only -q</automated>
  </verify>
  <done>Editable install succeeds; pytest collects all 8 test files with all named node IDs and zero collection errors; both YAML configs parse with yaml.safe_load.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SyntheticMacroGenerator — 4-state Markov-switching DGP</name>
  <files>portfolio_projects/macroregime/src/macroregime/data/synthetic.py, portfolio_projects/macroregime/tests/test_synthetic_macro.py</files>
  <behavior>
    - test_determinism: two generators with seed=42 produce identical macro DataFrames, identical asset OHLCV, identical true_regimes array; seed=43 differs
    - test_regime_structure: true regime sequence has 4 distinct states; mean dwell time > 10 monthly periods (persistence ~0.95); each state visited at least once over 30 years
    - test_hmm_recovers_planted_regimes: GaussianHMM(n_components=4, covariance_type='diag', random_state=42) fit on the full standardized monthly macro feature matrix; best label permutation (use scipy.optimize.linear_sum_assignment on the confusion matrix) achieves accuracy > 0.5 vs true_regimes (4-state chance = 0.25)
  </behavior>
  <action>
Implement SyntheticMacroGenerator in src/macroregime/data/synthetic.py per the verified DGP design in 03-RESEARCH.md Pattern 5:

- `__init__(self, n_years: int = 30, start_date: str = "1995-01-31", seed: int = 42)`; single `np.random.default_rng(seed)` instance — no global np.random calls (locked Phase 2 convention).
- 4-state Markov chain (Expansion/Stagflation/Recession/Recovery) using the TRANSITION_MATRIX from research (self-persistence 0.95-0.96). Simulate the MONTHLY regime path first; daily regimes are the month's regime broadcast to business days.
- REGIME_PARAMS table from research: per-regime means for 4 monthly macro series (CPIAUCSL m/m, UNRATE level, GDPC1 growth, T10Y2Y spread — add small Gaussian noise per series) and per-regime (mu, sigma) for 4 daily asset return series (EQUITY, BONDS, COMMODITY, CASH). Treat exact parameter values as configurable class-level defaults (research confidence MEDIUM on exact numbers); keep the qualitative ordering (recession: negative equity mu + high vol; expansion: positive equity mu + low vol).
- `generate()` returns a dataclass `SyntheticMacroPanel` with fields: `macro` (monthly DataFrame, month-end DatetimeIndex, columns=series ids, OBSERVATION dates — no lag applied here; lag is plan 03-02's job), `asset_ohlcv` (dict[str, pd.DataFrame] with open/high/low/close/volume on a business-day DatetimeIndex — build close from cumulative regime-conditional returns starting at 100.0, open=prev close, high/low = max/min(open,close)*(1±tiny), volume constant 1e6; matches qbacktest HistoricalDataHandler expectations), `true_regimes_monthly` (np.ndarray of planted states), `true_regimes_daily` (np.ndarray), `regime_names` (dict int→str).
- GDPC1 is quarterly in reality but generate it monthly-frequency-with-quarterly-values (repeat each quarter's value 3 months) OR simply monthly; keep monthly for simplicity and document.
- Use `pd.bdate_range` / `pd.date_range(freq="ME")`; never pct_change without fill_method=None anywhere in this package (strict FutureWarning-as-error CI).

Replace the three skipped stubs in tests/test_synthetic_macro.py with the real tests per <behavior>. Standardize features before HMM fit in the recovery test (z-score columns) and use a fixed seed.
  </action>
  <verify>
    <automated>cd portfolio_projects/macroregime && ../../quant/bin/python -m pytest tests/test_synthetic_macro.py -v -x</automated>
  </verify>
  <done>All three test_synthetic_macro tests pass; full suite (`pytest tests/ -q`) is green (remaining stubs skip); generator is fully deterministic under fixed seed.</done>
</task>

</tasks>

<verification>
- `cd portfolio_projects/macroregime && ../../quant/bin/python -m pytest tests/ -v --tb=short` — green (3 real passes, rest skipped stubs), zero warnings-as-errors.
- `../../quant/bin/python -c "import macroregime, yaml; yaml.safe_load(open('portfolio_projects/macroregime/configs/release_calendar.yml'))"` from repo root succeeds.
- No network access in any test (no fredapi import anywhere yet).
</verification>

<success_criteria>
- macroregime installable + importable from quant/ venv; src layout + hatchling per QUAL-05.
- All Wave-0 node IDs exist and collect (Nyquist contract satisfied for downstream plans).
- SyntheticMacroGenerator: deterministic, seeded, 4-state persistent Markov DGP, HMM-recoverable (MCR-01 synthetic default path).
</success_criteria>

<output>
After completion, create `.planning/phases/03-macroregime/03-01-SUMMARY.md`
</output>
