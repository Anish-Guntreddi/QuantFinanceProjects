---
phase: 03-macroregime
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - portfolio_projects/macroregime/src/macroregime/regime/alignment.py
  - portfolio_projects/macroregime/src/macroregime/regime/causal.py
  - portfolio_projects/macroregime/src/macroregime/regime/diagnostics.py
  - portfolio_projects/macroregime/src/macroregime/regime/__init__.py
  - portfolio_projects/macroregime/tests/test_regimes.py
autonomous: true
requirements: [MCR-04, MCR-05]

must_haves:
  truths:
    - "Appending future observations to the input does NOT change the causal regime label at any historical time t (THE phase-defining oracle, both HMM and GMM backends)"
    - "No call to model.predict / predict_proba / score_samples on data extending past the current bar exists anywhere in the signal path"
    - "Regime labels are aligned after every re-fit by np.argsort of state means on the chosen economic observable — aligned state 0 is always the lowest-mean state"
    - "Transition matrix rows sum to 1.0 and mean dwell times per regime are computable from any causal regime sequence"
  artifacts:
    - path: "portfolio_projects/macroregime/src/macroregime/regime/causal.py"
      provides: "CausalRegimeDetector with 'hmm' and 'gmm' backends, rolling re-fit, multi-start, warm-start"
      exports: ["CausalRegimeDetector"]
      min_lines: 100
    - path: "portfolio_projects/macroregime/src/macroregime/regime/alignment.py"
      provides: "align_regime_labels permutation via argsort(means_)"
      exports: ["align_regime_labels"]
    - path: "portfolio_projects/macroregime/src/macroregime/regime/diagnostics.py"
      provides: "transition_matrix, dwell_times, regime_run_lengths"
      exports: ["transition_matrix", "dwell_times"]
  key_links:
    - from: "portfolio_projects/macroregime/src/macroregime/regime/causal.py"
      to: "hmmlearn GaussianHMM"
      via: "fit on X[:t] then predict(X[:t+1])[-1] — last filtered state only"
      pattern: "predict\\(X\\[:t \\+ 1\\]\\)\\[-1\\]|predict\\(.*t \\+ 1.*\\)\\[-1\\]"
    - from: "portfolio_projects/macroregime/src/macroregime/regime/causal.py"
      to: "macroregime.regime.alignment"
      via: "align_regime_labels applied after EVERY re-fit before storing labels"
      pattern: "align_regime_labels"
---

<objective>
Build the regime detection core: label alignment (argsort of state means), persistence diagnostics (transition matrix, dwell times), and the CausalRegimeDetector — rolling re-fit on expanding windows producing strictly causal regime sequences for both hmmlearn GaussianHMM and sklearn GaussianMixture backends — with the causality oracle test that defines this phase.

Purpose: MCR-04/MCR-05. Research proved empirically that hmmlearn's predict/predict_proba are smoothed (55% historical disagreement when future data appended). The ONLY safe pattern is `model.predict(X[:t+1])[-1]` after fitting on X[:t]. This plan encodes that pattern once, tests it with an oracle, and nothing else in the project is allowed to produce regimes any other way.
Output: regime/ subpackage with oracle + alignment + diagnostics tests green.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-macroregime/03-RESEARCH.md
@portfolio_projects/macroregime/src/macroregime/data/synthetic.py

<interfaces>
Verified hmmlearn 0.3.3 facts (research, live-executed):
```python
model.predict(X)        # Viterbi — SMOOTHED, forbidden on full sequences in signal path
model.predict_proba(X)  # forward-backward — SMOOTHED, forbidden
model.score(X)          # total log-likelihood — use for multi-start selection
model.monitor_.converged  # bool;  model.monitor_.iter — EM iterations
# No n_init param — multi-start = manual loop over random_state seeds
# Warm-start: model.init_params = '' before refit → EM starts from current params
```
Causal pattern (verified): fit on X[:t], regime_at_t = model.predict(X[:t+1])[-1].
Alignment (verified): `order = np.argsort(model.means_[:, observable_dim]); aligned = order_inverse[raw_state]` — note argsort gives state indices sorted by mean; the mapping raw→aligned is `np.argsort(order)` (inverse permutation). Implement and unit-test the inverse correctly.
GMM nuance (research Pitfall 6): GaussianMixture scoring of a single bar IS causal given fixed params, but params must come from rolling re-fit on X[:t] only — same loop, `gm.predict(X[t:t+1])[0]` with aligned labels.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Label alignment + persistence diagnostics</name>
  <files>portfolio_projects/macroregime/src/macroregime/regime/alignment.py, portfolio_projects/macroregime/src/macroregime/regime/diagnostics.py, portfolio_projects/macroregime/src/macroregime/regime/__init__.py, portfolio_projects/macroregime/tests/test_regimes.py</files>
  <behavior>
    - test_label_alignment_stable_across_refits: fit GaussianHMM(K=3, random_state=42) on expanding windows end_t in [150, 200, 250, 300] of seeded 3-regime data; after applying align_regime_labels(observable_dim=0), sorted means are monotonically ascending at every window, and the relative ordering of regime identities (sign/rank of means per aligned slot) does not flip between consecutive windows
    - test_transition_matrix_rows_sum_to_one: transition_matrix(seq, k) rows each sum to 1.0 (rows with zero visits get uniform 1/k); dwell_times on a hand-built sequence [0,0,0,1,1,2,0,0] gives mean dwell {0: 2.5, 1: 2.0, 2: 1.0}
  </behavior>
  <action>
alignment.py: `align_regime_labels(means: np.ndarray, observable_dim: int = 0) -> np.ndarray` returning the raw→aligned mapping (the INVERSE permutation of np.argsort(means[:, observable_dim]) — document why: argsort lists raw indices in ascending-mean order; mapping a raw label to its rank requires the inverse). Accept a fitted model OR a raw means matrix (duck-type on `.means_`). Works for both GaussianHMM.means_ and GaussianMixture.means_.

diagnostics.py: `transition_matrix(seq: np.ndarray, n_states: int) -> np.ndarray` (empirical count/normalize on consecutive pairs, ignore -1 sentinel entries, uniform row for unvisited states); `dwell_times(seq, n_states) -> dict[int, float]` (mean run length per state via run-length encoding, ignoring -1); `regime_run_lengths(seq) -> list[tuple[int, int]]`. These operate on CAUSAL sequences (not model.transmat_) so diagnostics describe what the strategy actually traded on; also accept an optional fitted model to additionally report model.transmat_ and model.get_stationary_distribution() for the report.

Replace the two corresponding Wave-0 stubs in test_regimes.py with the real tests per <behavior>. Build the 3-regime test data from SyntheticMacroGenerator monthly features (collapse 4 planted states to 3 by merging, or use a small bespoke 3-state mixture sampler inside the test — keep it seeded and < 1s).
  </action>
  <verify>
    <automated>cd portfolio_projects/macroregime && ../../quant/bin/python -m pytest tests/test_regimes.py -v -x -k "alignment or transition"</automated>
  </verify>
  <done>Alignment returns the correct inverse permutation (unit-tested against a hand-built means matrix); diagnostics match hand-computed values; stability test green across all four windows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CausalRegimeDetector (HMM + GMM backends) + causality oracle</name>
  <files>portfolio_projects/macroregime/src/macroregime/regime/causal.py, portfolio_projects/macroregime/src/macroregime/regime/__init__.py, portfolio_projects/macroregime/tests/test_regimes.py</files>
  <behavior>
    - test_causality_future_data_does_not_change_past_regimes (THE phase oracle): build causal sequence on X (300 bars, seeded); build again on X with 50 future bars appended; assert np.array_equal(regimes[:300][valid], regimes_ext[:300][valid]) — every computed historical label identical. Parametrized over backend in ("hmm", "gmm")
    - test_hmm_convergence: after the final re-fit on synthetic data, detector.last_model_.monitor_.converged is True and monitor_.iter < n_iter
    - test_gmm_causal_sequence: GMM backend produces a sequence with no -1 after min_train, labels in [0, K), and deterministic across two identical runs
  </behavior>
  <action>
Implement `CausalRegimeDetector` in causal.py following the verified research pattern exactly:

```python
class CausalRegimeDetector:
    def __init__(self, backend="hmm", n_components=3, min_train=60,
                 refit_every=21, n_restarts=3, covariance_type="diag",
                 n_iter=200, tol=1e-4, observable_dim=0, random_seed=42): ...
    def fit_predict_causal(self, X: np.ndarray) -> np.ndarray: ...
```

fit_predict_causal loop (regimes[:min_train] = -1):
- Refit when t == min_train or (t - min_train) % refit_every == 0, ALWAYS fitting on X[:t] only. CRITICAL for the oracle: the refit schedule and every seed must be pure functions of t (never of len(X)) so appending future rows cannot perturb any historical fit.
- HMM backend: multi-start loop over `random_seed + offset` for offset in range(n_restarts), keep argmax model.score(X[:t]); after the first selected fit, enable warm-start by ALSO including the previous model (init_params='') as a candidate in subsequent refits, but keep candidate seeds t-independent... simplest deterministic scheme: at each refit do the full n_restarts cold multi-start PLUS the warm-started previous model, select by score. Document the scheme.
- Label at t: `model.predict(X[:t + 1])[-1]`, then map through `align_regime_labels(model.means_, observable_dim)` computed ONCE per refit (cache the permutation alongside the model; apply to every label produced by that model).
- GMM backend: `GaussianMixture(n_components, covariance_type='diag', n_init=n_restarts, random_state=random_seed, max_iter=n_iter)` fit on X[:t] at the same refit schedule; label at t = `gm.predict(X[t:t + 1])[0]` mapped through the same alignment on gm.means_.
- Expose `last_model_`, `refit_times_` (list of t where refits happened), and `alignments_` for diagnostics/report.
- K is a constructor arg (n_components) — K=3 default, K=2/4 used by the sensitivity analysis in plan 03-07. NO hyperparameter search over anything else (anti-feature).
- Module docstring + inline comment block: explicitly forbid model.predict/predict_proba/score_samples on full sequences in any signal path, citing the 55% disagreement finding (codex audit target).

Replace the three remaining Wave-0 stubs in test_regimes.py per <behavior>. Keep oracle test runtime sane: min_train=60, refit_every=30, n_restarts=2, K=3, 4 features, 300+50 bars (~10 refits per run). Export CausalRegimeDetector from regime/__init__.py.
  </action>
  <verify>
    <automated>cd portfolio_projects/macroregime && ../../quant/bin/python -m pytest tests/test_regimes.py -v</automated>
  </verify>
  <done>Oracle passes for BOTH backends (append-future leaves all historical labels unchanged); convergence flag asserted; full test_regimes.py green; runtime under ~60s.</done>
</task>

</tasks>

<verification>
- `cd portfolio_projects/macroregime && ../../quant/bin/python -m pytest tests/ -q` green.
- `grep -rn "predict_proba\|score_samples" src/macroregime/regime/` — no smoothed-posterior calls in the signal path (predict appears only as `predict(X[:t + 1])[-1]` and `predict(X[t:t + 1])[0]`).
- Running the oracle test twice produces identical sequences (determinism).
</verification>

<success_criteria>
- MCR-04: causal regime sequences via rolling re-fit for HMM and GMM; oracle test proves regime at t unchanged by future data.
- MCR-05: labels aligned via argsort(means_) after every re-fit; transition matrix + dwell-time diagnostics implemented and hand-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-macroregime/03-04-SUMMARY.md`
</output>
