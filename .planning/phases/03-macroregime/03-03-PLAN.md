---
phase: 03-macroregime
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - portfolio_projects/macroregime/src/macroregime/features/market.py
  - portfolio_projects/macroregime/src/macroregime/features/__init__.py
  - portfolio_projects/macroregime/tests/test_market_features.py
autonomous: true
requirements: [MCR-03]

must_haves:
  truths:
    - "Realized vol, momentum, drawdown, and rolling correlation at time t use only prices up to and including t-1 (lagged by construction)"
    - "Appending future price rows leaves every feature value at all earlier timestamps numerically unchanged"
    - "Feature NaN warm-up windows match the documented lookback for each feature"
  artifacts:
    - path: "portfolio_projects/macroregime/src/macroregime/features/market.py"
      provides: "realized_vol, momentum, drawdown, rolling_corr, build_market_features"
      exports: ["realized_vol", "momentum", "drawdown", "rolling_corr", "build_market_features"]
      min_lines: 60
  key_links:
    - from: "portfolio_projects/macroregime/src/macroregime/features/market.py"
      to: "pandas pct_change"
      via: "pct_change(fill_method=None) only — FutureWarning is an error in CI"
      pattern: "pct_change\\(fill_method=None\\)"
    - from: "portfolio_projects/macroregime/tests/test_market_features.py"
      to: "macroregime.features.market"
      via: "append-future invariance property test over all features"
      pattern: "build_market_features"
---

<objective>
Implement the daily market feature layer — realized volatility, momentum, drawdown, and rolling cross-asset correlation regime indicators — all strictly causal (shift(1)-lagged), plus a panel builder that assembles the daily market feature matrix fed to the market-regime model in plan 03-07.

Purpose: MCR-03. These features are one of the two regime-model inputs (daily market HMM); any look-ahead here contaminates every regime downstream.
Output: features/market.py with causal property tests green.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-macroregime/03-RESEARCH.md
@portfolio_projects/macroregime/src/macroregime/data/synthetic.py

<interfaces>
From plan 03-01 conftest (session fixtures available to tests):
```python
macro_panel   # SyntheticMacroPanel: .macro, .asset_ohlcv, .true_regimes_daily
asset_ohlcv   # dict[str, pd.DataFrame(open,high,low,close,volume)], bdate DatetimeIndex
```
Locked conventions (Phase 2, enforced by FutureWarning-as-error CI): `pct_change(fill_method=None)` always; all features lagged with `.shift(1)` so the value AT t is computable from information strictly before t's close.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Causal market features (realized vol, momentum, drawdown, rolling corr)</name>
  <files>portfolio_projects/macroregime/src/macroregime/features/market.py, portfolio_projects/macroregime/src/macroregime/features/__init__.py, portfolio_projects/macroregime/tests/test_market_features.py</files>
  <behavior>
    - test_realized_vol_causal: hand-build a 30-bar close series; realized_vol(close, window=21).iloc[t] equals np.std of returns over bars [t-21, t-1] (ddof=1, annualized sqrt(252)) — i.e. the bar-t return is NOT included (shift(1) applied)
    - test_feature_nan_warmup: realized_vol(window=21) has exactly 22 leading NaNs (1 return + 21 window + shift); momentum(lookback=63) has 64; drawdown valid from bar 2; rolling_corr(window=63) valid from bar 65
    - (Task 2 adds test_features_append_future_invariant)
  </behavior>
  <action>
Implement in features/market.py, each taking pd.Series/DataFrame of CLOSE prices and returning lag-safe outputs:
- `realized_vol(close, window=21, ann_factor=252)`: `ret = close.pct_change(fill_method=None)`; `ret.rolling(window).std(ddof=1) * sqrt(ann_factor)`, then `.shift(1)`.
- `momentum(close, lookback=63)`: `close.pct_change(lookback, fill_method=None).shift(1)`.
- `drawdown(close)`: `close / close.cummax() - 1.0`, then `.shift(1)` (cummax is causal but the t-bar close itself must be excluded from the t-dated feature).
- `rolling_corr(close_a, close_b, window=63)`: rolling Pearson correlation of the two return series, `.shift(1)`.
- `build_market_features(asset_ohlcv: dict[str, pd.DataFrame], vol_window=21, mom_lookback=63, corr_window=63) -> pd.DataFrame`: extract closes, compute per-asset realized_vol/momentum/drawdown for EQUITY/BONDS/COMMODITY plus rolling_corr(EQUITY, BONDS); column names like "EQUITY_vol", "EQUITY_mom", "EQUITY_dd", "eq_bd_corr". Drop no rows (caller decides); document the maximum warm-up length in the docstring and expose it as `build_market_features.warmup(...)` helper or returned attrs.
Write the two real tests per <behavior> replacing the Wave-0 skips. Export names via features/__init__.py.
  </action>
  <verify>
    <automated>cd portfolio_projects/macroregime && ../../quant/bin/python -m pytest tests/test_market_features.py -v -x -k "realized_vol_causal or nan_warmup"</automated>
  </verify>
  <done>Hand-computed expectations match; warm-up NaN counts exact; no FutureWarning raised under error filter.</done>
</task>

<task type="auto">
  <name>Task 2: Append-future invariance property test for all features</name>
  <files>portfolio_projects/macroregime/tests/test_market_features.py</files>
  <action>
Replace the test_features_append_future_invariant stub with a property test: build features on asset_ohlcv truncated to the first 400 bars → F1; build on the full panel → F2; assert `F2.loc[F1.index].equals(F1)` exactly (pd.testing.assert_frame_equal, check_exact=False with rtol=0 atol=0 — must be bitwise identical since causal features cannot depend on future rows). Parametrize over each individual feature function as well as build_market_features. This is the feature-layer analogue of the regime causality oracle and a codex-gate audit target.
  </action>
  <verify>
    <automated>cd portfolio_projects/macroregime && ../../quant/bin/python -m pytest tests/test_market_features.py -v</automated>
  </verify>
  <done>test_market_features.py fully green; truncated-vs-full feature frames identical on the common index for every feature.</done>
</task>

</tasks>

<verification>
- `cd portfolio_projects/macroregime && ../../quant/bin/python -m pytest tests/ -q` green.
- `grep -n "pct_change(" src/macroregime/features/market.py` — every call carries fill_method=None.
- `grep -n "shift(1)" src/macroregime/features/market.py` — present in all four feature functions.
</verification>

<success_criteria>
- MCR-03: realized vol, momentum, drawdown, rolling correlation implemented, causal by construction, append-future invariant proven by test.
</success_criteria>

<output>
After completion, create `.planning/phases/03-macroregime/03-03-SUMMARY.md`
</output>
