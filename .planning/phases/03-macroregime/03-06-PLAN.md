---
phase: 03-macroregime
plan: 06
type: execute
wave: 3
depends_on: ["03-01", "03-05"]
files_modified:
  - portfolio_projects/macroregime/src/macroregime/benchmarks/benchmarks.py
  - portfolio_projects/macroregime/src/macroregime/benchmarks/__init__.py
  - portfolio_projects/macroregime/tests/test_benchmarks.py
autonomous: true
requirements: [MCR-07]

must_haves:
  truths:
    - "60/40, equal-weight, and inverse-vol risk parity benchmarks all run through the SAME engine assembly (TargetWeightPortfolio + TargetWeightStrategy) with IDENTICAL slippage/commission/config parameters as the regime strategy"
    - "Risk-parity weights at every rebalance sum to 1.0, are non-negative, and use only trailing (as-of) returns"
    - "Each benchmark produces a BacktestResults with gross and net Sharpe populated"
  artifacts:
    - path: "portfolio_projects/macroregime/src/macroregime/benchmarks/benchmarks.py"
      provides: "build_60_40_weights, build_equal_weight_weights, build_risk_parity_weights, run_strategy_backtest, COST_PARAMS/ENGINE_PARAMS single source of truth"
      exports: ["build_60_40_weights", "build_equal_weight_weights", "build_risk_parity_weights", "run_strategy_backtest"]
      min_lines: 80
  key_links:
    - from: "portfolio_projects/macroregime/src/macroregime/benchmarks/benchmarks.py"
      to: "macroregime.allocation (TargetWeightPortfolio, TargetWeightStrategy)"
      via: "single run_strategy_backtest helper shared by regime strategy AND all benchmarks"
      pattern: "TargetWeightPortfolio"
    - from: "portfolio_projects/macroregime/src/macroregime/benchmarks/benchmarks.py"
      to: "skfolio InverseVolatility (or simple inverse-vol)"
      via: "trailing-window fit per rebalance date"
      pattern: "InverseVolatility|inverse_vol"
---

<objective>
Implement the three benchmark allocators (60/40, equal weight, inverse-vol risk parity) and the single shared `run_strategy_backtest` engine-assembly helper that guarantees the regime strategy and every benchmark use identical engine config and identical cost models — the cost-parity property the codex gate audits.

Purpose: MCR-07. A regime strategy is only honest research when compared against standard allocations under the exact same frictions.
Output: benchmarks/ subpackage; cost-parity and weight-validity tests green.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-macroregime/03-RESEARCH.md
@portfolio_projects/macroregime/src/macroregime/allocation/strategy.py
@portfolio_projects/macroregime/src/macroregime/allocation/portfolio.py

<interfaces>
From plan 03-05 (allocation layer):
```python
from macroregime.allocation import TargetWeightPortfolio, TargetWeightStrategy
# TargetWeightStrategy(weights: dict[pd.Timestamp, dict[str, float]])
# TargetWeightPortfolio(initial_capital, risk_manager=...)
from macroregime.allocation.weights import month_end_rebalance_dates
```
From qbacktest (verified):
```python
from qbacktest import EventDrivenBacktester, BacktestConfig, HistoricalDataHandler, RiskManager, SimulatedExecutionHandler
from qbacktest.execution.slippage import SpreadSlippage      # SpreadSlippage(spread_bps=...)
from qbacktest.execution.commission import PercentageCommission  # PercentageCommission(rate=...)
# BacktestResults.metrics is a MetricsReport with sharpe_ci_low/high, gross & net figures
```
From skfolio 0.20.1 (verified): `from skfolio.optimization import InverseVolatility`; `model.fit(returns_2d_array)`; `model.weights_`. Locked decision: InverseVolatility OR simple inverse-vol — keep it simple; prefer skfolio since installed, with the simple numpy fallback acceptable if its API fights the small panel.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Benchmark weight builders + shared engine-assembly helper</name>
  <files>portfolio_projects/macroregime/src/macroregime/benchmarks/benchmarks.py, portfolio_projects/macroregime/src/macroregime/benchmarks/__init__.py</files>
  <action>
In benchmarks.py:
- Module-level single source of truth read from configs/strategy_params.yml (costs + engine sections, loaded once via a `load_run_params()` helper): spread_bps=5.0, commission_rate=0.001, initial_capital=1_000_000, max_gross_exposure=1.05, max_position_weight=0.70. ALL backtests in this project construct cost/config objects from these values — never inline literals.
- `run_strategy_backtest(asset_ohlcv: dict[str, pd.DataFrame], weight_schedule: dict[pd.Timestamp, dict[str, float]], params=None, start=None, end=None) -> BacktestResults`: assembles HistoricalDataHandler(asset_ohlcv, start, end), TargetWeightStrategy(weight_schedule), TargetWeightPortfolio(initial_capital, risk_manager=RiskManager(...)), SimulatedExecutionHandler(SpreadSlippage(spread_bps), PercentageCommission(rate)), BacktestConfig(...), EventDrivenBacktester(..., portfolio=...), returns engine.run(). This ONE function is used by the regime strategy (plan 03-07) and all three benchmarks — cost parity by construction.
- `build_60_40_weights(rebalance_dates) -> schedule`: {EQUITY: 0.60, BONDS: 0.40, COMMODITY: 0.0, CASH: 0.0} at every date.
- `build_equal_weight_weights(rebalance_dates)`: 0.25 each.
- `build_risk_parity_weights(asset_returns: pd.DataFrame, rebalance_dates, lookback_bars=126)`: per research Pattern 7 — trailing window `asset_returns.loc[:date].tail(lookback)` (strictly as-of: slice must exclude returns after the rebalance date; use `.loc[:date]` then drop the date itself if present to avoid same-day information), skfolio InverseVolatility().fit(trailing.values) → dict(zip(columns, model.weights_)); equal-weight fallback when < 20 trailing bars. asset_returns built from closes with pct_change(fill_method=None).
Export all from benchmarks/__init__.py.
  </action>
  <verify>
    <automated>cd portfolio_projects/macroregime && ../../quant/bin/python -c "from macroregime.benchmarks import build_60_40_weights, build_equal_weight_weights, build_risk_parity_weights, run_strategy_backtest; print('ok')"</automated>
  </verify>
  <done>All builders importable; run_strategy_backtest is the only engine-assembly path in the package; params sourced from strategy_params.yml.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Benchmark tests — cost parity + risk-parity validity</name>
  <files>portfolio_projects/macroregime/tests/test_benchmarks.py</files>
  <behavior>
    - test_identical_costs_across_strategies: run all three benchmarks plus a dummy regime schedule through run_strategy_backtest on the same 4-asset fixture; assert the cost/config parameters used are identical by inspecting load_run_params() is the single source AND each run's results reflect costs (net Sharpe <= gross Sharpe when turnover > 0); additionally assert by construction: monkeypatch run_strategy_backtest's param loader to record invocations and verify every backtest flowed through it
    - test_risk_parity_weights: at every rebalance date weights sum to 1.0 ± 1e-9, all >= 0, and the lower-vol asset (CASH/BONDS) receives higher weight than EQUITY on the synthetic panel; weights computed at date d are unchanged when future return rows are appended (as-of property)
  </behavior>
  <action>
Replace the two Wave-0 stubs with real tests per <behavior>. Use a small fixture (asset_ohlcv truncated to ~300 bars, monthly rebalances) so the whole file runs < 30s. For the cost-parity test the structural assertion matters most: grep-free, runtime proof that benchmarks and regime strategy share run_strategy_backtest — e.g., call each public benchmark runner and assert they accept/forward the same params object, plus compare the PercentageCommission rate and SpreadSlippage bps attributes captured from a spy/monkeypatched SimulatedExecutionHandler constructor.
  </action>
  <verify>
    <automated>cd portfolio_projects/macroregime && ../../quant/bin/python -m pytest tests/test_benchmarks.py -v</automated>
  </verify>
  <done>Both tests green; risk-parity as-of property holds; all four strategies demonstrably share one cost/engine configuration.</done>
</task>

</tasks>

<verification>
- `cd portfolio_projects/macroregime && ../../quant/bin/python -m pytest tests/ -q` green.
- `grep -rn "SpreadSlippage\|PercentageCommission" src/macroregime/ | grep -v benchmarks.py` returns nothing outside test files — engine assembly centralized in run_strategy_backtest.
</verification>

<success_criteria>
- MCR-07: 60/40, equal weight, and inverse-vol risk parity all runnable over identical periods with identical costs through the same engine path as the regime strategy.
</success_criteria>

<output>
After completion, create `.planning/phases/03-macroregime/03-06-SUMMARY.md`
</output>
