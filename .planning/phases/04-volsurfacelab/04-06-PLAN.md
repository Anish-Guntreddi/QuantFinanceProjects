---
phase: 04-volsurfacelab
plan: 06
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04", "04-05"]
files_modified:
  - portfolio_projects/volsurfacelab/src/volsurfacelab/pipeline.py
  - portfolio_projects/volsurfacelab/src/volsurfacelab/report.py
  - portfolio_projects/volsurfacelab/tests/test_pipeline.py
  - portfolio_projects/volsurfacelab/tests/test_report.py
autonomous: true
requirements: [VSL-04]

must_haves:
  truths:
    - "VolSurfacePipeline.run() executes chain -> IV solve -> SVI calibration + no-arb gate -> RV forecasts -> VRP strategy and returns a frozen PipelineResults"
    - "Pipeline solves IVs from PRICES via robust_iv (true_iv used only as test oracle, never as a pipeline input)"
    - "ReportBuilder writes smile_T{X}.png per maturity, surface_3d.png and surface_heatmap.png covering k in [-1.5,1.5] and T in [0.25,1.0], without exceptions under the Agg backend"
    - "summary.md lands in parent(figures_dir) and contains the forecast comparison table (QLIKE/MSE/DM) and the Greeks risk summary"
    - "quick=True completes in under ~30 seconds for fast integration testing"
  artifacts:
    - path: "portfolio_projects/volsurfacelab/src/volsurfacelab/pipeline.py"
      provides: "VolSurfacePipeline orchestrator, frozen PipelineResults, load_config"
      exports: ["VolSurfacePipeline", "PipelineResults", "load_config"]
      min_lines: 100
    - path: "portfolio_projects/volsurfacelab/src/volsurfacelab/report.py"
      provides: "ReportBuilder — smile/3D/heatmap figures, P&L curve, forecast + Greeks tables, summary.md"
      contains: "matplotlib.use(\"Agg\")"
      min_lines: 120
    - path: "portfolio_projects/volsurfacelab/tests/test_report.py"
      provides: "figure files exist and are non-empty; Agg backend; summary.md content checks"
      min_lines: 40
  key_links:
    - from: "portfolio_projects/volsurfacelab/src/volsurfacelab/pipeline.py"
      to: "volsurfacelab.iv_solver"
      via: "solve_chain_iv on chain prices (not true_iv)"
      pattern: "solve_chain_iv"
    - from: "portfolio_projects/volsurfacelab/src/volsurfacelab/pipeline.py"
      to: "volsurfacelab.svi"
      via: "calibrate_surface + validate gate before any surface plotting"
      pattern: "calibrate_surface"
    - from: "portfolio_projects/volsurfacelab/src/volsurfacelab/report.py"
      to: "mpl_toolkits.mplot3d"
      via: "3D surface plot of fitted SVI IV over (k, T) grid"
      pattern: "mplot3d|projection=.3d."
---

<objective>
Assemble the end-to-end pipeline (VolSurfacePipeline + frozen PipelineResults) wiring all four wave-2 modules in dependency order, and build the ReportBuilder: smile/skew plots per maturity (observed IV points vs fitted SVI curve), 3D surface and heatmap plots, RV forecast comparison table, VRP strategy P&L curve (gross vs net), and the Greeks risk summary — figures via matplotlib Agg, markdown summary alongside.

Purpose: VSL-04 — phase success criterion #3 — plus the integration seam that the runner (plan 04-07) calls. The pipeline is where the no-arb gate actually gates: only validated slices reach plotting and strategy entry.
Output: pipeline.py + report.py with pipeline and figure tests green.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-volsurfacelab/04-RESEARCH.md
@portfolio_projects/volsurfacelab/src/volsurfacelab/chain.py
@portfolio_projects/volsurfacelab/src/volsurfacelab/iv_solver.py
@portfolio_projects/volsurfacelab/src/volsurfacelab/svi.py
@portfolio_projects/volsurfacelab/src/volsurfacelab/forecast.py
@portfolio_projects/volsurfacelab/src/volsurfacelab/strategy.py
@portfolio_projects/macroregime/src/macroregime/report

<interfaces>
Wave-2 module contracts (read the actual files above for exact signatures):
- iv_solver: `solve_chain_iv(chain) -> DataFrame with 'iv' column` (from prices; NaN on failures)
- svi: `calibrate_surface(chain_or_iv_frame) -> ({T: SVISliceFit}, excluded)`; `svi_w(k, *params)` for plotting fitted curves; NOTE: pipeline must calibrate on SOLVED ivs (w_obs = iv^2*T), not true_iv — adapt calibrate_surface input accordingly (it accepts k/w arrays per slice via fit_svi_slice if needed)
- forecast: `compare_forecasts(returns, train_frac) -> ForecastComparison` (table, dm_pvalues, convergence)
- strategy: `run_vrp_strategy(chain, returns, ...) -> VRPResult` (gross_pnl, net_pnl, vrp_series, greeks_summary)

Locked phase-3 conventions to mirror:
- matplotlib.use("Agg") at report module import BEFORE pyplot (qbacktest tearsheet pattern)
- summary.md written to parent(figures_dir): output layout reports/figures/*.png + reports/summary.md
- Frozen results dataclass; pipeline returns results in-process (no subprocess in tests)
- report.py is imported lazily by __init__ in plan 04-08 — keep pyplot imports inside report.py only
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VolSurfacePipeline + PipelineResults + test_pipeline.py</name>
  <files>portfolio_projects/volsurfacelab/src/volsurfacelab/pipeline.py, portfolio_projects/volsurfacelab/tests/test_pipeline.py</files>
  <behavior>
    - test_pipeline_runs_end_to_end: VolSurfacePipeline(seed=42, quick=True).run() returns PipelineResults without exception
    - test_results_frozen: dataclasses.FrozenInstanceError when assigning to a PipelineResults field
    - test_pipeline_uses_solved_iv: results.iv_frame['iv'] differs from true_iv by < 1e-6 but is produced by robust_iv (assert column present and pipeline path calls solve_chain_iv — verify via results.iv_frame having NaN-capable solved values, and a monkeypatch test that breaks robust_iv and observes pipeline IVs change/fail)
    - test_no_arb_gate_wired: pipeline run with a chain whose surface includes a planted calendar violation (make_calendar_violating_surface used to build a custom generator surface) yields results.excluded_slices non-empty and the violating maturity absent from results.svi_fits
    - test_forecast_and_strategy_attached: results.forecast.table has 3 model rows; results.vrp.net_pnl is a non-empty Series; results.config_used carries seed and cost_rate
  </behavior>
  <action>
Write tests first (RED commit `test(04-06): failing pipeline assembly tests`), then implement src/volsurfacelab/pipeline.py:

1. `load_config(path=None) -> dict` — yaml.safe_load of configs/volsurfacelab.yaml by default (resolve relative to the package via importlib.resources or Path(__file__) walk-up to the project dir; accept explicit path override for tests).
2. `PipelineResults` frozen dataclass: chain (ChainData), iv_frame (pd.DataFrame), svi_fits (dict[float, SVISliceFit]), excluded_slices (tuple), forecast (ForecastComparison), vrp (VRPResult), config_used (dict), seed (int).
3. `VolSurfacePipeline(config=None, seed=42, quick=False)` with `.run() -> PipelineResults`:
   a. Generate chain (SyntheticChainGenerator from config params) + underlying returns (n_days reduced to 400 when quick=True).
   b. validate_chain_coverage — fail fast on a malformed chain.
   c. solve_chain_iv on PRICES (the honest path — true_iv stays a test-only oracle; document this in the docstring as the leakage-equivalent discipline of this phase).
   d. Build per-maturity (k, w_obs = iv^2 * T) from solved IVs (drop NaN rows) and calibrate via fit_svi_slice per slice + validate_surface gate; collect excluded slices with reasons. If ALL slices are excluded, raise RuntimeError (nothing downstream is meaningful).
   e. compare_forecasts on underlying returns (train_frac from config; n_restarts reduced to 2 when quick=True).
   f. run_vrp_strategy with cost rates from config.
   g. Assemble frozen PipelineResults.
4. Allow dependency injection of the generator surface (param `svi_surface=None` defaulting to SYNTHETIC_SVI_SURFACE) so the planted-arb pipeline test can run end-to-end.

Commit `feat(04-06): VolSurfacePipeline end-to-end assembly with no-arb gating`.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/test_pipeline.py -q</automated>
  </verify>
  <done>Pipeline runs end-to-end on synthetic data in quick mode; results frozen; gate exclusions propagate to results; solved IVs (not true_iv) feed calibration.</done>
</task>

<task type="auto">
  <name>Task 2: ReportBuilder — smile/3D/heatmap figures, P&L curve, tables, summary.md + test_report.py</name>
  <files>portfolio_projects/volsurfacelab/src/volsurfacelab/report.py, portfolio_projects/volsurfacelab/tests/test_report.py</files>
  <action>
Implement src/volsurfacelab/report.py:

1. Module top: `import matplotlib; matplotlib.use("Agg")` BEFORE any pyplot import (locked qbacktest/macroregime convention).
2. `ReportBuilder(results: PipelineResults, output_dir: Path)` where output_dir is the figures dir (e.g. reports/figures). `.build() -> dict[str, Path]` producing:
   - `smile_T{T}.png` per validated maturity: scatter of solved IVs vs k, overlaid fitted SVI IV curve sqrt(svi_w(k_dense, *params)/T) over k in [-1.5, 1.5]; annotate ATM IV.
   - `surface_3d.png`: mpl_toolkits.mplot3d plot_surface of fitted IV over meshgrid(k in [-1.5,1.5] x T from validated slices); labeled axes.
   - `surface_heatmap.png`: seaborn heatmap of the same IV grid (T rows, k columns).
   - `vrp_pnl.png`: cumulative gross vs net strategy P&L on one axes, plus the VRP series on a twin/secondary panel.
   - `forecast_qlike.png`: bar chart of QLIKE per model.
   - `summary.md` written to output_dir.parent (locked phase-3 decision: figures under figures/, summary.md beside it): sections — Surface (validated/excluded slice counts, per-slice SVI params table), RV Forecast Comparison (markdown table of QLIKE/MSE + DM p-values, convergence flags), Strategy (gross/net total P&L, total costs, mean VRP, entry IV), Greeks Risk Summary (results.vrp.greeks_summary rendered as markdown table).
3. Close every figure (plt.close(fig)) after savefig — no open-figure leaks across tests.
4. tests/test_report.py: build into tmp_path/figures and assert (a) all expected PNGs exist and have size > 0, (b) tmp_path/summary.md exists and contains "QLIKE", "Greeks", and "net" substrings, (c) matplotlib backend is Agg after importing volsurfacelab.report, (d) excluded slices (if any injected) are named in summary.md. Use a session/module-scope quick pipeline result fixture to keep runtime low.

Commit `feat(04-06): ReportBuilder — smile/3D/heatmap surface figures, forecast and Greeks tables`.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/test_report.py -q && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>All figure files generated non-empty under Agg; summary.md beside figures dir with forecast table and Greeks summary; full suite green.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_pipeline.py tests/test_report.py -q` green
- Pipeline path: prices -> robust_iv -> SVI gate -> plots/strategy (true_iv only in tests)
- Figures cover the full synthetic moneyness/maturity range (k in [-1.5,1.5], T in {0.25,0.5,1.0})
- Full suite still green (~all wave-2 modules unbroken)
</verification>

<success_criteria>
- VSL-04 satisfied: smile/skew per maturity + 3D and heatmap surface plots produced and verified non-empty, covering the specified chain range
- Pipeline seam ready for the one-command runner (plan 04-07)
</success_criteria>

<output>
After completion, create `.planning/phases/04-volsurfacelab/04-06-SUMMARY.md`
</output>
