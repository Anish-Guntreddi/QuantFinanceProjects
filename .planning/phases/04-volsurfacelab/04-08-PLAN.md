---
phase: 04-volsurfacelab
plan: 08
type: execute
wave: 5
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05", "04-06", "04-07"]
files_modified:
  - portfolio_projects/volsurfacelab/src/volsurfacelab/__init__.py
  - portfolio_projects/volsurfacelab/tests/test_api.py
autonomous: true
requirements: [QUAL-01, QUAL-04]

must_haves:
  truths:
    - "volsurfacelab exposes a frozen public API via __all__ (chain, iv_solver, svi, forecast, strategy, pipeline, report entry points)"
    - "Importing volsurfacelab does NOT import matplotlib.pyplot or yfinance (ReportBuilder and load_yfinance_chain behind lazy __getattr__)"
    - "Full strict suite passes twice in a row with identical results (deterministic, offline, FutureWarning-as-error)"
    - "Codex read-only audit completes with all findings triaged: fixed in this plan or explicitly accepted with rationale in the SUMMARY"
  artifacts:
    - path: "portfolio_projects/volsurfacelab/src/volsurfacelab/__init__.py"
      provides: "public API: __all__, direct imports for light modules, lazy __getattr__ for ReportBuilder/load_yfinance_chain"
      contains: "__getattr__"
      min_lines: 40
    - path: "portfolio_projects/volsurfacelab/tests/test_api.py"
      provides: "API surface test: __all__ resolvable, lazy imports deferred, no pyplot at import"
      min_lines: 25
  key_links:
    - from: "portfolio_projects/volsurfacelab/src/volsurfacelab/__init__.py"
      to: "volsurfacelab.report"
      via: "lazy __getattr__ (macroregime ReportBuilder pattern)"
      pattern: "def __getattr__"
---

<objective>
Quality gate for Phase 4: freeze the public API (__all__ + lazy __getattr__ for heavy/optional imports), prove the strict suite is deterministic by running it twice, and pass the codex read-only audit (correctness focus: IV solver edge handling, SVI gate logic, QLIKE/DM conventions, no-look-ahead in HAR/strategy paths, standalone accounting).

Purpose: QUAL-01 and QUAL-04 — the locked per-phase completion gate. Phase 4 is not complete until codex findings are resolved.
Output: Frozen API, double-green strict suite, codex audit clean.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-volsurfacelab/04-RESEARCH.md
@portfolio_projects/macroregime/src/macroregime/__init__.py
@portfolio_projects/volsurfacelab/src/volsurfacelab/__init__.py

<interfaces>
Lazy-import convention (locked, macroregime pattern): heavy/optional symbols (ReportBuilder
pulls pyplot; load_yfinance_chain pulls yfinance) are resolved in module-level
`def __getattr__(name)` that imports on first access and raises AttributeError otherwise.
Light symbols are imported eagerly at the top.

Public API to freeze (final names from the implemented modules — read the source files):
- chain: SyntheticChainGenerator, ChainData, generate_underlying_returns, validate_chain_coverage, SYNTHETIC_SVI_SURFACE
- iv_solver: robust_iv, solve_chain_iv, bs_price
- svi: svi_w, g_func, fit_svi_slice, check_calendar_arb, validate_surface, calibrate_surface, SVISliceFit
- forecast: realized_variance, HARForecaster, fit_garch_robust, qlike, diebold_mariano, compare_forecasts, ForecastComparison
- strategy: OptionLeg, StandalonePortfolio, daily_gamma_pnl, compute_leg_greeks, portfolio_greeks_summary, run_vrp_strategy, VRPResult
- pipeline: VolSurfacePipeline, PipelineResults, load_config
- lazy: ReportBuilder, load_yfinance_chain
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API freeze — __all__ + lazy __getattr__ + test_api.py</name>
  <files>portfolio_projects/volsurfacelab/src/volsurfacelab/__init__.py, portfolio_projects/volsurfacelab/tests/test_api.py</files>
  <behavior>
    - test_all_symbols_resolvable: every name in volsurfacelab.__all__ resolves via getattr without error
    - test_lazy_report_import: after `importlib.reload`-fresh import of volsurfacelab in a subprocess (python -c), 'matplotlib.pyplot' not in sys.modules and 'yfinance' not in sys.modules; accessing volsurfacelab.ReportBuilder THEN pulls report
    - test_unknown_attribute_raises: getattr(volsurfacelab, 'nope') raises AttributeError
    - test_version: __version__ == "0.1.0"
  </behavior>
  <action>
Rewrite src/volsurfacelab/__init__.py following the macroregime pattern: docstring documenting the public API by section (Chain / IV / SVI / Forecast / Strategy / Pipeline / Report), eager imports of all light symbols from the interfaces list (verify exact names against the implemented source files first — if a module renamed something, the SOURCE is authoritative), `__all__` tuple covering every public symbol including the lazy ones, and `def __getattr__(name)` lazily importing ReportBuilder (from volsurfacelab.report) and load_yfinance_chain (from volsurfacelab.chain — note: only lazy if chain.py module-scope stays yfinance-free, which it must; ReportBuilder is the pyplot-deferral case). Write tests/test_api.py per the behavior list — the subprocess check uses `python -c "import volsurfacelab, sys; assert 'matplotlib.pyplot' not in sys.modules; assert 'yfinance' not in sys.modules"`.

Commit `feat(04-08): freeze public API with lazy ReportBuilder import`.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/test_api.py -q && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -c "import volsurfacelab, sys; assert 'matplotlib.pyplot' not in sys.modules"</automated>
  </verify>
  <done>__all__ frozen and fully resolvable; pyplot/yfinance deferred to first access; test_api.py green.</done>
</task>

<task type="auto">
  <name>Task 2: Strict suite x2 + codex read-only audit + finding resolution</name>
  <files>portfolio_projects/volsurfacelab/src/volsurfacelab/__init__.py</files>
  <action>
1. Run the full strict suite TWICE from portfolio_projects/volsurfacelab/ and confirm identical pass counts (determinism check): `python -m pytest tests/ -v --tb=short --durations=10` (quant venv). Any flaky test is a blocker — fix before proceeding.
2. Run the codex read-only audit (locked phase-gate decision):
   `codex exec --sandbox read-only "Review portfolio_projects/volsurfacelab for correctness. Focus: (1) IV solver — can robust_iv ever raise or return a garbage vol? (2) SVI no-arb gate — can a violated slice reach plotting or strategy entry? Is the calendar check correctly restricted to the traded k-range? (3) QLIKE — is the Patton convention (rv/h - log(rv/h) - 1) implemented with the correct asymmetry? (4) Look-ahead — do HAR regressors, GARCH OOS forecasts, or the VRP strategy use any data from time t or later to act at time t? (5) Standalone accounting — does cash + positions + costs reconcile; any qbacktest import? (6) Greeks — theta/252 convention; sign conventions. Report concrete findings with file:line."`
3. Triage every finding: fix real bugs in the relevant module (small fixes belong in this plan; a structural gap means filing a gap-closure plan via /gsd:plan-phase --gaps), re-run the suite after each fix, and record accepted-as-is findings with rationale for the SUMMARY.
4. Re-run the strict suite once more after fixes; commit any code changes as `fix(04-08): resolve codex audit findings — <description>`.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/ -q && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>Strict suite green twice consecutively; codex audit run; all findings fixed or explicitly accepted with rationale documented for the SUMMARY.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -q` passes twice in a row with identical results, offline, FutureWarning-as-error
- `python -c "import volsurfacelab"` imports cleanly without pyplot/yfinance side effects
- Codex read-only review executed; zero unresolved findings
</verification>

<success_criteria>
- QUAL-01 and QUAL-04 satisfied for Phase 4; public API frozen; phase ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/04-volsurfacelab/04-08-SUMMARY.md`
</output>
