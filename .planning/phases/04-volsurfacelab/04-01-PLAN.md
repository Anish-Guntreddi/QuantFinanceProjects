---
phase: 04-volsurfacelab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - portfolio_projects/volsurfacelab/pyproject.toml
  - portfolio_projects/volsurfacelab/requirements.txt
  - portfolio_projects/volsurfacelab/configs/volsurfacelab.yaml
  - portfolio_projects/volsurfacelab/src/volsurfacelab/__init__.py
  - portfolio_projects/volsurfacelab/src/volsurfacelab/chain.py
  - portfolio_projects/volsurfacelab/tests/conftest.py
  - portfolio_projects/volsurfacelab/tests/test_chain.py
  - portfolio_projects/volsurfacelab/tests/test_iv_solver.py
  - portfolio_projects/volsurfacelab/tests/test_svi.py
  - portfolio_projects/volsurfacelab/tests/test_forecast.py
  - portfolio_projects/volsurfacelab/tests/test_strategy.py
  - portfolio_projects/volsurfacelab/tests/test_integration.py
autonomous: true
requirements: [VSL-01]

must_haves:
  truths:
    - "After editable install, `import volsurfacelab` succeeds from any directory using the quant venv"
    - "SyntheticChainGenerator with the same seed produces byte-identical options DataFrames and underlying return series on repeated calls"
    - "Generated chain covers log-moneyness k in [-1.5, 1.5] across maturities {0.25, 0.5, 1.0} with both calls and puts, and records true_iv per row"
    - "validate_chain_coverage flags a chain missing a maturity or with truncated moneyness range"
    - "Full test suite runs offline; Wave-0 stubs for unimplemented modules skip cleanly (no errors, no network)"
  artifacts:
    - path: "portfolio_projects/volsurfacelab/pyproject.toml"
      provides: "hatchling build, src layout, pytest config with filterwarnings error::FutureWarning"
      contains: "error::FutureWarning"
    - path: "portfolio_projects/volsurfacelab/src/volsurfacelab/chain.py"
      provides: "SyntheticChainGenerator, ChainData, generate_underlying_returns, validate_chain_coverage, planted-arb helpers, lazy yfinance loader"
      exports: ["SyntheticChainGenerator", "ChainData", "generate_underlying_returns", "validate_chain_coverage", "SYNTHETIC_SVI_SURFACE"]
      min_lines: 120
    - path: "portfolio_projects/volsurfacelab/tests/conftest.py"
      provides: "fix_seeds autouse fixture + session-scope chain and underlying-path fixtures"
      contains: "session"
    - path: "portfolio_projects/volsurfacelab/configs/volsurfacelab.yaml"
      provides: "chain params, SVI calibration grid, GARCH restarts, strategy cost rates"
  key_links:
    - from: "portfolio_projects/volsurfacelab/tests/conftest.py"
      to: "volsurfacelab.chain"
      via: "package import (no sys.path hacks)"
      pattern: "from volsurfacelab.chain import"
    - from: "portfolio_projects/volsurfacelab/src/volsurfacelab/chain.py"
      to: "yfinance"
      via: "lazy import inside function body (never module scope)"
      pattern: "def load_yfinance_chain"
---

<objective>
Create the volsurfacelab package skeleton (src layout, hatchling, editable install into the quant venv), Wave-0 test stubs for every downstream module, and the deterministic synthetic data layer: an options chain generated from a known SVI surface (exact ground truth for all downstream oracle tests) plus a seeded GARCH(1,1) underlying return path (ground truth for RV forecasting and strategy P&L).

Purpose: VSL-01 plus the shared test fixtures every other plan in this phase consumes. The chain is generated FROM known SVI parameters via Black-Scholes so IV round-trip (VSL-02) and SVI recovery (VSL-03) have exact answers.
Output: Installed package, green test_chain.py, skipping stubs for all other test files.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-volsurfacelab/04-RESEARCH.md
@portfolio_projects/macroregime/pyproject.toml

<interfaces>
Ground-truth SVI surface (verified in quant venv — butterfly g(k) > 0.21 at all maturities, calendar compliant in [-1.5, 1.5]; same b, rho, m, sigma across T means w(k,T2)-w(k,T1) = a2-a1, so calendar monotonicity holds everywhere iff a increases with T):

```python
SYNTHETIC_SVI_SURFACE = {
    # T: (a, b, rho, m, sigma) — ATM IVs: 0.25y ~25%, 0.5y ~22%, 1.0y ~20%
    0.25: (-0.0084, 0.08, -0.3, 0.0, 0.3),
    0.50: ( 0.0002, 0.08, -0.3, 0.0, 0.3),
    1.00: ( 0.0160, 0.08, -0.3, 0.0, 0.3),
}
# w(k) = a + b*(rho*(k-m) + sqrt((k-m)^2 + sigma^2));  true_iv = sqrt(w(k,T)/T)
```

ChainData contract (frozen — downstream plans consume this exact shape):

```python
@dataclass(frozen=True)
class ChainData:
    options: pd.DataFrame   # columns: T, K, k, flag('c'/'p'), price, true_iv, forward
    spot: float             # 100.0
    risk_free: float        # 0.05
    seed: int
```

BS pricing for chain generation (vollib namespace, NOT py_vollib — DeprecationWarning):
```python
from vollib.black_scholes import black_scholes  # black_scholes(flag, S, K, t, r, sigma)
```

Underlying path DGP (gives GARCH/EGARCH something real to recover in plan 04-04):
```python
# GARCH(1,1): h_t = omega + alpha*r_{t-1}^2 + beta*h_{t-1}; r_t = sqrt(h_t)*z_t
# omega=2e-6, alpha=0.08, beta=0.90 -> long-run annualized vol ~15.9%
# Single seeded np.random.default_rng(seed) — no global np.random (Phase 2 locked convention)
def generate_underlying_returns(seed: int = 42, n_days: int = 750,
                                omega: float = 2e-6, alpha: float = 0.08,
                                beta: float = 0.90) -> pd.Series:
    ...  # business-day DatetimeIndex starting 2020-01-01
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package skeleton, pyproject, config YAML, Wave-0 test stubs, editable install</name>
  <files>portfolio_projects/volsurfacelab/pyproject.toml, portfolio_projects/volsurfacelab/requirements.txt, portfolio_projects/volsurfacelab/configs/volsurfacelab.yaml, portfolio_projects/volsurfacelab/src/volsurfacelab/__init__.py, portfolio_projects/volsurfacelab/tests/conftest.py, portfolio_projects/volsurfacelab/tests/test_iv_solver.py, portfolio_projects/volsurfacelab/tests/test_svi.py, portfolio_projects/volsurfacelab/tests/test_forecast.py, portfolio_projects/volsurfacelab/tests/test_strategy.py, portfolio_projects/volsurfacelab/tests/test_integration.py</files>
  <action>
Create portfolio_projects/volsurfacelab/ mirroring the macroregime conventions (hatchling, src layout):

1. pyproject.toml: name "volsurfacelab", version 0.1.0, requires-python ">=3.11". Dependencies: numpy>=2.0, pandas>=2.1,<3.0, scipy>=1.10, statsmodels>=0.14, arch>=7.0, py-vollib>=1.0, matplotlib>=3.8, seaborn>=0.13, pyyaml>=6.0. Optional extra real-data = ["yfinance>=0.2"]. Do NOT depend on qbacktest — VolSurfaceLab is architecturally independent (locked roadmap decision). [tool.hatch.build.targets.wheel] packages = ["src/volsurfacelab"]. [tool.pytest.ini_options]: testpaths=["tests"], filterwarnings=["error::FutureWarning"].
2. requirements.txt: pin the same deps (versions already in quant venv: numpy 2.2.6, pandas 2.3.2, scipy 1.16.1, statsmodels 0.14.5, arch 7.2.0, py-vollib 1.0.12, matplotlib 3.10.6, seaborn 0.13.2).
3. configs/volsurfacelab.yaml with sections: chain (spot 100.0, risk_free 0.05, maturities [0.25, 0.5, 1.0], n_strikes 13, k_min -1.5, k_max 1.5, seed 42), underlying (n_days 750, seed 42, garch: omega 2.0e-6, alpha 0.08, beta 0.90), svi (k_constraint: [-3.0, 3.0, 100 points], calendar_k_range: [-1.5, 1.5], n_restarts 5), forecast (train_frac 0.67, dm_max_lags 4, garch_restarts 5), strategy (initial_cash 100000.0, cost_rate 0.001, delta_hedge_cost_rate 0.001), report (output_dir "reports").
4. src/volsurfacelab/__init__.py: docstring + __version__ = "0.1.0" only (full public API frozen in plan 04-08).
5. tests/conftest.py: fix_seeds autouse fixture (np.random.seed(42) defensive reset; all production code uses default_rng). Session-scope fixtures `chain` and `underlying_returns` that import from volsurfacelab.chain — guard with pytest.importorskip is NOT needed since Task 2 in this same plan implements chain.py; write them against the final API.
6. Wave-0 stubs: tests/test_iv_solver.py, test_svi.py, test_forecast.py, test_strategy.py, test_integration.py each containing module docstring naming the requirement (VSL-02, VSL-03, VSL-05, VSL-06/07, VSL-08) and one `@pytest.mark.skip(reason="Wave 0 stub — implemented in plan 04-0N")` placeholder test.
7. Install: `/Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pip install -e portfolio_projects/volsurfacelab`.

No sys.path hacks anywhere (Phase 2 locked convention).
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -c "import volsurfacelab; print(volsurfacelab.__version__)" && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>Editable install succeeds; `import volsurfacelab` prints 0.1.0; pytest collects all stub files with skips and zero failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SyntheticChainGenerator + underlying GARCH path + coverage validation + test_chain.py</name>
  <files>portfolio_projects/volsurfacelab/src/volsurfacelab/chain.py, portfolio_projects/volsurfacelab/tests/test_chain.py, portfolio_projects/volsurfacelab/tests/conftest.py</files>
  <behavior>
    - Determinism: two generators with seed=42 produce identical options DataFrames (pd.testing.assert_frame_equal) and identical return series
    - Coverage: chain has exactly 3 maturities x 13 strikes x 2 flags = 78 rows; k spans [-1.5, 1.5]; all prices > 0; true_iv in (0.05, 1.0)
    - Ground truth consistency: for each row, true_iv == sqrt(w(k, T)/T) of SYNTHETIC_SVI_SURFACE params to 1e-12
    - validate_chain_coverage raises/returns failure for a chain with a maturity dropped or k range truncated to [-0.5, 0.5]
    - Underlying path: len == n_days, no NaN, annualized vol of full sample in (0.08, 0.30), deterministic under fixed seed
    - Planted-arb helpers: make_butterfly_violating_params returns params with min g(k) < 0; make_calendar_violating_surface returns surface where a decreases between two maturities
  </behavior>
  <action>
Implement src/volsurfacelab/chain.py:

1. `SYNTHETIC_SVI_SURFACE` constant and a module-private `_svi_total_variance(k, a, b, rho, m, sigma)` (the 1-line w(k) formula). This is deliberately duplicated from the future svi.py calibrator so generator and calibrator are INDEPENDENT implementations — SVI recovery in plan 04-03 is then a genuine oracle, and wave-2 plans stay parallel.
2. `ChainData` frozen dataclass per the interfaces block.
3. `SyntheticChainGenerator(spot=100.0, risk_free=0.05, maturities=(0.25,0.5,1.0), n_strikes=13, k_min=-1.5, k_max=1.5, seed=42)` with `.generate() -> ChainData`: forward F = S*exp(r*T); strikes K = F*exp(k) on the k grid; true_iv = sqrt(w(k,T)/T); price both calls and puts via `from vollib.black_scholes import black_scholes`. Columns exactly: T, K, k, flag, price, true_iv, forward. Generation is fully deterministic (no noise in v1 — exact ground truth; the seed field is recorded for API stability).
4. `generate_underlying_returns(seed=42, n_days=750, omega=2e-6, alpha=0.08, beta=0.90) -> pd.Series` — GARCH(1,1) DGP per interfaces block, single np.random.default_rng(seed), h_0 = omega/(1-alpha-beta), business-day DatetimeIndex from 2020-01-01 via pd.bdate_range.
5. `validate_chain_coverage(chain: ChainData, required_maturities, k_min, k_max) -> None` — raises ValueError naming the missing maturity or the uncovered moneyness side.
6. Negative-test helpers: `make_butterfly_violating_params()` returning (0.04, 1.5, -0.9, 0.0, 0.05) (extreme params, verified violating) and `make_calendar_violating_surface()` returning SYNTHETIC_SVI_SURFACE with a(T=0.5) set below a(T=0.25).
7. `load_yfinance_chain(ticker: str)` — optional real-data path; `import yfinance` INSIDE the function body (lazy import, macroregime FredMacroLoader pattern); re-solve nothing here, just return raw calls/puts frames with a clear docstring that IVs must be re-solved via robust_iv (plan 04-02). No test may touch the network — only test that `inspect.getsource` shows no module-scope yfinance import, or simply that importing volsurfacelab.chain does not import yfinance (assert 'yfinance' not in sys.modules after fresh import).

Write tests in tests/test_chain.py FIRST for the behaviors above (TDD: commit failing tests with `test(04-01): ...`, then implement and commit `feat(04-01): ...`). Finalize conftest.py session fixtures: `chain` (SyntheticChainGenerator().generate()) and `underlying_returns` (generate_underlying_returns()).
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/test_chain.py -q && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>test_chain.py fully green (determinism, coverage, ground-truth consistency, validation failures, planted-arb helpers); full suite green with stubs skipping; chain and underlying_returns fixtures available session-wide.</done>
</task>

</tasks>

<verification>
- `pip show volsurfacelab` reports editable install from portfolio_projects/volsurfacelab
- `python -m pytest tests/ -q` from the package dir: test_chain green, 5 stub files skipping, zero failures, no network access
- Importing volsurfacelab.chain does not import yfinance or matplotlib.pyplot
</verification>

<success_criteria>
- VSL-01 satisfied on the synthetic default path: deterministic chain with validated moneyness/maturity coverage; optional yfinance loader present behind lazy import
- All downstream plans can consume the `chain` and `underlying_returns` fixtures and SYNTHETIC_SVI_SURFACE ground truth
</success_criteria>

<output>
After completion, create `.planning/phases/04-volsurfacelab/04-01-SUMMARY.md`
</output>
