---
phase: 04-volsurfacelab
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - portfolio_projects/volsurfacelab/src/volsurfacelab/iv_solver.py
  - portfolio_projects/volsurfacelab/tests/test_iv_solver.py
autonomous: true
requirements: [VSL-02]

must_haves:
  truths:
    - "robust_iv recovers true_iv for every row of the synthetic chain (calls and puts, all maturities) with absolute error < 1e-6"
    - "A below-intrinsic price returns float('nan') — no exception escapes robust_iv under any input"
    - "A near-zero deep-OTM price returns NaN or a positive finite vol — never raises, never returns negative/inf"
    - "The brentq fallback recovers IV when the LetsBeRational primary path is bypassed (verified by direct fallback test)"
    - "solve_chain_iv returns the chain options frame with an 'iv' column; failed rows hold NaN, success rate is reported"
  artifacts:
    - path: "portfolio_projects/volsurfacelab/src/volsurfacelab/iv_solver.py"
      provides: "robust_iv (LetsBeRational + brentq fallback, never raises), solve_chain_iv (chain-wide solver), bs_price helper"
      exports: ["robust_iv", "solve_chain_iv", "bs_price"]
      min_lines: 60
    - path: "portfolio_projects/volsurfacelab/tests/test_iv_solver.py"
      provides: "round-trip 1e-6 oracle, graceful-failure tests, fallback test"
      min_lines: 60
  key_links:
    - from: "portfolio_projects/volsurfacelab/src/volsurfacelab/iv_solver.py"
      to: "py_lets_be_rational.exceptions"
      via: "BelowIntrinsicException/AboveMaximumException handling"
      pattern: "from py_lets_be_rational.exceptions import"
    - from: "portfolio_projects/volsurfacelab/tests/test_iv_solver.py"
      to: "tests/conftest.py chain fixture"
      via: "round-trip against ChainData.options true_iv column"
      pattern: "true_iv"
---

<objective>
Build the robust implied-volatility solver: vollib LetsBeRational as the fast primary path, scipy brentq bisection as the fallback, NaN (never an exception) on irrecoverable inputs. Round-trip the entire synthetic chain to 1e-6 against known true_iv.

Purpose: VSL-02 — phase success criterion #1. Every downstream consumer (SVI calibration on real chains, strategy entry IVs) assumes this solver never crashes the pipeline on a bad quote.
Output: iv_solver.py with green oracle tests.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-volsurfacelab/04-RESEARCH.md
@portfolio_projects/volsurfacelab/src/volsurfacelab/chain.py
@portfolio_projects/volsurfacelab/tests/conftest.py

<interfaces>
Verified solver pattern (04-RESEARCH.md, executed live in quant venv — vollib namespace, exception types live in py_lets_be_rational, NOT vollib.helper):

```python
from vollib.black_scholes.implied_volatility import implied_volatility as _lbr_iv
from py_lets_be_rational.exceptions import BelowIntrinsicException, AboveMaximumException
from scipy.optimize import brentq
from scipy.stats import norm

def robust_iv(price: float, S: float, K: float, T: float, r: float, flag: str) -> float:
    """Implied vol or float('nan') on irrecoverable failure. NEVER raises."""
    # 1. LetsBeRational primary (catch Below/Above -> nan; catch generic -> fall through)
    # 2. brentq on bs_price(sigma) - price over [1e-6, 20.0], xtol=1e-10, maxiter=200
```

Chain fixture contract (from plan 04-01): `chain.options` columns T, K, k, flag, price, true_iv, forward; chain.spot=100.0, chain.risk_free=0.05.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing oracle tests for round-trip, graceful failure, and fallback</name>
  <files>portfolio_projects/volsurfacelab/tests/test_iv_solver.py</files>
  <behavior>
    - test_round_trip_full_chain: for every row in chain.options, abs(robust_iv(price, spot, K, T, r, flag) - true_iv) < 1e-6 (calls AND puts — both flags exercised)
    - test_below_intrinsic_returns_nan: call with price = max(S - K*exp(-rT), 0) - 0.01 (below intrinsic, ITM call) returns NaN, no exception
    - test_above_maximum_returns_nan: call priced above S returns NaN, no exception
    - test_deep_otm_tiny_price: K = 3*S, T=0.05, price=1e-12 — returns NaN or finite positive vol, never raises, never negative/inf
    - test_zero_and_negative_price: price=0.0 and price=-0.5 both return NaN
    - test_brentq_fallback_recovers: call the module's brentq fallback path directly (expose as _brentq_iv or via monkeypatching _lbr_iv to raise RuntimeError) and assert it recovers a known vol to 1e-6
    - test_solve_chain_iv: returns DataFrame with 'iv' column, all 78 synthetic rows solved (zero NaN on the clean chain), and matches true_iv to 1e-6
  </behavior>
  <action>
Replace the Wave-0 stub in tests/test_iv_solver.py with the tests above, importing `from volsurfacelab.iv_solver import robust_iv, solve_chain_iv, bs_price` (module does not exist yet — tests MUST fail at import). Run pytest to confirm RED, then commit: `test(04-02): add failing IV round-trip and graceful-failure oracle tests`.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/test_iv_solver.py -q; test $? -ne 0</automated>
  </verify>
  <done>test_iv_solver.py fails on missing volsurfacelab.iv_solver; RED commit exists.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement robust_iv, bs_price, solve_chain_iv</name>
  <files>portfolio_projects/volsurfacelab/src/volsurfacelab/iv_solver.py</files>
  <action>
Implement src/volsurfacelab/iv_solver.py exactly per the verified research pattern:

1. `bs_price(sigma, S, K, T, r, flag)` — closed-form Black-Scholes via scipy.stats.norm (used by the fallback and reusable by tests; independent of vollib so the fallback truly is a second implementation).
2. `robust_iv(price, S, K, T, r, flag) -> float`:
   - Guard rails first: if price <= 0 or T <= 0 or S <= 0 or K <= 0, return nan immediately.
   - Primary: `_lbr_iv(price, S, K, T, r, flag)`; accept only finite result > 0. Catch (BelowIntrinsicException, AboveMaximumException) -> return nan (these are economically meaningless quotes, not solver failures). Catch any other Exception -> fall through to fallback.
   - Fallback: brentq on `lambda sig: bs_price(sig, S, K, T, r, flag) - price` over [1e-6, 20.0], xtol=1e-10, maxiter=200, wrapped in try/except returning nan. Note: brentq requires a sign change on the bracket; if f(1e-6) and f(20.0) share a sign, brentq raises ValueError which the except catches -> nan. This IS the vega-floor behavior: vanishing-vega quotes have no bracketing root and resolve to nan instead of a garbage vol.
3. `solve_chain_iv(chain: ChainData) -> pd.DataFrame` — copy of chain.options with an added 'iv' column from row-wise robust_iv, plus log (logging.getLogger(__name__).warning) of how many rows failed (NaN) out of total.
4. Module docstring stating the never-raises contract and the vollib-vs-py_vollib namespace decision (py_vollib emits DeprecationWarning).

Run pytest until green, then commit: `feat(04-02): robust IV solver — LetsBeRational + brentq fallback, 1e-6 round-trip`.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/test_iv_solver.py -q && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>All test_iv_solver.py tests green; full suite green; round-trip < 1e-6 on all 78 chain rows; no unhandled exception possible from robust_iv.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_iv_solver.py -q` green: round-trip oracle, below-intrinsic/above-maximum NaN, deep OTM/ITM graceful handling, fallback recovery
- grep confirms `from py_lets_be_rational.exceptions import` (correct exception source) and no `from py_vollib` imports
</verification>

<success_criteria>
- VSL-02 satisfied: 1e-6 round-trip on known-vol chain; deep OTM/ITM inputs handled gracefully (NaN, never an unhandled exception); brentq fallback demonstrated
</success_criteria>

<output>
After completion, create `.planning/phases/04-volsurfacelab/04-02-SUMMARY.md`
</output>
