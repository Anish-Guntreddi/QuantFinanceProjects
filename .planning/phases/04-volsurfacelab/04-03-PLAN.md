---
phase: 04-volsurfacelab
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - portfolio_projects/volsurfacelab/src/volsurfacelab/svi.py
  - portfolio_projects/volsurfacelab/tests/test_svi.py
autonomous: true
requirements: [VSL-03]

must_haves:
  truths:
    - "fit_svi_slice recovers the known SYNTHETIC_SVI_SURFACE parameters per maturity to ~1e-3 from the chain's total variance points (multi-restart SLSQP with butterfly constraint)"
    - "A slice fit with planted butterfly-violating parameters is excluded by validate_surface with a logged warning, not an exception"
    - "A calendar-violating surface (a decreasing in T) triggers exclusion of the longer slice with a logged warning"
    - "The clean synthetic surface passes both butterfly and calendar gates with zero exclusions (no false positives — calendar check restricted to traded k-range)"
    - "Downstream consumers receive only validated slices: calibrate_surface returns fits and the gate verdict together"
  artifacts:
    - path: "portfolio_projects/volsurfacelab/src/volsurfacelab/svi.py"
      provides: "svi_w/svi_wp/svi_wpp/g_func, fit_svi_slice (multi-restart SLSQP + g(k)>=0 constraint), check_calendar_arb, validate_surface, calibrate_surface, SVISliceFit dataclass"
      exports: ["svi_w", "g_func", "fit_svi_slice", "check_calendar_arb", "validate_surface", "calibrate_surface", "SVISliceFit"]
      min_lines: 120
    - path: "portfolio_projects/volsurfacelab/tests/test_svi.py"
      provides: "parameter-recovery oracle, planted butterfly/calendar arb negative tests, false-positive guard test"
      min_lines: 80
  key_links:
    - from: "portfolio_projects/volsurfacelab/src/volsurfacelab/svi.py"
      to: "scipy.optimize.minimize SLSQP"
      via: "inequality constraint {'type':'ineq','fun': g_func over k-grid}"
      pattern: "SLSQP"
    - from: "portfolio_projects/volsurfacelab/tests/test_svi.py"
      to: "volsurfacelab.chain"
      via: "SYNTHETIC_SVI_SURFACE ground truth + make_butterfly_violating_params/make_calendar_violating_surface"
      pattern: "make_(butterfly|calendar)_violating"
---

<objective>
Implement SVI calibration per maturity slice (raw SVI, 5 params, multi-restart SLSQP with the Gatheral-Jacquier g(k) >= 0 butterfly constraint) and the static no-arbitrage gate (butterfly convexity per slice + calendar monotonicity across slices, checked on the TRADED moneyness range only). Violated slices are logged and excluded — never silently passed downstream.

Purpose: VSL-03 — phase success criterion #2. The no-arb gate is the quality control that makes every downstream surface plot and strategy IV trustworthy.
Output: svi.py with parameter-recovery oracle and planted-arbitrage negative tests green.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-volsurfacelab/04-RESEARCH.md
@portfolio_projects/volsurfacelab/src/volsurfacelab/chain.py
@portfolio_projects/volsurfacelab/configs/volsurfacelab.yaml

<interfaces>
Verified SVI math (04-RESEARCH.md, executed live — SLSQP converged, recovered params to 4dp):

```python
# w(k)  = a + b*(rho*(k-m) + sqrt((k-m)^2 + sigma^2))
# w'(k) = b*(rho + (k-m)/sqrt((k-m)^2 + sigma^2))
# w''(k)= b*sigma^2 / ((k-m)^2 + sigma^2)^1.5
# g(k)  = (1 - k*w'/(2*w))^2 - (w'^2/4)*(1/w + 0.25) + w''/2   >= 0  (butterfly no-arb)

K_CONSTRAINT = np.linspace(-3.0, 3.0, 100)
SVI_BOUNDS = [(-0.5, 1.0), (1e-4, 2.0), (-0.999, 0.999), (-3.0, 3.0), (1e-4, 2.0)]  # a,b,rho,m,sigma
INITIAL_GUESSES = [
    (0.04, 0.3, -0.3, 0.0, 0.2), (0.02, 0.2, -0.1, -0.1, 0.3),
    (0.06, 0.4, -0.5, 0.1, 0.15), (0.01, 0.15, 0.0, 0.0, 0.4), (0.08, 0.5, -0.7, 0.0, 0.1),
]
# Objective: sum((svi_w(k_obs,*p) - w_obs)**2); accept only res.success; keep best res.fun
```

CRITICAL pitfall (verified): calendar monotonicity check MUST be restricted to the traded
k-range [-1.5, 1.5] (config calendar_k_range). Deep-wing |k| > 1.5 violations are
parameterization artifacts, not genuine arbitrage. Gate behavior: warnings.warn + exclude,
never raise — the rest of the surface proceeds.

Inputs from plan 04-01 (chain.py): chain.options has columns T, k, true_iv; total variance
w_obs = true_iv**2 * T per maturity slice. Negative-test helpers:
make_butterfly_violating_params() -> (0.04, 1.5, -0.9, 0.0, 0.05);
make_calendar_violating_surface() -> dict with a(T=0.5) < a(T=0.25).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing SVI recovery oracle and planted-arbitrage gate tests</name>
  <files>portfolio_projects/volsurfacelab/tests/test_svi.py</files>
  <behavior>
    - test_g_func_positive_on_clean_surface: g_func(k_grid, *params) > 0 for all SYNTHETIC_SVI_SURFACE slices over k in [-3, 3]
    - test_fit_recovers_known_params: for each maturity, build (k_obs, w_obs = true_iv^2 * T) from the chain fixture; fit_svi_slice recovers (a, b, rho, m, sigma) with np.allclose(atol=1e-3); fit.success is True
    - test_fit_returns_failure_not_exception: fitting garbage data (w_obs = random noise around 0 with negatives clipped) yields success=False or a fit — but never an unhandled exception
    - test_butterfly_violation_excluded: validate_surface({0.25: make_butterfly_violating_params(), 0.5: clean_params}) excludes the violating slice and emits a warning matching "butterfly" (pytest.warns)
    - test_calendar_violation_excluded: validate_surface(make_calendar_violating_surface()) excludes the longer-dated violating slice with a warning matching "[Cc]alendar"
    - test_no_false_positive_on_clean_surface: validate_surface(SYNTHETIC_SVI_SURFACE) returns all 3 slices, zero warnings (warnings.catch_warnings with error filter scoped to the module's warnings)
    - test_calendar_check_traded_range_only: check_calendar_arb accepts a k_grid argument and the default grid is within [-1.5, 1.5]
    - test_calibrate_surface_end_to_end: calibrate_surface(chain) returns dict {T: SVISliceFit} with 3 validated slices and an empty excluded list for the clean chain
  </behavior>
  <action>
Replace the Wave-0 stub in tests/test_svi.py with the tests above importing from volsurfacelab.svi (module absent — RED). Run pytest to confirm failure, commit: `test(04-03): add failing SVI recovery oracle and no-arb gate tests`.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/test_svi.py -q; test $? -ne 0</automated>
  </verify>
  <done>test_svi.py fails on missing volsurfacelab.svi; RED commit exists.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement SVI calibrator and no-arb validation gate</name>
  <files>portfolio_projects/volsurfacelab/src/volsurfacelab/svi.py</files>
  <action>
Implement src/volsurfacelab/svi.py per the verified research pattern:

1. `svi_w`, `svi_wp`, `svi_wpp`, `g_func` — vectorized numpy, exact formulas from the interfaces block.
2. `SVISliceFit` frozen dataclass: T, params (tuple of 5 floats), success (bool), sse (float), n_restarts_used (int).
3. `fit_svi_slice(k_obs, w_obs, k_constraint=None, initial_guesses=None) -> SVISliceFit`: SLSQP per restart with SVI_BOUNDS, butterfly inequality constraint g_func(K_CONSTRAINT, *p) (vectorized 'ineq'), options ftol=1e-12, maxiter=500. Accept only res.success; keep lowest res.fun. If all restarts fail: SVISliceFit(success=False). Wrap each minimize call in try/except (SLSQP can raise on pathological inputs) — a restart failure is skipped, never propagated.
4. `check_calendar_arb(params_by_maturity, k_grid=None, tol=1e-10)` — default k_grid np.linspace(-1.5, 1.5, 200) (traded range ONLY, per pitfall); returns list of (T_short, T_long, n_violated_points).
5. `validate_surface(params_by_maturity) -> dict`: (a) per-slice butterfly check g(k) >= -1e-8 over [-3,3]x100 — violators warned ("butterfly violation ... excluded") and dropped; (b) calendar check over survivors — for each violating pair, warn ("Calendar violation ...") and drop the LONGER maturity; (c) also verify w(k) > 0 over the constraint grid (handles the negative-a edge case, pitfall 7). Returns surviving {T: params}.
6. `calibrate_surface(chain: ChainData, calendar_k_range=(-1.5, 1.5)) -> tuple[dict[float, SVISliceFit], list]`: group chain.options by T, build w_obs = true_iv^2*T from the call rows (calls and puts share true_iv), fit each slice, run validate_surface on the successful fits, return (validated fits keyed by T, excluded list of (T, reason) tuples). Reads nothing from disk — config values passed as arguments.

Run pytest until green; commit `feat(04-03): SVI calibration with butterfly-constrained SLSQP + no-arb gate`.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/test_svi.py -q && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>All test_svi.py tests green: params recovered to 1e-3, planted butterfly and calendar violations excluded with warnings, clean surface passes with zero exclusions; full suite green.</done>
</task>

</tasks>

<verification>
- Recovery oracle: fitted (a,b,rho,m,sigma) match SYNTHETIC_SVI_SURFACE within 1e-3 for all three maturities
- Negative tests: both planted-arb constructions excluded with warnings; no exception raised by the gate
- False-positive guard: clean surface produces zero warnings — proves calendar check is range-restricted
</verification>

<success_criteria>
- VSL-03 satisfied: per-maturity SVI calibration with static no-arb validation (butterfly + calendar) gating downstream analysis; violated slices logged and excluded with clear warnings
</success_criteria>

<output>
After completion, create `.planning/phases/04-volsurfacelab/04-03-SUMMARY.md`
</output>
