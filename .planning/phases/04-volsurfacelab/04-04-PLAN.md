---
phase: 04-volsurfacelab
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - portfolio_projects/volsurfacelab/src/volsurfacelab/forecast.py
  - portfolio_projects/volsurfacelab/tests/test_forecast.py
autonomous: true
requirements: [VSL-05]

must_haves:
  truths:
    - "QLIKE follows Patton (2011): qlike(rv, 2*rv) < qlike(rv, 0.5*rv) — over-forecasting penalized less than under-forecasting; qlike(rv, rv) == 0"
    - "GARCH and EGARCH fits on the synthetic GARCH(1,1) path return convergence flag True (multi-restart wrapper, % scaling)"
    - "HAR forecasts at date t are built only from RV data dated t-1 and earlier (shift(1) on all regressors — leakage-tested)"
    - "Diebold-Mariano on a constructed loss differential where model 1 is strictly better yields negative dm_stat and p < 0.05"
    - "compare_forecasts produces one table with QLIKE and MSE for HAR/GARCH/EGARCH plus pairwise DM p-values on a common OOS window"
  artifacts:
    - path: "portfolio_projects/volsurfacelab/src/volsurfacelab/forecast.py"
      provides: "realized_variance, HARForecaster (statsmodels OLS), fit_garch_robust (multi-restart, convergence flag), garch_oos_forecast, qlike, diebold_mariano, compare_forecasts, ForecastComparison dataclass"
      exports: ["realized_variance", "HARForecaster", "fit_garch_robust", "qlike", "diebold_mariano", "compare_forecasts", "ForecastComparison"]
      min_lines: 140
    - path: "portfolio_projects/volsurfacelab/tests/test_forecast.py"
      provides: "QLIKE asymmetry oracle, DM correctness test, GARCH convergence test, HAR no-look-ahead test"
      min_lines: 80
  key_links:
    - from: "portfolio_projects/volsurfacelab/src/volsurfacelab/forecast.py"
      to: "arch.arch_model"
      via: "returns scaled x100 before fit; conditional variance rescaled /1e4 after"
      pattern: "\\* 100"
    - from: "portfolio_projects/volsurfacelab/src/volsurfacelab/forecast.py"
      to: "statsmodels OLS"
      via: "HAR regression + DM test with cov_type='HAC'"
      pattern: "cov_type=.HAC."
---

<objective>
Build the realized-volatility forecasting stack: HAR-RV baseline via statsmodels OLS (NOT arch HARX — DataScaleWarning pitfall), GARCH(1,1) and EGARCH(1,1) via arch with a multi-restart robust wrapper and asserted convergence flags, QLIKE (Patton 2011) + MSE evaluation, and pairwise Diebold-Mariano tests via HAC OLS — all compared on a common out-of-sample window of the synthetic underlying path.

Purpose: VSL-05 — phase success criterion #4. The QLIKE direction and the no-look-ahead HAR construction are the two oracle-style correctness properties; the rest is honest model comparison plumbing.
Output: forecast.py producing a ForecastComparison consumed by the pipeline (plan 04-06).
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-volsurfacelab/04-RESEARCH.md
@portfolio_projects/volsurfacelab/src/volsurfacelab/chain.py
@portfolio_projects/volsurfacelab/tests/conftest.py

<interfaces>
Verified patterns (04-RESEARCH.md, executed live in quant venv, arch 7.2.0):

```python
# QLIKE — Patton (2011): L(h, rv) = rv/h - log(rv/h) - 1  (h=forecast, rv=actual)
# Under-forecast penalized MORE. Unit test: qlike(rv, 2*rv) < qlike(rv, 0.5*rv).

# HAR-RV via statsmodels OLS (NOT arch HARX): regressors rv.shift(1),
# rv.rolling(5).mean().shift(1), rv.rolling(22).mean().shift(1)

# GARCH multi-restart (arch needs %-scale returns):
GARCH_STARTING_PARAMS = [None, [0.01,0.1,0.85], [0.05,0.05,0.90], [0.001,0.15,0.80], [0.1,0.20,0.70]]
# fit(disp='off', show_warning=False); accept convergence_flag == 0; pick best AIC
# Rescale: conditional variance / 1e4 to return to decimal-return units

# DM test: OLS of loss differential d = L1 - L2 on a constant,
# cov_type='HAC', cov_kwds={'maxlags': 4}; dm_stat = tvalue, negative => model1 better
```

Input from plan 04-01: `generate_underlying_returns(seed=42, n_days=750)` — daily decimal
log-returns from a GARCH(1,1) DGP (omega=2e-6, alpha=0.08, beta=0.90), so GARCH has true
persistence to recover. conftest provides session fixture `underlying_returns`.

Known limitation to encode in docstrings (pitfall 6): daily squared returns are a noisy RV
proxy; similar QLIKE across models and DM p > 0.05 is a data limitation, not a bug — the
report (plan 04-07) must state this honestly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing QLIKE/DM/convergence/no-look-ahead oracle tests</name>
  <files>portfolio_projects/volsurfacelab/tests/test_forecast.py</files>
  <behavior>
    - test_qlike_asymmetry: with rv = abs(default_rng(0).normal(size=200))**2 + 1e-6: qlike(rv, 2*rv) < qlike(rv, 0.5*rv)
    - test_qlike_perfect_forecast_zero: qlike(rv, rv) == pytest.approx(0.0)
    - test_dm_detects_better_model: rv_actual random; rv_hat1 = rv_actual + small noise (sd 0.1x), rv_hat2 = rv_actual + large noise (sd 1.0x); diebold_mariano returns dm_stat < 0 and p_value < 0.05
    - test_dm_equal_models_insignificant: identical forecast arrays plus iid noise of equal scale → p_value > 0.05 (use generous noise seeds to avoid flakiness; fix seed)
    - test_garch_converges_on_synthetic_path: fit_garch_robust(underlying_returns, vol='GARCH') returns converged=True; same for vol='EGARCH'
    - test_garch_persistence_recovered: GARCH alpha+beta estimate in (0.85, 1.0) on the synthetic path (true 0.98)
    - test_har_no_look_ahead: HARForecaster fitted on rv[:t] then predicting t must equal prediction from a model given rv with rv[t] replaced by an outlier (future value at t cannot influence the forecast FOR t since all regressors are shift(1)); implement by asserting forecast at index t is unchanged when rv values at indices >= t are perturbed
    - test_compare_forecasts_table: compare_forecasts(underlying_returns, train_frac=0.67) returns ForecastComparison with table containing rows {HAR, GARCH, EGARCH} and columns {qlike, mse}, dm_pvalues dict with 3 pairwise keys, and convergence flags all True
  </behavior>
  <action>
Replace the Wave-0 stub in tests/test_forecast.py with the tests above importing from volsurfacelab.forecast (RED — module absent). Confirm failure, commit: `test(04-04): add failing QLIKE/DM/GARCH-convergence/HAR-leakage oracle tests`.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/test_forecast.py -q; test $? -ne 0</automated>
  </verify>
  <done>test_forecast.py fails on missing volsurfacelab.forecast; RED commit exists.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement HAR/GARCH/EGARCH forecasters, QLIKE/MSE, DM, comparison harness</name>
  <files>portfolio_projects/volsurfacelab/src/volsurfacelab/forecast.py</files>
  <action>
Implement src/volsurfacelab/forecast.py:

1. `realized_variance(returns: pd.Series) -> pd.Series` — daily squared returns as RV proxy; docstring states the noise limitation (Pitfall 6) explicitly.
2. `HARForecaster`: fit(rv_train) builds regressors rv.shift(1), rv.rolling(5).mean().shift(1), rv.rolling(22).mean().shift(1), drops NaN, fits statsmodels OLS with add_constant. predict(rv_full, start) applies frozen train coefficients to the shifted regressors over the OOS index — by construction the forecast for date t uses only data <= t-1. Store params_ and rsquared_.
3. `fit_garch_robust(returns, vol='GARCH', n_restarts=5) -> (result, converged)` — exact research pattern: scale returns x100, loop GARCH_STARTING_PARAMS, fit(disp='off', show_warning=False), accept convergence_flag == 0, keep best AIC, swallow per-restart exceptions.
4. `garch_oos_forecast(returns, vol, split_idx) -> pd.Series` — fit on returns[:split_idx] (x100), then use res.forecast or a manual one-step recursion with frozen parameters over the OOS window (arch: `model.fit(first_obs=0, last_obs=split_idx)` then `res.forecast(horizon=1, start=split_idx, reindex=True)`); rescale variance /1e4 back to decimal units. One-step-ahead only — forecast for t uses returns through t-1.
5. `qlike(rv_actual, rv_hat) -> float` — Patton formula with rv_hat floored at 1e-10; code comment stating the convention and asymmetry direction. `mse(rv_actual, rv_hat) -> float`.
6. `diebold_mariano(rv_actual, rv_hat1, rv_hat2, max_lags=4) -> dict` — squared-error loss differential, OLS on constant with cov_type='HAC'; docstring caveat: with N≈250 OOS obs the asymptotic t-approximation is indicative only (research open question 2).
7. `ForecastComparison` frozen dataclass: table (pd.DataFrame indexed HAR/GARCH/EGARCH, columns qlike/mse), dm_pvalues (dict of pair->{dm_stat,p_value}), convergence (dict model->bool), oos_index, forecasts (dict model->pd.Series).
8. `compare_forecasts(returns, train_frac=0.67, n_restarts=5) -> ForecastComparison` — single common OOS window: split at int(train_frac*len), align all three forecast series on the same OOS index (drop the HAR warm-up overlap consistently), assert convergence flags before building the table (raise RuntimeError with a clear message if a model failed to converge — convergence is a hard requirement per VSL-05).

Run pytest until green; commit `feat(04-04): HAR/GARCH/EGARCH RV forecasting with QLIKE, MSE and Diebold-Mariano`.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/test_forecast.py -q && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>All test_forecast.py tests green: QLIKE asymmetry correct, DM discriminates constructed better/worse models, GARCH+EGARCH converge on synthetic path with persistence recovered, HAR proven leak-free; full suite green.</done>
</task>

</tasks>

<verification>
- QLIKE direction oracle green (the single most error-prone convention in this module)
- fit_garch_robust returns converged=True for both GARCH and EGARCH on the synthetic path
- compare_forecasts table has all three models on one common OOS window with pairwise DM p-values
- No DataScaleWarning in pytest output (returns scaled x100; HARX not used)
</verification>

<success_criteria>
- VSL-05 satisfied: HAR baseline then GARCH/EGARCH with robust multi-restart fitting and asserted convergence flags; QLIKE + MSE evaluation; DM tests between models
</success_criteria>

<output>
After completion, create `.planning/phases/04-volsurfacelab/04-04-SUMMARY.md`
</output>
