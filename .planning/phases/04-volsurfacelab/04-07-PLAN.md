---
phase: 04-volsurfacelab
plan: 07
type: execute
wave: 4
depends_on: ["04-06"]
files_modified:
  - portfolio_projects/volsurfacelab/run_pipeline.py
  - portfolio_projects/volsurfacelab/README.md
  - portfolio_projects/volsurfacelab/tests/test_integration.py
autonomous: true
requirements: [VSL-08]

must_haves:
  truths:
    - "`python run_pipeline.py --quick` from portfolio_projects/volsurfacelab/ exits 0 and writes reports/figures/*.png plus reports/summary.md"
    - "main(argv=None) -> int is callable in-process by tests (no subprocess), with --quick, --seed and --output-dir flags"
    - "Two runs with the same seed produce identical strategy P&L series (end-to-end determinism)"
    - "README is an honest research report: question, data, methodology, results, and limitations including the daily-RV proxy noise, DM small-sample caveat, and discrete-hedging approximation"
    - "No network access anywhere in the integration test path"
  artifacts:
    - path: "portfolio_projects/volsurfacelab/run_pipeline.py"
      provides: "one-command runner: main(argv=None) -> int, argparse with --quick/--seed/--output-dir"
      contains: "def main(argv=None)"
      min_lines: 60
    - path: "portfolio_projects/volsurfacelab/README.md"
      provides: "research report: question, data, methodology, how-to-run, results, limitations"
      min_lines: 80
    - path: "portfolio_projects/volsurfacelab/tests/test_integration.py"
      provides: "in-process runner test, output existence checks, determinism test"
      min_lines: 40
  key_links:
    - from: "portfolio_projects/volsurfacelab/run_pipeline.py"
      to: "volsurfacelab.pipeline.VolSurfacePipeline"
      via: "package import (no sys.path hacks)"
      pattern: "from volsurfacelab.pipeline import"
    - from: "portfolio_projects/volsurfacelab/tests/test_integration.py"
      to: "run_pipeline.main"
      via: "in-process call with tmp_path output dir"
      pattern: "main\\(\\["
---

<objective>
Ship the one-command runner (run_pipeline.py with the locked main(argv=None) -> int pattern), the end-to-end integration tests, and the README research report with honest framing of every known limitation.

Purpose: VSL-08 — phase success criterion #5 and the QUAL-02 research-report requirement. This is the surface a reviewer actually touches first.
Output: One command produces the full research report; integration suite green.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-volsurfacelab/04-RESEARCH.md
@portfolio_projects/volsurfacelab/src/volsurfacelab/pipeline.py
@portfolio_projects/volsurfacelab/src/volsurfacelab/report.py
@portfolio_projects/macroregime/run_macroregime.py
@portfolio_projects/macroregime/README.md

<interfaces>
From plan 04-06: `VolSurfacePipeline(config=None, seed=42, quick=False).run() -> PipelineResults`;
`ReportBuilder(results, output_dir).build() -> dict[str, Path]` — output_dir is the FIGURES
dir; summary.md goes to output_dir.parent (locked layout: reports/figures/*.png + reports/summary.md).

Runner conventions (locked since Phase 2, see run_macroregime.py): module docstring with usage
examples; `def main(argv=None) -> int` returning 0 on success; argparse; no sys.path hacks
(package is editable-installed); --quick reduces n_days and restart counts but exercises every
report section.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_pipeline.py runner + test_integration.py</name>
  <files>portfolio_projects/volsurfacelab/run_pipeline.py, portfolio_projects/volsurfacelab/tests/test_integration.py</files>
  <behavior>
    - test_runner_quick: main(["--quick", "--output-dir", str(tmp_path)]) returns 0; tmp_path/figures contains smile_*.png, surface_3d.png, surface_heatmap.png, vrp_pnl.png; tmp_path/summary.md exists and mentions QLIKE and Greeks
    - test_runner_seed_determinism: two main() runs with --seed 7 into separate tmp dirs produce byte-identical summary.md strategy P&L numbers (compare parsed net P&L lines or re-run pipeline in-process and compare series)
    - test_runner_bad_args: main(["--seed", "not-an-int"]) returns nonzero (argparse SystemExit caught and converted) OR raises SystemExit(2) — pick one behavior and pin it
    - test_no_network: integration path imports neither yfinance nor fredapi (assert not in sys.modules after a quick run)
  </behavior>
  <action>
Replace the Wave-0 stub in tests/test_integration.py with the tests above (RED commit `test(04-07): failing runner integration tests`), then implement run_pipeline.py:

1. Module docstring with usage examples (mirror run_macroregime.py style).
2. argparse: --quick (store_true), --seed (int, default 42), --output-dir (default "reports", figures go to <output-dir>/figures), --config (optional path).
3. `main(argv=None) -> int`: parse args; build VolSurfacePipeline(seed, quick, config); run; ReportBuilder(results, Path(output_dir)/"figures").build(); print a compact console summary (validated/excluded slice counts, QLIKE table, gross/net P&L, mean VRP); return 0. Catch exceptions at the top level, print to stderr, return 1.
4. `if __name__ == "__main__": raise SystemExit(main())`.

Commit `feat(04-07): one-command runner with in-process-testable main()`.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/test_integration.py -q && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python run_pipeline.py --quick --output-dir /tmp/vsl_smoke && ls /tmp/vsl_smoke/figures/*.png /tmp/vsl_smoke/summary.md</automated>
  </verify>
  <done>Integration tests green; CLI smoke run exits 0 producing figures + summary.md; deterministic under fixed seed.</done>
</task>

<task type="auto">
  <name>Task 2: README research report with honest framing</name>
  <files>portfolio_projects/volsurfacelab/README.md</files>
  <action>
Write README.md as a research report (mirror macroregime README structure):

1. **Research question**: Is implied volatility a biased forecast of realized volatility (variance risk premium), and can the gap be measured and harvested net of costs?
2. **Data**: deterministic synthetic options chain generated from a known SVI surface (spot 100, r 5%, T in {0.25, 0.5, 1.0}, k in [-1.5, 1.5]); seeded GARCH(1,1) underlying path (long-run vol ~16%); optional yfinance real-chain path (IVs always re-solved from mid-price via robust_iv, never trusted from the vendor column).
3. **Methodology**: LetsBeRational + brentq IV solving; per-maturity raw-SVI calibration with Gatheral-Jacquier butterfly constraint under SLSQP and a static no-arb gate (butterfly + calendar on the traded range); HAR-RV baseline vs GARCH/EGARCH with QLIKE/MSE and Diebold-Mariano; short delta-hedged ATM straddle gamma-scalping P&L with premium and hedge costs, standalone accounting.
4. **How to run**: editable install + `python run_pipeline.py --quick` / full run; pytest command; config file pointers.
5. **Results**: embed reports/figures/*.png (smile, 3D surface, heatmap, P&L) and the forecast comparison + Greeks tables from a committed run (run the full pipeline once and paste actual numbers — never invented figures).
6. **Limitations (honest framing — REQUIRED)**: (a) daily squared returns are a noisy RV proxy — model QLIKE differences and DM p-values are indicative, not decisive (research pitfall 6); (b) DM asymptotics with N≈250 OOS observations are approximate (open question 2); (c) continuous-hedging gamma P&L approximation ignores discrete daily-rebalance error (open question 3); (d) synthetic chain has no bid-ask microstructure — cost model is parametric; (e) VRP "edge" on synthetic data only demonstrates machinery, not a tradable anomaly.

Commit `docs(04-07): README research report with results and limitations`.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && grep -q "Limitations" README.md && grep -qi "QLIKE" README.md && grep -qi "variance risk premium" README.md && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>README covers question/data/methodology/how-to-run/results/limitations with real run numbers and figure references; full suite green.</done>
</task>

</tasks>

<verification>
- `python run_pipeline.py --quick` exits 0 from a clean shell and produces reports/figures/*.png + reports/summary.md
- Integration tests call main() in-process (no subprocess) and pass offline
- README limitations section names the RV-proxy, DM small-sample, and discrete-hedging caveats explicitly
</verification>

<success_criteria>
- VSL-08 satisfied: one-command runner produces the research report with surface figures, forecast comparison table, and strategy P&L + Greeks risk summary
- QUAL-02 satisfied for this phase (README research report)
</success_criteria>

<output>
After completion, create `.planning/phases/04-volsurfacelab/04-07-SUMMARY.md`
</output>
