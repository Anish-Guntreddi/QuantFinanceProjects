---
phase: 04-volsurfacelab
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - portfolio_projects/volsurfacelab/src/volsurfacelab/strategy.py
  - portfolio_projects/volsurfacelab/tests/test_strategy.py
autonomous: true
requirements: [VSL-06, VSL-07]

must_haves:
  truths:
    - "StandalonePortfolio cash invariant holds after open: cash == initial_cash - signed_premium - costs (1e-9 tolerance); accounting never touches qbacktest"
    - "Long-gamma daily P&L is positive on a day where r_t^2 > IV^2*dt and negative when r_t^2 < IV^2*dt (sign oracle)"
    - "Short delta-hedged straddle run over the synthetic path reports gross and net P&L with costs strictly reducing net relative to gross"
    - "Greeks signs are correct: call delta in (0,1), put delta in (-1,0), gamma > 0, vega > 0, theta < 0; portfolio theta reported per-day (annualized/252)"
    - "VRP series IV^2 - RV is computed and summarized; strategy results are deterministic under a fixed seed"
  artifacts:
    - path: "portfolio_projects/volsurfacelab/src/volsurfacelab/strategy.py"
      provides: "OptionLeg, StandalonePortfolio, daily_gamma_pnl, compute_leg_greeks, portfolio_greeks_summary, run_vrp_strategy, VRPResult dataclass"
      exports: ["OptionLeg", "StandalonePortfolio", "daily_gamma_pnl", "compute_leg_greeks", "portfolio_greeks_summary", "run_vrp_strategy", "VRPResult"]
      min_lines: 140
    - path: "portfolio_projects/volsurfacelab/tests/test_strategy.py"
      provides: "portfolio invariant test, gamma P&L sign oracle, Greeks sign tests, gross-vs-net cost test"
      min_lines: 80
  key_links:
    - from: "portfolio_projects/volsurfacelab/src/volsurfacelab/strategy.py"
      to: "vollib.black_scholes.greeks.analytical"
      via: "delta/gamma/vega/theta per leg; theta divided by 252 for per-day decay"
      pattern: "from vollib.black_scholes.greeks import analytical"
    - from: "portfolio_projects/volsurfacelab/src/volsurfacelab/strategy.py"
      to: "volsurfacelab.chain"
      via: "entry IV from chain true_iv / underlying path drives daily returns"
      pattern: "ChainData"
---

<objective>
Build the IV-vs-RV variance-risk-premium strategy with STANDALONE P&L accounting (locked decision: VolSurfaceLab does not route through the qbacktest event engine): OptionLeg/StandalonePortfolio accounting primitives, the delta-hedged gamma-scalping daily P&L formula, transaction costs on premium and hedge notional, and per-position Greeks (delta, gamma, vega, theta) with a portfolio risk summary.

Purpose: VSL-06 and VSL-07 — the strategy half of phase success criterion #5. The accounting invariant and the gamma P&L sign oracle are the correctness anchors; Greeks feed the report's risk summary.
Output: strategy.py producing a VRPResult consumed by the pipeline (plan 04-06).
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-volsurfacelab/04-RESEARCH.md
@portfolio_projects/volsurfacelab/src/volsurfacelab/chain.py
@portfolio_projects/volsurfacelab/tests/conftest.py

<interfaces>
Verified patterns (04-RESEARCH.md, executed live):

```python
# Greeks (vollib namespace): bs_greeks.<greek>(flag, S, K, t, r, sigma)
from vollib.black_scholes.greeks import analytical as bs_greeks
# delta('c',100,100,0.5,0.05,0.2)=0.5977; gamma=0.02736; vega=0.2736 (per unit vol)
# theta=-0.0222 PER YEAR -> divide by 252 for per-day decay (Pitfall 8: NOT 365)

# Delta-hedged long-gamma daily P&L (continuous-hedge approximation):
# pnl_t = 0.5 * gamma * S^2 * (r_t^2 - IV^2 * dt),  dt = 1/252
# Short-vol position flips the sign: earns when realized r_t^2 < IV^2*dt
# Discrete daily-hedging error acknowledged in report, not modeled (research open question 3)

# StandalonePortfolio: open() subtracts signed premium + cost (cost = abs(qty*price)*cost_rate);
# mark() accumulates daily P&L; never imports qbacktest
```

Inputs from plan 04-01: chain fixture (ATM entry IV = true_iv at k≈0) and
underlying_returns fixture (750-day seeded GARCH path). Annualized RV proxy from squared
returns; VRP_t = IV_atm^2 - realized variance over the holding window.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing accounting invariant, gamma sign oracle, and Greeks sign tests</name>
  <files>portfolio_projects/volsurfacelab/tests/test_strategy.py</files>
  <behavior>
    - test_portfolio_invariant_after_open: open a long leg (qty=+10, price=2.5) and a short leg (qty=-5, price=3.0): cash == initial - (10*2.5) - (-5*3.0) - costs where costs = (25.0 + 15.0)*cost_rate; tolerance 1e-9
    - test_portfolio_mark_pnl: after open, mark with prices moved +0.5 on the long leg and -0.4 on the short leg: pnl == 10*0.5 + (-5)*(-0.4)
    - test_gamma_pnl_sign_oracle: daily_gamma_pnl(S=100, gamma=0.03, daily_return=0.03, iv=0.20) > 0 (3% move vs 20% IV: r^2=9e-4 > IV^2/252≈1.59e-4) and daily_gamma_pnl(..., daily_return=0.001, ...) < 0
    - test_gamma_pnl_zero_at_breakeven: daily_return = iv*sqrt(1/252) gives pnl == approx(0)
    - test_greeks_signs: compute_leg_greeks for ATM call: delta in (0,1), gamma>0, vega>0, theta<0; ATM put: delta in (-1,0); theta_daily == theta_annual/252
    - test_short_straddle_greeks_aggregate: portfolio of short 1 ATM call + short 1 ATM put has |net delta| < 0.15, gamma < 0, vega < 0
    - test_run_vrp_strategy_costs_reduce_net: VRPResult.net_pnl.sum() < VRPResult.gross_pnl.sum() strictly; total_costs > 0
    - test_run_vrp_strategy_deterministic: two runs on the same fixtures produce identical pnl series
    - test_vrp_series_present: VRPResult.vrp_series has same index as the strategy window and mean VRP is finite
  </behavior>
  <action>
Replace the Wave-0 stub in tests/test_strategy.py with the tests above importing from volsurfacelab.strategy (RED). Confirm failure, commit: `test(04-05): add failing strategy accounting, gamma-pnl and Greeks oracle tests`.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/test_strategy.py -q; test $? -ne 0</automated>
  </verify>
  <done>test_strategy.py fails on missing volsurfacelab.strategy; RED commit exists.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement OptionLeg/StandalonePortfolio, gamma P&L, Greeks, VRP strategy runner</name>
  <files>portfolio_projects/volsurfacelab/src/volsurfacelab/strategy.py</files>
  <action>
Implement src/volsurfacelab/strategy.py (NO qbacktest import anywhere — standalone accounting is a locked roadmap decision; state this in the module docstring):

1. `OptionLeg` frozen dataclass: option_id, flag, K, T_entry, qty (signed), entry_price, entry_iv.
2. `StandalonePortfolio` dataclass: initial_cash=100_000.0, cost_rate=0.001, delta_hedge_cost_rate=0.001; `open(leg)` per the verified pattern (cash -= signed premium + cost; cost = abs(qty*entry_price)*cost_rate, appended to cost_history); `mark(current_prices: dict) -> float` summing qty*(price - entry_price) per held leg; `close(option_id, price)` realizing P&L back to cash with exit cost. Keep pnl_history/cost_history lists.
3. `compute_leg_greeks(leg, S, t_remaining, r, sigma) -> dict` — delta/gamma/vega/theta via vollib analytical, each scaled by leg.qty; include theta_daily = theta/252 (Pitfall 8 — 252, not 365). `portfolio_greeks_summary(portfolio, S, r, iv_lookup) -> pd.DataFrame` — per-leg rows plus a TOTAL row.
4. `daily_gamma_pnl(S, gamma, daily_return, iv, dt=1/252) -> float` — exact research formula 0.5*gamma*S^2*(r^2 - iv^2*dt). Positive gamma input = long-gamma position.
5. `run_vrp_strategy(chain: ChainData, returns: pd.Series, cost_rate=0.001, delta_hedge_cost_rate=0.001, side='short') -> VRPResult`:
   - Entry: ATM straddle at the shortest maturity (k closest to 0, T=0.25): short 1 call + short 1 put (side='short' harvests the variance risk premium; 'long' supported for symmetry).
   - Entry gamma from compute_leg_greeks at entry; entry IV = ATM true_iv.
   - Daily gross P&L over the returns window via daily_gamma_pnl with the SHORT sign (negative gamma exposure: pnl_t = -0.5*gamma*S^2*(r_t^2 - IV^2*dt) for the short side).
   - Daily hedge cost: abs(delta-rebalance notional) approximated as gamma*S^2*abs(r_t) * delta_hedge_cost_rate; entry/exit premium costs via StandalonePortfolio cost_rate.
   - Document the continuous-hedging approximation and daily-rebalance error in the docstring (research open question 3 — approximation is intentional; research goal is VRP analysis, not production hedging simulation).
6. `VRPResult` frozen dataclass: gross_pnl (pd.Series), net_pnl (pd.Series), total_costs (float), vrp_series (pd.Series of IV^2 minus rolling realized variance), entry_iv (float), greeks_summary (pd.DataFrame), side (str).

Run pytest until green; commit `feat(04-05): VRP delta-hedged straddle strategy with standalone P&L and Greeks`.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/test_strategy.py -q && /Users/anishguntreddi/Documents/QuantFinanceProjects/quant/bin/python -m pytest tests/ -q && ! grep -rn "import qbacktest\|from qbacktest" /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/volsurfacelab/src/</automated>
  </verify>
  <done>All test_strategy.py tests green: invariant, mark P&L, gamma sign oracle, Greeks signs, gross-vs-net, determinism; zero qbacktest imports in src/; full suite green.</done>
</task>

</tasks>

<verification>
- Accounting invariant test green to 1e-9; cost accounting separates premium costs from hedge costs
- Gamma P&L sign oracle and breakeven test green
- Greeks signs and theta/252 convention verified; short-straddle aggregate Greeks correct
- `grep -rn qbacktest portfolio_projects/volsurfacelab/src/` returns nothing
</verification>

<success_criteria>
- VSL-06 satisfied: VRP spread analysis with delta-hedged straddle P&L using standalone accounting including premium and hedge costs, gross and net reported
- VSL-07 satisfied: delta/gamma/vega/theta per position with portfolio risk summary DataFrame ready for the report
</success_criteria>

<output>
After completion, create `.planning/phases/04-volsurfacelab/04-05-SUMMARY.md`
</output>
