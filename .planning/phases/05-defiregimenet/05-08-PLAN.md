---
phase: 05-defiregimenet
plan: 08
type: execute
wave: 4
depends_on: ["05-07"]
files_modified:
  - portfolio_projects/defiregimenet/run_pipeline.py
  - portfolio_projects/defiregimenet/README.md
  - portfolio_projects/defiregimenet/tests/test_report.py
autonomous: true
requirements: [DFR-07]

must_haves:
  truths:
    - "`python run_pipeline.py --quick` exits 0 and produces reports/summary.md plus reports/figures/*.png with no network access"
    - "Bad CLI arguments exit with code 2 (argparse SystemExit propagated); runtime failures exit 1"
    - "README is a publication-style research report: abstract, data, methodology, results, robustness, limitations — with figure references and how-to-run"
    - "The README limitations section covers Gaussian-HMM-on-fat-tails misspecification, synthetic-data caveats, and the persistence-baseline asymmetry"
  artifacts:
    - path: "portfolio_projects/defiregimenet/run_pipeline.py"
      provides: "main(argv=None) -> int runner: --quick, --seed, --output-dir; wires run_pipeline + ReportBuilder"
      contains: "def main(argv=None)"
      min_lines: 50
    - path: "portfolio_projects/defiregimenet/README.md"
      provides: "publication-style research report"
      contains: "## Limitations"
      min_lines: 120
    - path: "portfolio_projects/defiregimenet/tests/test_report.py"
      provides: "runner integration tests: offline artifacts, --quick exit 0, bad args exit 2"
      min_lines: 40
  key_links:
    - from: "portfolio_projects/defiregimenet/run_pipeline.py"
      to: "defiregimenet.pipeline.run_pipeline + defiregimenet.report.builder.ReportBuilder"
      via: "single-command orchestration"
      pattern: "run_pipeline"
    - from: "portfolio_projects/defiregimenet/tests/test_report.py"
      to: "run_pipeline.py"
      via: "importlib.util.spec_from_file_location (no sys.path hacks — locked Phase 3/4 pattern)"
      pattern: "spec_from_file_location"
---

<objective>
One-command runner and the publication-style README research report, plus runner integration tests.

Purpose: DFR-07 — the deliverable surface of the project: one command, full honest report.
Output: run_pipeline.py, README.md, tests/test_report.py.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-defiregimenet/05-07-SUMMARY.md
@portfolio_projects/volsurfacelab/run_pipeline.py
@portfolio_projects/macroregime/README.md

<interfaces>
Runner conventions (locked across Phases 3-4):
- main(argv: list[str] | None = None) -> int; argparse with --quick, --seed (default 42),
  --output-dir (default reports/figures relative to the package dir, resolved from __file__)
- catch Exception -> print to stderr, return 1; let argparse SystemExit PROPAGATE so bad
  args exit 2 (test asserts via pytest.raises(SystemExit) checking code == 2)
- figures land in {output_dir}; summary.md in {output_dir.parent} (ReportBuilder handles this)
- if __name__ == "__main__": raise SystemExit(main())

Consumes (from 05-07): defiregimenet.pipeline.{load_config, run_pipeline},
defiregimenet.report.builder.ReportBuilder — read final signatures from 05-07-SUMMARY.md
and source.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_pipeline.py runner + integration tests</name>
  <files>portfolio_projects/defiregimenet/run_pipeline.py, portfolio_projects/defiregimenet/tests/test_report.py</files>
  <behavior>
    - test_runner_quick: main(["--quick", "--output-dir", str(tmp_path/"figures")]) returns 0; tmp_path/figures contains >= 6 PNGs; tmp_path/summary.md exists
    - test_runner_offline: the quick run completes with network access unavailable (no socket use — implicit: synthetic-only path; assert no ccxt in sys.modules after run)
    - test_runner_bad_args: main(["--bogus-flag"]) raises SystemExit with code 2
    - test_runner_deterministic_artifacts: two quick runs into separate dirs produce summary.md files with identical Results tables (compare the results section text)
  </behavior>
  <action>
    Implement per the interfaces block. Import run_pipeline.py in tests via
    importlib.util.spec_from_file_location (locked pattern, no sys.path hacks). Quick mode
    must finish in well under 120s; if the full quick run exceeds the budget, shrink quick
    params in configs (coordinate with pipeline defaults — pipeline owns quick sizing, runner
    just passes quick=True).
  </action>
  <verify>
    <automated>cd portfolio_projects/defiregimenet && python -m pytest tests/test_report.py -q</automated>
  </verify>
  <done>One command produces the complete report artifact set offline with correct exit-code semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Publication-style README research report</name>
  <files>portfolio_projects/defiregimenet/README.md</files>
  <action>
    Run `python run_pipeline.py` (full mode, seed 42) once to generate the committed
    reports/figures and real result numbers, then write README.md as a research paper:
    - Abstract: hybrid ML + econometric regime detection in synthetic DeFi markets; honest
      evaluation under purged/embargoed CV with strict label quarantine
    - Research question: do ML classifiers on lagged features beat causal HMM/GMM persistence
      at predicting forward-looking regime classes in fat-tailed 24/7 crypto markets?
    - Data: synthetic DGP description (4-state latent Markov, Student-t df=4, per-regime
      GARCH vol, market factor 0.7, 24/7 calendar), data-quality validation, optional ccxt path
    - Methodology: label construction (forward H=5, quarantined), causal features, per-token
      CausalRegimeDetector reuse, RegimeCVEvaluator (embargo >= H invariant), GARCH/HAR
      forecasting reuse, Cramér's V
    - Results: model comparison table (accuracy/log-loss, all four models), per-token
      diagnostics, cross-token heatmap, QLIKE table — embed actual numbers and figure links
      (reports/figures/*.png)
    - Robustness: k-sensitivity (structural metrics only — state explicitly that Sharpe-based
      K selection is rejected by design), StudentsT GARCH variant, label-horizon note
    - Limitations: Gaussian HMM misspecification on fat tails (cite the ~54%-on-2-class
      research finding), synthetic data realism bounds, persistence-baseline/CV asymmetry,
      no transaction-cost or strategy layer (detection project, not a trading backtest)
    - How to run: venv activation, editable installs (macroregime/alpharank/volsurfacelab
      prerequisites), `python run_pipeline.py [--quick]`, pytest command
  </action>
  <verify>
    <automated>cd portfolio_projects/defiregimenet && python -c "
import pathlib, re, sys
t = pathlib.Path('README.md').read_text()
required = ['Abstract', 'Data', 'Methodology', 'Results', 'Robustness', 'Limitations']
missing = [s for s in required if s not in t]
figs = list(pathlib.Path('reports/figures').glob('*.png'))
sys.exit(0 if not missing and len(figs) >= 6 else 1)
"</automated>
  </verify>
  <done>README reads as a complete research report with real generated numbers and committed figures.</done>
</task>

</tasks>

<verification>
- `cd portfolio_projects/defiregimenet && python run_pipeline.py --quick` exits 0, offline
- `cd portfolio_projects/defiregimenet && python -m pytest tests/ -q` fully green (no stubs left)
</verification>

<success_criteria>
- DFR-07 satisfied: one-command runner + publication-style report with all six sections and figures
</success_criteria>

<output>
After completion, create `.planning/phases/05-defiregimenet/05-08-SUMMARY.md`
</output>
