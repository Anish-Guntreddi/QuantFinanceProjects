---
phase: 05-defiregimenet
plan: 09
type: execute
wave: 5
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06", "05-07", "05-08"]
files_modified:
  - portfolio_projects/defiregimenet/src/defiregimenet/__init__.py
  - portfolio_projects/defiregimenet/tests/test_api.py
autonomous: true
requirements: [QUAL-01, QUAL-04]

must_haves:
  truths:
    - "defiregimenet exposes a frozen public API via __all__; importing defiregimenet does NOT import matplotlib.pyplot, xgboost, arch, or ccxt (heavy symbols behind lazy __getattr__)"
    - "defiregimenet.labels is NOT re-exported at package top level (quarantine extends to the API surface — evaluation/pipeline import it by full path)"
    - "Full strict suite passes twice in a row with identical results (deterministic, offline, FutureWarning-as-error)"
    - "Codex read-only audit (label separation + crypto data quality focus) completes with every finding fixed or explicitly accepted with rationale in the SUMMARY"
  artifacts:
    - path: "portfolio_projects/defiregimenet/src/defiregimenet/__init__.py"
      provides: "frozen __all__; eager imports for light modules; lazy __getattr__ for ReportBuilder, classifiers, vol_forecast, load_ccxt_panel, AND pipeline symbols (run_pipeline, PipelineResults, load_config) — pipeline imports classifiers which pulls xgboost"
      contains: "def __getattr__"
      min_lines: 40
    - path: "portfolio_projects/defiregimenet/tests/test_api.py"
      provides: "API surface test: __all__ resolvable, lazy imports deferred, no pyplot/xgboost/ccxt at import, labels not in __all__"
      min_lines: 30
  key_links:
    - from: "portfolio_projects/defiregimenet/src/defiregimenet/__init__.py"
      to: "defiregimenet.report.builder"
      via: "lazy __getattr__ (macroregime ReportBuilder pattern, locked)"
      pattern: "def __getattr__"
---

<objective>
Phase 5 quality gate: freeze the public API (lazy __getattr__ for heavy/optional imports), prove the strict suite is deterministic by running it twice, and pass the codex read-only audit with focus on label separation (quarantine, embargo >= H, forward-shift correctness) and crypto data quality (24/7 calendar, fat tails, anomaly validation, ccxt path).

Purpose: QUAL-01 and QUAL-04 — the locked per-phase completion gate. Phase 5 (and the milestone) is not complete until codex findings are resolved.
Note on QUAL-03: not applicable to this phase — DeFiRegimeNet is a regime classification system with no trading strategy or backtest layer, so there is no net-of-cost performance or significance reporting to produce. Record QUAL-03 as N/A (with this rationale) in the SUMMARY.
Output: Frozen API, double-green strict suite, codex audit clean.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-defiregimenet/05-RESEARCH.md
@portfolio_projects/volsurfacelab/src/volsurfacelab/__init__.py
@portfolio_projects/defiregimenet/src/defiregimenet/__init__.py

<interfaces>
Lazy-import convention (locked, macroregime/volsurfacelab pattern): heavy or optional symbols
resolve in module-level `def __getattr__(name)` importing on first access, AttributeError
otherwise. Light symbols import eagerly.

Public API to freeze (verify final names against implemented sources before writing):
- EAGER (numpy/pandas/scipy/sklearn-only import chains — verified light):
  - data: CryptoGenerator, CryptoPanel, validate_crypto_data, inject_anomalies
  - features: build_feature_matrix, build_feature_panel, expanding_zscore
  - regime: detect_regimes_per_token (CausalRegimeDetector NOT re-exported at top level — it
    belongs to macroregime; import from defiregimenet.regime.detector if needed)
  - analytics: per_token_diagnostics, k_sensitivity_per_token, cramers_v,
    cross_token_regime_correlation
  - evaluation: RegimeCVEvaluator, labels_to_probas
- LAZY via __getattr__ (each pulls xgboost, arch, pyplot, or ccxt transitively):
  - LogisticRegimeClassifier, XGBRegimeClassifier        (models.classifiers -> xgboost)
  - per_token_forecast_comparison, garch_studentst_variance  (forecast.vol_forecast -> arch)
  - ReportBuilder                                         (report.builder -> matplotlib.pyplot)
  - load_ccxt_panel                                       (data.real -> ccxt, optional)
  - run_pipeline, PipelineResults, load_config            (pipeline -> models.classifiers ->
    xgboost; pipeline MUST be behind the lazy gate — do NOT import it eagerly)
- EXCLUDED from __all__ by design: make_regime_labels (quarantine — document the exclusion
  in the __init__ docstring)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API freeze with lazy __getattr__ + surface test</name>
  <files>portfolio_projects/defiregimenet/src/defiregimenet/__init__.py, portfolio_projects/defiregimenet/tests/test_api.py</files>
  <behavior>
    - test_all_resolvable: every name in __all__ resolves via getattr(defiregimenet, name) — including the lazy ones (run_pipeline, PipelineResults, load_config, ReportBuilder, classifiers, forecast symbols, load_ccxt_panel), which import their modules on first access
    - test_lazy_imports_deferred: in a fresh subprocess, `import defiregimenet` leaves matplotlib.pyplot, xgboost, arch, ccxt out of sys.modules — this requires pipeline to be lazy (pipeline imports models.classifiers which imports xgboost)
    - test_labels_not_exported: "make_regime_labels" not in defiregimenet.__all__; getattr(defiregimenet, "make_regime_labels") raises AttributeError
    - test_unknown_attr: getattr raises AttributeError for a bogus name
  </behavior>
  <action>
    Write __init__.py per the interfaces block: eager imports ONLY for the verified-light
    modules (data.synthetic, features.crypto, regime.detector, analytics.*,
    evaluation.cv_evaluator); lazy __getattr__ for classifier/forecast/report/ccxt symbols
    AND the pipeline symbols (run_pipeline, PipelineResults, load_config). Pipeline is lazy
    by decision, not discretion: pipeline.py imports models.classifiers which imports
    xgboost, so an eager pipeline import would drag xgboost into every `import defiregimenet`.
    Replace the Wave-0 expectations: test_api.py is a new file.
  </action>
  <verify>
    <automated>cd portfolio_projects/defiregimenet && python -m pytest tests/test_api.py -q && python -c "import sys, defiregimenet; assert 'matplotlib.pyplot' not in sys.modules and 'xgboost' not in sys.modules"</automated>
  </verify>
  <done>Frozen __all__, deferred heavy imports (pipeline included in the lazy gate), labels excluded from the public surface.</done>
</task>

<task type="auto">
  <name>Task 2: Strict suite x2 + codex read-only audit</name>
  <files>portfolio_projects/defiregimenet/tests/test_api.py</files>
  <action>
    1. Run the full suite twice from portfolio_projects/defiregimenet/:
       `python -m pytest tests/ -q --tb=short` — both runs must be green with identical
       pass/skip counts (record both outputs in the SUMMARY).
    2. Run the codex read-only gate (locked project decision):
       `codex exec --sandbox read-only` with an audit prompt focused on:
       - label separation: AST quarantine completeness (dynamic imports? importlib?),
         shift(-H) correctness in labels.py, embargo >= H actually reaching skfolio,
         no label-derived columns in any feature path, baselines not trained on labels
       - crypto data quality: freq='D' everywhere, validate_crypto_data coverage,
         true_states used only as test oracle, ccxt path validated and lazy
       - general correctness: GARCH scaling round-trip (including target-date labeling:
         OOS forecast index = returns.index[split_idx + 1:]), Cramér's V edge cases,
         determinism (n_jobs=1, seeds), report numbers traceable to PipelineResults
    3. Triage every finding: fix real issues in this plan (small diffs only — anything
       structural goes back to the orchestrator), or document accepted findings with
       rationale in the SUMMARY. Re-run the suite after any fix.
    4. In the SUMMARY, record QUAL-03 as N/A for Phase 5 with the rationale: DeFiRegimeNet
       is a regime classification system — there is no trading strategy or backtest, hence
       no net-of-cost performance or statistical-significance table to report.
  </action>
  <verify>
    <automated>cd portfolio_projects/defiregimenet && python -m pytest tests/ -q --tb=short && python -m pytest tests/ -q --tb=short</automated>
  </verify>
  <done>Double-green deterministic suite; codex audit findings all resolved or accepted with written rationale; QUAL-03 N/A recorded in the SUMMARY.</done>
</task>

</tasks>

<verification>
- Full suite green twice consecutively, offline, FutureWarning-as-error
- Codex audit transcript summarized in SUMMARY with triage table
- `python -c "import defiregimenet"` is light (< 2s, no heavy modules)
</verification>

<success_criteria>
- QUAL-01: deterministic offline suite proven by double run
- QUAL-04: codex read-only review passed with label-separation + crypto-data-quality focus, no unresolved findings
- QUAL-03 explicitly recorded as N/A in the SUMMARY (no strategy/backtest layer in this phase)
- Phase 5 complete — all DFR requirements verified by the suite built in plans 01-08
</success_criteria>

<output>
After completion, create `.planning/phases/05-defiregimenet/05-09-SUMMARY.md`
</output>
