---
phase: 05-defiregimenet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - portfolio_projects/defiregimenet/pyproject.toml
  - portfolio_projects/defiregimenet/requirements.txt
  - portfolio_projects/defiregimenet/configs/params.yml
  - portfolio_projects/defiregimenet/src/defiregimenet/__init__.py
  - portfolio_projects/defiregimenet/src/defiregimenet/data/__init__.py
  - portfolio_projects/defiregimenet/src/defiregimenet/data/synthetic.py
  - portfolio_projects/defiregimenet/src/defiregimenet/data/real.py
  - portfolio_projects/defiregimenet/src/defiregimenet/features/__init__.py
  - portfolio_projects/defiregimenet/src/defiregimenet/models/__init__.py
  - portfolio_projects/defiregimenet/src/defiregimenet/regime/__init__.py
  - portfolio_projects/defiregimenet/src/defiregimenet/evaluation/__init__.py
  - portfolio_projects/defiregimenet/src/defiregimenet/analytics/__init__.py
  - portfolio_projects/defiregimenet/src/defiregimenet/forecast/__init__.py
  - portfolio_projects/defiregimenet/src/defiregimenet/report/__init__.py
  - portfolio_projects/defiregimenet/tests/conftest.py
  - portfolio_projects/defiregimenet/tests/test_synthetic.py
  - portfolio_projects/defiregimenet/tests/test_labels.py
  - portfolio_projects/defiregimenet/tests/test_features.py
  - portfolio_projects/defiregimenet/tests/test_regime.py
  - portfolio_projects/defiregimenet/tests/test_diagnostics.py
  - portfolio_projects/defiregimenet/tests/test_classifiers.py
  - portfolio_projects/defiregimenet/tests/test_cv_evaluator.py
  - portfolio_projects/defiregimenet/tests/test_cross_token.py
  - portfolio_projects/defiregimenet/tests/test_forecast.py
  - portfolio_projects/defiregimenet/tests/test_pipeline.py
  - portfolio_projects/defiregimenet/tests/test_report.py
autonomous: true
requirements: [DFR-01, QUAL-05]

must_haves:
  truths:
    - "After editable install, `import defiregimenet` succeeds from any directory using the quant venv"
    - "CryptoGenerator with the same seed produces byte-identical OHLCV panels and true-state sequences on repeated calls"
    - "Generated calendar is 24/7: every 14-day window contains 14 consecutive daily bars including weekends (freq='D', never 'B')"
    - "Generated returns show fat tails (excess kurtosis > 1.0) and vol clustering (fitted GARCH(1,1) alpha+beta > 0.9)"
    - "validate_crypto_data emits UserWarning for injected calendar gaps and volume anomalies; clean data passes silently"
    - "Full test suite runs offline and green from day one: Wave-0 stubs skip cleanly; the AST label-quarantine guard is LIVE (passes trivially with no labels.py yet)"
  artifacts:
    - path: "portfolio_projects/defiregimenet/pyproject.toml"
      provides: "hatchling build, src layout, pytest config with filterwarnings error::FutureWarning"
      contains: "error::FutureWarning"
    - path: "portfolio_projects/defiregimenet/src/defiregimenet/data/synthetic.py"
      provides: "CryptoGenerator, CryptoPanel frozen dataclass, validate_crypto_data, inject_anomalies, DGP param constants"
      exports: ["CryptoGenerator", "CryptoPanel", "validate_crypto_data", "inject_anomalies"]
      min_lines: 150
    - path: "portfolio_projects/defiregimenet/src/defiregimenet/data/real.py"
      provides: "load_ccxt_panel with lazy ccxt import (never module scope), routed through validate_crypto_data"
      contains: "def load_ccxt_panel"
    - path: "portfolio_projects/defiregimenet/tests/conftest.py"
      provides: "fix_seeds autouse fixture + session-scope seeded_crypto_panel fixture"
      contains: "session"
  key_links:
    - from: "portfolio_projects/defiregimenet/tests/conftest.py"
      to: "defiregimenet.data.synthetic"
      via: "session-scope fixture constructing CryptoGenerator(seed=42)"
      pattern: "CryptoGenerator"
    - from: "portfolio_projects/defiregimenet/tests/test_labels.py"
      to: "src/defiregimenet source tree"
      via: "AST walk over all .py files asserting no import of defiregimenet.labels outside evaluation/pipeline"
      pattern: "ast\\.(walk|parse)"
---

<objective>
Create the defiregimenet package skeleton (src layout, hatchling, editable install into the quant venv), the deterministic synthetic crypto generator (24/7 calendar, Student-t fat tails, per-regime GARCH vol clustering, shared 4-state latent Markov regime, market factor), crypto data-quality validation with anomaly injection, the optional lazy ccxt loader, and the Wave-0 test scaffold (conftest fixtures + stubs for every module, with the AST label-quarantine guard live from day one).

Purpose: DFR-01 — the data foundation every other plan consumes. Wave 2 runs five plans in parallel against the fixtures created here.
Output: Installable package, CryptoGenerator + CryptoPanel + validate_crypto_data, green offline suite.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-defiregimenet/05-RESEARCH.md
@portfolio_projects/volsurfacelab/pyproject.toml
@portfolio_projects/macroregime/src/macroregime/data/synthetic.py

<interfaces>
DGP parameters (from research, verified):
- 4 tokens (BTC, ETH, SOL, AVAX), ~3 years daily bars, pd.date_range(..., freq='D') — NEVER 'B'
- Single shared 4-state latent Markov market regime sequence (persistence ~0.97, all rows
  ergodic — include small off-diagonal mass so all 4 states are visited; see Phase 3
  TRANSITION_MATRIX ergodicity decision)
- State encoding (LOCKED, must match labels.py in plan 05-02):
  0=bear/low-vol, 1=bear/high-vol, 2=bull/low-vol, 3=bull/high-vol
- Per-state GARCH(1,1) vol params and daily drifts per research CRYPTO_DGP_PARAMS table
- Student-t innovations df=4 via scipy.stats.t (single seeded np.random.default_rng per
  generator — alpharank locked convention; pass rng to scipy via random_state)
- Per-token return = 0.7 * market_component + 0.3 * idiosyncratic_component (both components
  draw vol from the shared regime state)
- OHLCV via cumulative cumprod of log-return exp (NO pct_change anywhere — Phase 3 locked:
  FutureWarning-as-error)
- Volume: log-normal baseline, scaled up in high-vol states

CryptoPanel (frozen dataclass):
- tokens: tuple[str, ...]
- ohlcv: dict[str, pd.DataFrame]  (columns open/high/low/close/volume, DatetimeIndex freq='D')
- true_states: np.ndarray  (shape (T,), shared latent sequence — TEST ORACLE ONLY; the honest
  pipeline path never uses true_states for features or training targets; same discipline as
  volsurfacelab's true_iv)

validate_crypto_data(ohlcv: pd.DataFrame) -> list[str]:
- warnings.warn(UserWarning) and collect messages; NEVER raise (volsurfacelab gate convention)
- checks: calendar gaps (missing daily dates / NaN bars), non-positive prices,
  volume anomalies (volume > 20x rolling 30-bar median), zero-volume runs

inject_anomalies(df, gap_indices, volume_spike_indices) -> pd.DataFrame (copy; for tests)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package skeleton, pyproject, configs, editable install</name>
  <files>portfolio_projects/defiregimenet/pyproject.toml, portfolio_projects/defiregimenet/requirements.txt, portfolio_projects/defiregimenet/configs/params.yml, portfolio_projects/defiregimenet/src/defiregimenet/__init__.py, portfolio_projects/defiregimenet/src/defiregimenet/data/__init__.py, portfolio_projects/defiregimenet/src/defiregimenet/features/__init__.py, portfolio_projects/defiregimenet/src/defiregimenet/models/__init__.py, portfolio_projects/defiregimenet/src/defiregimenet/regime/__init__.py, portfolio_projects/defiregimenet/src/defiregimenet/evaluation/__init__.py, portfolio_projects/defiregimenet/src/defiregimenet/analytics/__init__.py, portfolio_projects/defiregimenet/src/defiregimenet/forecast/__init__.py, portfolio_projects/defiregimenet/src/defiregimenet/report/__init__.py</files>
  <action>
    Mirror the volsurfacelab pyproject pattern: hatchling build backend, src layout, name
    "defiregimenet", version 0.1.0, requires-python >=3.11. Pytest config in
    [tool.pytest.ini_options]: testpaths=["tests"], filterwarnings=["error::FutureWarning"].
    Dependencies: numpy, pandas, scipy, scikit-learn, xgboost, hmmlearn, skfolio, arch,
    statsmodels, matplotlib, PyYAML (all already in quant venv). Sibling packages macroregime,
    alpharank, volsurfacelab are editable-installed in the venv — document in requirements.txt
    as `-e ../macroregime` style lines with a comment that they are already installed.
    Top-level __init__.py: __version__ = "0.1.0" ONLY — public API frozen in plan 05-09;
    all subpackage __init__.py files are empty (consumers import modules directly, e.g.
    `from defiregimenet.analytics.cross_token import cramers_v`). This keeps wave-2 plans
    file-disjoint.
    configs/params.yml: tokens [BTC, ETH, SOL, AVAX], n_years 3, seed 42, fat_tail_df 4,
    market_factor_weight 0.7, regime {n_components 4, min_train 60, refit_every 21,
    n_restarts 3}, labels {horizon 5}, cv {n_folds 6, n_test_folds 2, purged_size 5,
    embargo_size 5}, forecast {train_frac 0.67, n_restarts 5}.
    Then `pip install -e portfolio_projects/defiregimenet` into the quant venv.
  </action>
  <verify>
    <automated>cd /tmp && python -c "import defiregimenet; print(defiregimenet.__version__)"</automated>
  </verify>
  <done>Editable install succeeds; import works from outside the package directory; pyproject contains error::FutureWarning filter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CryptoGenerator, data-quality validation, lazy ccxt loader</name>
  <files>portfolio_projects/defiregimenet/src/defiregimenet/data/synthetic.py, portfolio_projects/defiregimenet/src/defiregimenet/data/real.py, portfolio_projects/defiregimenet/tests/test_synthetic.py</files>
  <behavior>
    - test_calendar_24_7: generated index is freq='D' with no missing dates (len(index) == (index[-1]-index[0]).days + 1); contains Saturdays and Sundays
    - test_fat_tails: excess kurtosis of close-to-close log returns > 1.0 for every token
    - test_vol_clustering: volsurfacelab.forecast.fit_garch_robust on one token's returns returns converged=True with alpha+beta > 0.9
    - test_determinism: two CryptoGenerator(seed=42).generate() calls produce identical ohlcv frames (pd.testing.assert_frame_equal) and identical true_states
    - test_true_states_shape: true_states in {0,1,2,3}, all 4 states visited, mean dwell > 10 bars
    - test_gap_warning: inject_anomalies with gap_indices triggers pytest.warns(UserWarning, match="gap")
    - test_volume_anomaly_warning: volume spike x50 triggers pytest.warns(UserWarning, match="volume")
    - test_clean_data_no_warnings: pristine panel produces no warnings (warnings.catch_warnings + simplefilter error)
    - test_ccxt_lazy: `import defiregimenet.data.real` then assert "ccxt" not in sys.modules
  </behavior>
  <action>
    Implement per the interfaces block. Single np.random.default_rng(seed) drives everything:
    Markov chain simulation, Student-t draws (scipy.stats.t.rvs(df=4, random_state=rng)),
    GARCH recursion sigma2[t] = omega + alpha*r[t-1]^2 + beta*sigma2[t-1] per shared regime
    state, market + idio composition, OHLC construction from close path (high/low as close *
    exp(+/- |intraday noise|), open = previous close * small gap), log-normal volume scaled by
    regime vol level. NO pct_change calls. validate_crypto_data per interfaces (warn, never
    raise). inject_anomalies returns a copy.
    data/real.py: load_ccxt_panel(symbols, exchange_id="binance", ...) with `import ccxt`
    INSIDE the function body raising a helpful ImportError if missing; fetched frames pass
    through validate_crypto_data before return. Never called in tests beyond the lazy-import
    assertion.
  </action>
  <verify>
    <automated>cd portfolio_projects/defiregimenet && python -m pytest tests/test_synthetic.py -q</automated>
  </verify>
  <done>All test_synthetic.py behaviors pass; generator is deterministic, fat-tailed, vol-clustered, 24/7; quality validation warns on injected anomalies only.</done>
</task>

<task type="auto">
  <name>Task 3: conftest fixtures + Wave-0 stubs with live AST quarantine guard</name>
  <files>portfolio_projects/defiregimenet/tests/conftest.py, portfolio_projects/defiregimenet/tests/test_labels.py, portfolio_projects/defiregimenet/tests/test_features.py, portfolio_projects/defiregimenet/tests/test_regime.py, portfolio_projects/defiregimenet/tests/test_diagnostics.py, portfolio_projects/defiregimenet/tests/test_classifiers.py, portfolio_projects/defiregimenet/tests/test_cv_evaluator.py, portfolio_projects/defiregimenet/tests/test_cross_token.py, portfolio_projects/defiregimenet/tests/test_forecast.py, portfolio_projects/defiregimenet/tests/test_pipeline.py, portfolio_projects/defiregimenet/tests/test_report.py</files>
  <action>
    conftest.py: autouse fix_seeds fixture (np.random.seed(42)); session-scope
    `seeded_crypto_panel` fixture returning CryptoGenerator(seed=42, n_years=3,
    tokens=("BTC","ETH","SOL","AVAX")).generate(); session-scope `small_crypto_panel`
    (n_years=2, 2 tokens) for fast unit tests.
    test_labels.py: implement test_label_quarantine FULLY (AST walk per 05-RESEARCH.md Pattern 1
    — ALLOWED_IMPORTERS = {"defiregimenet.evaluation", "defiregimenet.pipeline"}); it passes
    trivially now and protects all wave-2 executors. Add skipped stub
    test_labels_are_forward_looking with pytest.skip("Wave 0 stub — implemented in 05-02").
    All other test files: module-level pytest.skip("Wave 0 stub — implemented in 05-NN",
    allow_module_level=True) with the owning plan number (features 05-02, regime/diagnostics
    05-03, classifiers/cv 05-04, forecast 05-05, cross_token 05-06, pipeline 05-07,
    report 05-08).
  </action>
  <verify>
    <automated>cd portfolio_projects/defiregimenet && python -m pytest tests/ -q</automated>
  </verify>
  <done>Full suite green offline: test_synthetic + quarantine guard pass, stubs skip cleanly; no network access anywhere.</done>
</task>

</tasks>

<verification>
- `cd portfolio_projects/defiregimenet && python -m pytest tests/ -q` green, offline, < 120s
- `python -c "import defiregimenet"` from /tmp succeeds
- grep confirms no `freq='B'`, no `pct_change` in src/
</verification>

<success_criteria>
- DFR-01 satisfied: deterministic 24/7 fat-tailed vol-clustered multi-token generator with data-quality validation and optional lazy ccxt path
- QUAL-05 satisfied: shared conventions established for the project — src layout, pyproject.toml (hatchling), YAML configs in configs/, per-project requirements.txt, figures destined for reports/figures/
- Wave-2 plans can start in parallel against seeded_crypto_panel
</success_criteria>

<output>
After completion, create `.planning/phases/05-defiregimenet/05-01-SUMMARY.md`
</output>
