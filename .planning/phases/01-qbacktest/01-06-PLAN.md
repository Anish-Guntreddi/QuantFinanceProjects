---
phase: 01-qbacktest
plan: 06
type: execute
wave: 4
depends_on: ["01-02", "01-03", "01-04", "01-05"]
files_modified:
  - portfolio_projects/qbacktest/src/qbacktest/engine.py
  - portfolio_projects/qbacktest/src/qbacktest/__init__.py
  - portfolio_projects/qbacktest/tests/test_engine.py
  - portfolio_projects/qbacktest/tests/test_execution.py
  - portfolio_projects/qbacktest/tests/test_determinism.py
  - portfolio_projects/qbacktest/tests/test_metrics.py
autonomous: true
requirements: [QBT-02, QBT-03, QBT-04, QBT-06]

must_haves:
  truths:
    - "An order generated from bar T's signal fills at bar T+1's open — an oracle look-ahead strategy earns Sharpe < 0.5 under every slippage/spread/commission configuration"
    - "Two runs with identical config and seed produce identical equity curves (deterministic loop)"
    - "Unfilled pending orders at end-of-data are logged as cancelled with zero cost and excluded from trade statistics"
    - "A signal that violates risk limits never produces a fill (blocked at order generation)"
    - "BacktestResults exposes gross_sharpe, net_sharpe, and cost_bps fields"
  artifacts:
    - path: "portfolio_projects/qbacktest/src/qbacktest/engine.py"
      provides: "EventDrivenBacktester main loop, BacktestConfig, BacktestResults, pending-order T+1 buffer"
      contains: "_pending_orders"
      min_lines: 150
  key_links:
    - from: "portfolio_projects/qbacktest/src/qbacktest/engine.py"
      to: "qbacktest.data DataHandler.peek_next_bar"
      via: "flush pending orders at peeked next bar's open BEFORE advancing"
      pattern: "peek_next_bar"
    - from: "portfolio_projects/qbacktest/src/qbacktest/engine.py"
      to: "qbacktest.portfolio.Portfolio.on_fill"
      via: "_handle_fill_event delegates ALL accounting to on_fill"
      pattern: "on_fill"
    - from: "portfolio_projects/qbacktest/src/qbacktest/engine.py"
      to: "qbacktest.metrics.compute_metrics"
      via: "BacktestResults.metrics built from equity curve + costs"
      pattern: "compute_metrics"
---

<objective>
Assemble the EventDrivenBacktester: deterministic main loop with the T+1 pending-order buffer, event dispatch wiring strategy → portfolio → execution → accounting, end-of-data order cancellation, and BacktestConfig/BacktestResults. Land the phase's three engine-level oracle tests.

Purpose: This is where the T+1 invariant becomes enforced behavior — the central correctness claim of the whole package (roadmap success criterion 3).
Output: qbacktest.engine; test_engine.py, test_determinism.py, and test_execution.py::test_t_plus_one_fill_oracle green.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-qbacktest/01-RESEARCH.md
@.planning/phases/01-qbacktest/01-02-SUMMARY.md
@.planning/phases/01-qbacktest/01-03-SUMMARY.md
@.planning/phases/01-qbacktest/01-04-SUMMARY.md
@.planning/phases/01-qbacktest/01-05-SUMMARY.md

<interfaces>
<!-- CONSUMES (all already built and tested):
  events:    EventQueue.put/get/empty; MarketEvent/SignalEvent/OrderEvent/FillEvent
  data:      DataHandler.update_bars() -> list[MarketEvent]; peek_next_bar(symbol) -> dict|None; continue_backtest
  strategy:  Strategy.initialize(data_handler); calculate_signals(MarketEvent) -> list[SignalEvent]
  portfolio: Portfolio(initial_capital, position_size, risk_manager); on_fill(fill); mark_to_market(ts, prices);
             generate_orders(signal, price) -> list[OrderEvent]; check_accounting_invariant(); equity_curve; total_traded_value; trade_pnls; cumulative_costs
  execution: SimulatedExecutionHandler(slippage_model, commission_model).fill_at_open(order, next_bar) -> FillEvent|None
  risk:      RiskManager(max_position_weight, max_gross_exposure)
  metrics:   compute_metrics(...) -> MetricsReport
-->

<!-- PROVIDES to plans 01-07 (walk-forward) and 01-08 (demo/tearsheet). Do not deviate: -->
```python
# qbacktest/engine.py — public contract
@dataclass
class BacktestConfig:
    initial_capital: float = 100_000.0
    position_size: float = 0.1
    max_position_weight: float = 0.2
    max_gross_exposure: float = 1.0
    start: pd.Timestamp | None = None
    end: pd.Timestamp | None = None

@dataclass
class BacktestResults:
    equity_curve: pd.Series          # DatetimeIndex -> net equity
    gross_returns: pd.Series
    net_returns: pd.Series
    metrics: MetricsReport
    trades: list[FillEvent]
    cancelled_orders: list[OrderEvent]
    # convenience passthroughs (test_results_has_net_sharpe asserts these):
    gross_sharpe: float; net_sharpe: float; cost_bps: float

class EventDrivenBacktester:
    def __init__(self, data_handler, strategy, portfolio=None, execution_handler=None,
                 config: BacktestConfig | None = None): ...
    def run(self) -> BacktestResults
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine main loop with T+1 pending buffer and EOD cancellation</name>
  <files>portfolio_projects/qbacktest/src/qbacktest/engine.py, portfolio_projects/qbacktest/src/qbacktest/__init__.py, portfolio_projects/qbacktest/tests/test_engine.py</files>
  <behavior>
    - test_strategy_plugin_seam: a buy-and-hold Strategy subclass runs end-to-end on synthetic_bars; engine calls calculate_signals once per symbol per bar; results contain >= 1 fill
    - test_risk_limits_block_order: strategy demanding position_size 0.5 with max_position_weight 0.1 → zero fills for the oversized order, run completes without exception
    - test_eod_pending_cancelled: strategy that signals only on the final bar → order is buffered, no T+1 bar exists → results.cancelled_orders contains it, zero cost incurred, not in results.trades
    - test_invariant_after_run: after run(), abs(portfolio.check_accounting_invariant()) < 1e-6
  </behavior>
  <action>
Implement EventDrivenBacktester per RESEARCH.md "Pattern 1" loop, in this exact per-iteration order: (1) if _pending_orders, for each order peek_next_bar(order.symbol) and fill via execution_handler.fill_at_open, putting FillEvents on the queue; clear buffer; (2) market_events = data_handler.update_bars(); enqueue; (3) drain queue dispatching by type — MARKET → strategy.calculate_signals (enqueue signals) + portfolio.mark_to_market with current closes; SIGNAL → portfolio.generate_orders(signal, latest close) (enqueue orders); ORDER → append to _pending_orders (NEVER fill here); FILL → portfolio.on_fill. After the loop: any remaining _pending_orders are logged at WARNING as cancelled and stored in results.cancelled_orders with zero cost (locked decision). Build defaults in __init__ from config: Portfolio with RiskManager, SimulatedExecutionHandler with Zero models if none given. run() builds BacktestResults: equity Series from portfolio.equity_curve, gross returns from pre-cost equity (track cost-free parallel equity or add back cumulative cost deltas per bar), net from actual equity, metrics via compute_metrics, passthrough gross_sharpe/net_sharpe/cost_bps. NO reset() method — fresh instances only (locked). Update top-level __init__.py exports: EventDrivenBacktester, BacktestConfig, BacktestResults, Strategy, DataHandler, HistoricalDataHandler, RiskManager, SimulatedExecutionHandler, MetricsReport. Replace test_engine.py stubs.
  </action>
  <verify>
    <automated>cd portfolio_projects/qbacktest && python3 -m pytest tests/test_engine.py -x -q</automated>
  </verify>
  <done>End-to-end run on synthetic data with fills, risk blocking, EOD cancellation, and post-run invariant all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: T+1 oracle test and determinism test</name>
  <files>portfolio_projects/qbacktest/tests/test_execution.py, portfolio_projects/qbacktest/tests/test_determinism.py, portfolio_projects/qbacktest/tests/test_metrics.py</files>
  <behavior>
    - test_t_plus_one_fill_oracle: oracle strategy peeks data_handler internals to know close[t+1] vs close[t], signals accordingly. Parametrize over slippage/spread/commission configs: (ZeroSlippage,ZeroCommission), (FixedSlippage(10),PercentageCommission(0.001)), (SpreadSlippage(20),FixedCommission(1.0)). Under T+1 fill the look-ahead edge vanishes: assert abs(annualized net Sharpe) < 0.5 for ALL configs. Also assert every FillEvent.fill_price corresponds to the open (± slippage) of the bar AFTER the signal bar — never the signal bar's close
    - test_same_seed_same_results: build engine twice with identical config and SyntheticOHLCVGenerator(seed=42) data → pd.testing.assert_series_equal(r1.equity_curve, r2.equity_curve) exactly, and identical fill counts
    - test_results_has_net_sharpe: BacktestResults from a real run has float gross_sharpe, net_sharpe, cost_bps; gross_sharpe >= net_sharpe when costs configured
  </behavior>
  <action>
Replace the three remaining W0 stubs with real tests per 01-VALIDATION.md node ids (tests/test_execution.py::test_t_plus_one_fill_oracle, tests/test_determinism.py::test_same_seed_same_results, tests/test_metrics.py::test_results_has_net_sharpe). The oracle strategy is the verification weapon from RESEARCH.md Pitfall 1: it is ALLOWED to cheat (read future bars from the raw data dict in the test) because T+1 fill must neutralize the cheat. If oracle Sharpe comes out > 0.5, the engine is filling same-bar — fix the engine, never loosen the threshold. Cross-check fill timestamps: for each fill, assert fill.timestamp > timestamp of the signal bar that caused it.
  </action>
  <verify>
    <automated>cd portfolio_projects/qbacktest && python3 -m pytest tests/test_execution.py::test_t_plus_one_fill_oracle tests/test_determinism.py tests/test_metrics.py -x -q</automated>
  </verify>
  <done>Oracle neutralized under all three cost configs; byte-identical reruns; gross/net fields present on results.</done>
</task>

</tasks>

<verification>
- `cd portfolio_projects/qbacktest && python3 -m pytest tests/ -q` → only walk-forward stubs still skip; zero failures
- `python3 -c "from qbacktest import EventDrivenBacktester, BacktestConfig, BacktestResults"` succeeds
- `grep -n "def reset" portfolio_projects/qbacktest/src/qbacktest/engine.py` → no matches (locked: no reset method)
</verification>

<success_criteria>
- QBT-02: full deterministic event loop proven by test_same_seed_same_results
- QBT-03: strategy plug-in seam proven end-to-end through the engine
- QBT-04: oracle test demonstrates T+1 open fill under all slippage/spread configurations (roadmap success criterion 3)
- QBT-06: risk limits enforced at order generation, verified through the engine path
</success_criteria>

<output>
After completion, create `.planning/phases/01-qbacktest/01-06-SUMMARY.md`
</output>
