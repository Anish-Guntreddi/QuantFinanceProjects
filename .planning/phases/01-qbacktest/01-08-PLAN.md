---
phase: 01-qbacktest
plan: 08
type: execute
wave: 5
depends_on: ["01-06"]
files_modified:
  - portfolio_projects/qbacktest/src/qbacktest/tearsheet/renderer.py
  - portfolio_projects/qbacktest/src/qbacktest/tearsheet/__init__.py
  - portfolio_projects/qbacktest/examples/demo_ma_strategy.py
  - portfolio_projects/qbacktest/run_demo.py
  - portfolio_projects/qbacktest/README.md
  - portfolio_projects/qbacktest/tests/test_tearsheet.py
autonomous: false
requirements: [QBT-09, QUAL-02]

must_haves:
  truths:
    - "`python3 run_demo.py` from portfolio_projects/qbacktest exits 0 and writes reports/figures/demo_tearsheet.png"
    - "The tearsheet PNG has exactly 3 panels: equity curve, drawdown, monthly returns"
    - "The printed summary table shows gross and net-of-cost Sharpe side by side, plus bootstrap CI"
    - "Tearsheet rendering with a < 2-bar equity curve returns gracefully instead of crashing"
  artifacts:
    - path: "portfolio_projects/qbacktest/src/qbacktest/tearsheet/renderer.py"
      provides: "TearsheetRenderer: 3-panel matplotlib PNG + summary table"
      contains: "savefig"
      min_lines: 80
    - path: "portfolio_projects/qbacktest/run_demo.py"
      provides: "one-command end-to-end demo: synthetic data → MA strategy → engine → tearsheet"
      contains: "demo_tearsheet"
    - path: "portfolio_projects/qbacktest/README.md"
      provides: "research question, data description, methodology, how-to-run, results summary"
      min_lines: 60
  key_links:
    - from: "portfolio_projects/qbacktest/run_demo.py"
      to: "qbacktest.tearsheet.TearsheetRenderer"
      via: "renders BacktestResults to reports/figures/demo_tearsheet.png"
      pattern: "TearsheetRenderer"
    - from: "portfolio_projects/qbacktest/src/qbacktest/tearsheet/renderer.py"
      to: "BacktestResults.metrics"
      via: "summary table reads MetricsReport gross/net fields"
      pattern: "net_sharpe"
---

<objective>
Build the 3-panel matplotlib tearsheet renderer, the demo MA-crossover strategy, the one-command `run_demo.py` runner, and the package README.

Purpose: Roadmap success criterion 5 — one command produces a tearsheet PNG and a summary table with gross and net Sharpe side by side on synthetic data.
Output: Working demo end-to-end; human-verified tearsheet legibility.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-qbacktest/01-RESEARCH.md
@.planning/phases/01-qbacktest/01-06-SUMMARY.md

<interfaces>
<!-- CONSUMES from plan 01-06:
  BacktestResults: equity_curve (pd.Series, DatetimeIndex), net_returns, gross_returns,
                   metrics (MetricsReport with gross_sharpe, net_sharpe, cost_bps, sortino,
                   max_drawdown, turnover, hit_rate, sharpe_ci_low/high, total_return, n_trades)
  EventDrivenBacktester(data_handler, strategy, config).run() -> BacktestResults
  Strategy ABC, HistoricalDataHandler, SyntheticOHLCVGenerator, SimulatedExecutionHandler,
  FixedSlippage, PercentageCommission -->

```python
# qbacktest/tearsheet/renderer.py — public contract
class TearsheetRenderer:
    def __init__(self, output_dir: str | Path = "reports/figures"): ...
    def render(self, results: BacktestResults, title: str, filename: str = "tearsheet.png") -> Path | None
        # returns written path, or None when equity curve has < 2 bars (graceful guard)
    def summary_table(self, results: BacktestResults) -> str   # formatted text table, gross | net columns
```
</interfaces>

Locked decisions:
- Tearsheet is exactly 3 panels: equity curve (top), drawdown (middle), monthly returns bar chart (bottom); matplotlib PNG
- Figures live under reports/figures/ (QUAL-05)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TearsheetRenderer (3-panel PNG + summary table)</name>
  <files>portfolio_projects/qbacktest/src/qbacktest/tearsheet/renderer.py, portfolio_projects/qbacktest/src/qbacktest/tearsheet/__init__.py, portfolio_projects/qbacktest/tests/test_tearsheet.py</files>
  <behavior>
    - test_render_writes_png: render a real BacktestResults from a quick synthetic run → returned Path exists, file size > 10KB
    - test_render_short_curve_graceful: results with a 1-bar equity curve → render returns None, no exception, no file
    - test_summary_table_gross_net_side_by_side: table string contains both gross and net Sharpe values and the CI bounds
  </behavior>
  <action>
Use `matplotlib.use("Agg")` before pyplot import (headless safety; never import matplotlib at qbacktest package init). Build a 3-row figure (figsize ~(10,12), constrained_layout): (1) equity curve line with PercentFormatter-free currency axis; (2) drawdown area fill using the vectorized expanding-max formula; (3) monthly returns bar chart via net_returns.resample('ME').apply(lambda r: (1+r).prod()-1) — note pandas 2.2+ uses 'ME', never 'M'-deprecated variants that raise under FutureWarning-as-error. Guard every panel with the len<2 check from RESEARCH.md Pitfall 6. summary_table(): fixed-width text table with rows [Total Return, Sharpe, Sharpe 95% CI, Sortino, Max Drawdown, Turnover, Hit Rate, Cost (bps), Trades] and columns Gross | Net (use MetricsReport fields; gross column gets n/a for cost-only rows). Close figures after savefig to avoid open-figure warnings.
  </action>
  <verify>
    <automated>cd portfolio_projects/qbacktest && python3 -m pytest tests/test_tearsheet.py -x -q</automated>
  </verify>
  <done>PNG written and non-trivial; degenerate curve handled; table pairs gross and net.</done>
</task>

<task type="auto">
  <name>Task 2: Demo MA strategy, run_demo.py, README</name>
  <files>portfolio_projects/qbacktest/examples/demo_ma_strategy.py, portfolio_projects/qbacktest/run_demo.py, portfolio_projects/qbacktest/README.md</files>
  <action>
examples/demo_ma_strategy.py: MovingAverageCrossStrategy(Strategy) — LONG when fast SMA (20) crosses above slow SMA (50), EXIT on cross below, using data_handler.get_latest_bars windows; importable class plus __main__ guard. run_demo.py (package root — roadmap names this file): loads configs/backtest_config.yml via yaml.safe_load, generates SyntheticOHLCVGenerator(symbols=["AAPL","MSFT","GOOG"], n_bars=504, seed=42) data (>= 252 bars so all tearsheet panels exercise — Pitfall 6), runs EventDrivenBacktester with FixedSlippage(10) + PercentageCommission(0.001), renders TearsheetRenderer to reports/figures/demo_tearsheet.png, prints the gross/net summary table to stdout, exits 0. README.md sections (QUAL-02): What is QBacktest (research question: honest event-driven backtesting with T+1 fills), Data (synthetic GBM generator, seeded), Methodology (event loop, T+1 fill invariant, accounting invariant, walk-forward isolation — with the four invariants listed), Installation (`pip3 install -e portfolio_projects/qbacktest`), How to run (`python3 run_demo.py`, `python3 -m pytest tests/`), Results summary (paste demo summary table + embed reports/figures/demo_tearsheet.png), API quickstart code block.
  </action>
  <verify>
    <automated>cd portfolio_projects/qbacktest && python3 run_demo.py && test -s reports/figures/demo_tearsheet.png && echo OK</automated>
  </verify>
  <done>One command produces tearsheet PNG + stdout summary with gross and net Sharpe side by side; README complete per QUAL-02.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify tearsheet visual quality</name>
  <files>reports/figures/demo_tearsheet.png (read-only inspection)</files>
  <what-built>End-to-end demo: synthetic data → MA crossover strategy → T+1 engine → 3-panel tearsheet PNG + gross/net summary table (manual-only verification per 01-VALIDATION.md).</what-built>
  <action>Pause for human visual verification of the rendered tearsheet — rendering aesthetics are not assertable in pytest.</action>
  <how-to-verify>
1. `cd portfolio_projects/qbacktest && python3 run_demo.py`
2. Open `reports/figures/demo_tearsheet.png`
3. Confirm: (a) 3 legible panels — equity curve, drawdown, monthly returns; (b) axes labeled with readable dates; (c) stdout summary table shows Gross and Net Sharpe side by side with the 95% CI.
  </how-to-verify>
  <verify>Human confirms 3 legible panels and gross/net summary table per steps above.</verify>
  <done>User types "approved"; any rendering issues described are fixed and re-verified.</done>
  <resume-signal>Type "approved" or describe rendering issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd portfolio_projects/qbacktest && python3 -m pytest tests/ -q` → 0 failures
- `python3 run_demo.py; echo $?` → 0; reports/figures/demo_tearsheet.png exists and is non-empty
- README.md covers research question, data, methodology, how-to-run, results
</verification>

<success_criteria>
- QBT-09: tearsheet module + demo strategy + one-command runner all working end-to-end on synthetic data (roadmap success criterion 5, code half)
- QUAL-02: README complete with all five required sections and an embedded figure
</success_criteria>

<output>
After completion, create `.planning/phases/01-qbacktest/01-08-SUMMARY.md`
</output>
