---
phase: 01-qbacktest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - portfolio_projects/qbacktest/pyproject.toml
  - portfolio_projects/qbacktest/requirements.txt
  - portfolio_projects/qbacktest/configs/backtest_config.yml
  - portfolio_projects/qbacktest/reports/figures/.gitkeep
  - portfolio_projects/qbacktest/src/qbacktest/__init__.py
  - portfolio_projects/qbacktest/src/qbacktest/data/__init__.py
  - portfolio_projects/qbacktest/src/qbacktest/data/synthetic.py
  - portfolio_projects/qbacktest/src/qbacktest/strategy/__init__.py
  - portfolio_projects/qbacktest/src/qbacktest/portfolio/__init__.py
  - portfolio_projects/qbacktest/src/qbacktest/execution/__init__.py
  - portfolio_projects/qbacktest/src/qbacktest/risk/__init__.py
  - portfolio_projects/qbacktest/src/qbacktest/metrics/__init__.py
  - portfolio_projects/qbacktest/src/qbacktest/walk_forward/__init__.py
  - portfolio_projects/qbacktest/src/qbacktest/tearsheet/__init__.py
  - portfolio_projects/qbacktest/tests/conftest.py
  - portfolio_projects/qbacktest/tests/test_synthetic.py
  - portfolio_projects/qbacktest/tests/test_packaging.py
  - portfolio_projects/qbacktest/tests/test_events.py
  - portfolio_projects/qbacktest/tests/test_portfolio.py
  - portfolio_projects/qbacktest/tests/test_execution.py
  - portfolio_projects/qbacktest/tests/test_engine.py
  - portfolio_projects/qbacktest/tests/test_metrics.py
  - portfolio_projects/qbacktest/tests/test_walk_forward.py
  - portfolio_projects/qbacktest/tests/test_determinism.py
autonomous: true
requirements: [QBT-01, QBT-10, QUAL-05]

must_haves:
  truths:
    - "pip3 install -e portfolio_projects/qbacktest succeeds and `import qbacktest` works from any cwd"
    - "SyntheticOHLCVGenerator with the same seed produces byte-identical output on two calls"
    - "pytest suite runs green from portfolio_projects/qbacktest with FutureWarning-as-error enforced via pyproject"
  artifacts:
    - path: "portfolio_projects/qbacktest/pyproject.toml"
      provides: "hatchling build config, pytest config with filterwarnings error::FutureWarning"
      contains: "hatchling"
    - path: "portfolio_projects/qbacktest/src/qbacktest/data/synthetic.py"
      provides: "SyntheticOHLCVGenerator (GBM, seedable, multi-asset)"
      min_lines: 40
    - path: "portfolio_projects/qbacktest/tests/conftest.py"
      provides: "seed fixtures + synthetic_bars fixture"
      contains: "synthetic_bars"
  key_links:
    - from: "portfolio_projects/qbacktest/tests/conftest.py"
      to: "qbacktest.data.synthetic"
      via: "package import (proves editable install works, no sys.path hacks)"
      pattern: "from qbacktest"
---

<objective>
Create the qbacktest package skeleton (Wave 0): installable src-layout package, deterministic synthetic OHLCV generator (the sole test data source), seeded conftest fixtures, and stub test files for every downstream oracle/invariant test.

Purpose: Locked Wave 0 decision — package skeleton + test stubs must exist before any implementation wave. Every later plan replaces a stub with real tests; nothing later needs to invent test infrastructure.
Output: Installed editable package, passing test_synthetic.py + test_packaging.py, skip-marked stubs for all remaining test files.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-qbacktest/01-RESEARCH.md
@.planning/phases/01-qbacktest/01-VALIDATION.md

Constraints (locked):
- Package at portfolio_projects/qbacktest, src layout, pyproject.toml + hatchling
- Install with `pip3 install -e` into the existing `quant/` venv (python3/pip3 already on PATH)
- Synthetic seedable OHLCV generator is the ONLY test data source; no network in tests
- Tests enforce `-W error::FutureWarning` (via pyproject filterwarnings so plain pytest enforces it)
- Do NOT modify core_research_backtesting/ — new code only under portfolio_projects/qbacktest/
- No sys.path.append() anywhere in tests — editable install only
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package skeleton and install editable</name>
  <files>portfolio_projects/qbacktest/pyproject.toml, portfolio_projects/qbacktest/requirements.txt, portfolio_projects/qbacktest/configs/backtest_config.yml, portfolio_projects/qbacktest/reports/figures/.gitkeep, portfolio_projects/qbacktest/src/qbacktest/__init__.py, portfolio_projects/qbacktest/src/qbacktest/{data,strategy,portfolio,execution,risk,metrics,walk_forward,tearsheet}/__init__.py</files>
  <action>
Create directory tree per the RESEARCH.md "Recommended Project Structure". pyproject.toml exactly:

```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "qbacktest"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["numpy>=2.2.6", "pandas>=2.3.0", "matplotlib>=3.7", "scipy>=1.11", "pyyaml>=6.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/qbacktest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["error::FutureWarning"]
```

Top-level `src/qbacktest/__init__.py` for now: `__version__ = "0.1.0"` plus `from qbacktest.data.synthetic import SyntheticOHLCVGenerator` (added in Task 2's commit if needed — keep import working by creating synthetic.py stub class first or defer the import line to Task 2). All subpackage `__init__.py` files start empty. requirements.txt mirrors project dependencies plus pytest, pytest-cov. configs/backtest_config.yml holds defaults: initial_capital: 100000.0, position_size: 0.1, cost_bps: 10, slippage model name, commission model name. Add reports/figures/.gitkeep.

Then install: `pip3 install -e portfolio_projects/qbacktest` from repo root.

Do NOT add yfinance to dependencies. Do NOT import matplotlib at package init (only inside tearsheet module later).
  </action>
  <verify>
    <automated>python3 -c "import qbacktest; print(qbacktest.__version__)" && pip3 show qbacktest</automated>
  </verify>
  <done>Editable install succeeds; `import qbacktest` prints 0.1.0 from any cwd; src layout prevents accidental root imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SyntheticOHLCVGenerator with real tests</name>
  <files>portfolio_projects/qbacktest/src/qbacktest/data/synthetic.py, portfolio_projects/qbacktest/tests/test_synthetic.py</files>
  <behavior>
    - test_determinism: two generators with seed=42 produce identical dict[str, DataFrame] (use pd.testing.assert_frame_equal)
    - test_different_seeds: seed=42 vs seed=43 produce different close paths
    - test_multi_asset_independent_paths: 3 symbols produce mutually different close series
    - test_ohlc_sanity: high >= max(open, close) and low <= min(open, close) for every bar; volume > 0
    - test_shape_and_index: n_bars rows, business-day DatetimeIndex, columns exactly [open, high, low, close, volume]
  </behavior>
  <action>
Implement `SyntheticOHLCVGenerator` exactly per the verified pattern in 01-RESEARCH.md "Pattern 4": GBM closes via `np.random.default_rng(seed + i*1000)` per symbol, synthesized open/high/low/volume, `pd.bdate_range` index, `generate() -> dict[str, pd.DataFrame]`. Constructor params: symbols, n_bars=504, start_date="2022-01-03", mu=0.0002, sigma=0.015, initial_price=100.0, seed=42. Use only `np.random.default_rng` (never global np.random.seed inside the generator). Write tests first (RED), then implement (GREEN). Add `from qbacktest.data.synthetic import SyntheticOHLCVGenerator` export to src/qbacktest/__init__.py.
  </action>
  <verify>
    <automated>cd portfolio_projects/qbacktest && python3 -m pytest tests/test_synthetic.py -x -q</automated>
  </verify>
  <done>All five behaviors pass; same seed → identical frames; generator is importable as qbacktest.SyntheticOHLCVGenerator.</done>
</task>

<task type="auto">
  <name>Task 3: Create conftest fixtures, packaging test, and stub test files</name>
  <files>portfolio_projects/qbacktest/tests/conftest.py, portfolio_projects/qbacktest/tests/test_packaging.py, portfolio_projects/qbacktest/tests/test_events.py, portfolio_projects/qbacktest/tests/test_portfolio.py, portfolio_projects/qbacktest/tests/test_execution.py, portfolio_projects/qbacktest/tests/test_engine.py, portfolio_projects/qbacktest/tests/test_metrics.py, portfolio_projects/qbacktest/tests/test_walk_forward.py, portfolio_projects/qbacktest/tests/test_determinism.py</files>
  <action>
conftest.py per RESEARCH.md: autouse `fix_seeds` fixture (np.random.seed(42), random.seed(42), PYTHONHASHSEED=42) and `synthetic_bars` fixture returning SyntheticOHLCVGenerator(symbols=["AAPL","MSFT","GOOG"], n_bars=504, seed=42).generate(). No network imports anywhere.

test_packaging.py (REAL, passing now): spawn `python3 -c "import qbacktest"` via subprocess with cwd=tempfile.mkdtemp() to prove sibling-project importability (QBT-01); assert returncode 0. Also assert `import qbacktest` does not import yfinance or make network modules available (check `"yfinance" not in sys.modules` after import in a subprocess).

Stub files — each contains the exact future test node ids from 01-VALIDATION.md, body `pytest.skip("W0 stub — implemented in plan 01-NN")`:
- test_events.py: test_priority_ordering, test_fifo_tie_break (plan 01-02)
- test_metrics.py: test_metrics_fields_present, test_bootstrap_ci_order, test_results_has_net_sharpe (plan 01-03; test_results_has_net_sharpe lands in 01-06)
- test_portfolio.py: test_round_trip_flat_price, test_partial_close, test_position_reversal, test_accounting_invariant_after_every_fill (plan 01-04)
- test_execution.py: test_fill_price_components, test_t_plus_one_fill_oracle (plans 01-05, 01-06)
- test_engine.py: test_strategy_plugin_seam, test_risk_limits_block_order (plan 01-06)
- test_determinism.py: test_same_seed_same_results (plan 01-06)
- test_walk_forward.py: test_no_state_bleed_sentinel, test_oos_aggregation (plan 01-07)
  </action>
  <verify>
    <automated>cd portfolio_projects/qbacktest && python3 -m pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>
  <done>Full suite green (passes + skips, zero failures); synthetic_bars fixture importable by all stubs; packaging test proves import from foreign cwd.</done>
</task>

</tasks>

<verification>
- `pip3 show qbacktest` shows editable install pointing at portfolio_projects/qbacktest
- `cd portfolio_projects/qbacktest && python3 -m pytest tests/ -q` → 0 failures (real synthetic + packaging tests pass, stubs skip)
- `git status` shows no changes under core_research_backtesting/
</verification>

<success_criteria>
- QBT-01: editable install + import from temp cwd verified by test_packaging.py
- QBT-10: deterministic seedable multi-asset generator verified by test_synthetic.py
- QUAL-05: src layout, pyproject.toml, YAML config, per-project requirements.txt, reports/figures/ all present
- Every Wave 0 gap in 01-RESEARCH.md and 01-VALIDATION.md checked off
</success_criteria>

<output>
After completion, create `.planning/phases/01-qbacktest/01-01-SUMMARY.md`
</output>
