---
phase: 01-qbacktest
plan: 09
type: execute
wave: 6
depends_on: ["01-07", "01-08"]
files_modified:
  - portfolio_projects/qbacktest/src/qbacktest/__init__.py
  - portfolio_projects/qbacktest/tests/test_packaging.py
autonomous: true
requirements: [QUAL-01, QUAL-04]

must_haves:
  truths:
    - "Full pytest suite passes with -W error::FutureWarning, fully offline, with zero remaining skip stubs"
    - "import qbacktest pulls in no network library (yfinance/requests/urllib3 absent from sys.modules)"
    - "The complete public API (engine, strategy, data, walk-forward, tearsheet, metrics) is importable from the qbacktest top level"
    - "Codex read-only review has run and every finding is either fixed or explicitly triaged with rationale"
  artifacts:
    - path: "portfolio_projects/qbacktest/src/qbacktest/__init__.py"
      provides: "complete public API exports including WalkForwardRunner, WalkForwardWindow, generate_windows, TearsheetRenderer"
      contains: "WalkForwardRunner"
  key_links:
    - from: "portfolio_projects/qbacktest/src/qbacktest/__init__.py"
      to: "qbacktest.walk_forward, qbacktest.tearsheet"
      via: "top-level re-exports (downstream phases import from qbacktest directly)"
      pattern: "from qbacktest.walk_forward"
---

<objective>
Close out the phase quality gate: finalize the public API surface, prove the suite is deterministic/offline/FutureWarning-clean with no leftover stubs, and run the locked codex read-only review with findings triage.

Purpose: QUAL-01 and QUAL-04 are completion gates — the phase cannot be marked done until the suite is strict-green and the external review passes (locked decision from project init).
Output: Strict full-suite pass, codex review findings resolved, API frozen for Phase 2 (AlphaRank) consumption.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-qbacktest/01-VALIDATION.md
@.planning/phases/01-qbacktest/01-07-SUMMARY.md
@.planning/phases/01-qbacktest/01-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize public API and strict full-suite gate</name>
  <files>portfolio_projects/qbacktest/src/qbacktest/__init__.py, portfolio_projects/qbacktest/tests/test_packaging.py</files>
  <action>
Add the wave-5 exports to src/qbacktest/__init__.py: WalkForwardRunner, WalkForwardWindow, generate_windows (from qbacktest.walk_forward), TearsheetRenderer (from qbacktest.tearsheet — ensure this does NOT trigger matplotlib import at package init; if renderer.py imports matplotlib at module top, keep the top-level export lazy via __getattr__ or move matplotlib import inside methods). Define __all__. Extend test_packaging.py: (a) test_public_api_complete — every name in __all__ imports from `qbacktest` top level; (b) test_no_network_modules_on_import — subprocess imports qbacktest and asserts 'yfinance', 'requests', 'urllib3' not in sys.modules; (c) test_no_skipped_tests — run `python3 -m pytest tests/ -q --collect-only` style check or assert via pytest hooks that no test in the suite carries the W0 skip marker anymore (simplest: grep tests/ for 'W0 stub' and assert zero matches). Then run the strict gate twice back-to-back to confirm determinism: `python3 -m pytest tests/ -v -W error::FutureWarning`.
  </action>
  <verify>
    <automated>cd portfolio_projects/qbacktest && python3 -m pytest tests/ -v -W error::FutureWarning 2>&1 | tail -5 && ! grep -rn "W0 stub" tests/</automated>
  </verify>
  <done>Full suite strict-green twice in a row; zero skips/stubs; offline import proven; API complete for sibling-project import (Phase 2 dependency).</done>
</task>

<task type="auto">
  <name>Task 2: Codex read-only review and findings triage</name>
  <files>portfolio_projects/qbacktest/src/qbacktest/ (fixes only where findings require)</files>
  <action>
Run the locked external gate: `codex exec --sandbox read-only "Review portfolio_projects/qbacktest for correctness: (1) verify orders can never fill at the same bar's close — trace the pending-order buffer in src/qbacktest/engine.py; (2) verify cash/positions/costs are mutated ONLY inside Portfolio.on_fill; (3) audit for look-ahead leakage in data handling, metrics, and walk-forward windows; (4) check tests for hidden network access or unseeded randomness. Report findings as a numbered list with severity."` Capture output. For each finding: fix it (with a test if behavioral) or document explicit triage rationale in the SUMMARY (cosmetic/false-positive). Re-run the strict suite after any fix: `python3 -m pytest tests/ -v -W error::FutureWarning`. If the codex CLI is unavailable or requires authentication, surface this as a blocking checkpoint rather than skipping the gate — QUAL-04 cannot be self-certified.
  </action>
  <verify>
    <automated>cd portfolio_projects/qbacktest && python3 -m pytest tests/ -q -W error::FutureWarning 2>&1 | tail -3</automated>
  </verify>
  <done>Codex review executed; all findings fixed or triaged with rationale recorded in SUMMARY; suite still strict-green (roadmap success criterion 5, review half).</done>
</task>

</tasks>

<verification>
- `cd portfolio_projects/qbacktest && python3 -m pytest tests/ -v -W error::FutureWarning` → all pass, no skips
- `python3 -c "import qbacktest; print(sorted(qbacktest.__all__))"` lists the full public API
- Codex findings list + resolutions present in 01-09-SUMMARY.md
- All five roadmap success criteria for Phase 1 now demonstrably TRUE
</verification>

<success_criteria>
- QUAL-01: deterministic, seeded, offline pytest suite passing under -W error::FutureWarning with zero stubs remaining
- QUAL-04: codex read-only review executed with all findings triaged/resolved before phase completion
</success_criteria>

<output>
After completion, create `.planning/phases/01-qbacktest/01-09-SUMMARY.md`
</output>
