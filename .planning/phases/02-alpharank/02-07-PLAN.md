---
phase: 02-alpharank
plan: 07
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06]
files_modified:
  - portfolio_projects/alpharank/src/alpharank/report/__init__.py
  - portfolio_projects/alpharank/src/alpharank/report/builder.py
  - portfolio_projects/alpharank/run_pipeline.py
  - portfolio_projects/alpharank/README.md
  - portfolio_projects/alpharank/tests/test_integration.py
autonomous: true
requirements: [ALR-08, ALR-09, QUAL-02, QUAL-03]

must_haves:
  truths:
    - "python run_pipeline.py runs generator -> features -> labels -> purged-CV model comparison -> decile backtests -> attribution -> report and exits 0"
    - "run_pipeline.py --quick completes in under ~2 minutes with reduced data and exits 0"
    - "reports/figures/ contains the IC comparison, IC decay, monthly IC series, and gross-vs-net equity figures after a run"
    - "The report shows gross AND net Sharpe side by side with turnover, plus factor attribution (alpha, betas, R-squared) for the strategy"
    - "README documents the research question, the PLANTED synthetic alpha (so results are honest), methodology, how-to-run, results, and robustness"
  artifacts:
    - path: "portfolio_projects/alpharank/run_pipeline.py"
      provides: "one-command runner with --quick and isolated --real-data flags"
      contains: "--quick"
    - path: "portfolio_projects/alpharank/src/alpharank/report/builder.py"
      provides: "figure rendering + markdown results report"
      min_lines: 80
    - path: "portfolio_projects/alpharank/README.md"
      provides: "research report: question, data (planted alpha documented), methodology, results, robustness"
      contains: "planted"
  key_links:
    - from: "portfolio_projects/alpharank/run_pipeline.py"
      to: "alpharank.models.comparison.run_model_comparison"
      via: "single evaluation call feeding both the IC table and decile backtests"
      pattern: "run_model_comparison"
    - from: "portfolio_projects/alpharank/run_pipeline.py"
      to: "alpharank.portfolio.backtest.run_decile_backtest"
      via: "per-model OOS scores -> build_decile_weights -> backtest"
      pattern: "run_decile_backtest"
    - from: "portfolio_projects/alpharank/src/alpharank/report/builder.py"
      to: "matplotlib Agg backend"
      via: "matplotlib.use('Agg') before pyplot import (headless safety, Phase 1 pattern)"
      pattern: "use\\(.Agg.\\)"
---

<objective>
Assemble the one-command research pipeline: runner script, report builder with figures, README research report, and the end-to-end integration tests.

Purpose: ALR-09 (one-command runner producing a research report with figures), ALR-08 (attribution results land in the report), QUAL-02/QUAL-03 (README with results; gross-vs-net with significance).
Output: run_pipeline.py, report/builder.py, README.md, fully implemented test_integration.py.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-alpharank/02-RESEARCH.md
@portfolio_projects/alpharank/src/alpharank/models/comparison.py
@portfolio_projects/alpharank/src/alpharank/portfolio/backtest.py
@portfolio_projects/alpharank/src/alpharank/analytics/attribution.py
@portfolio_projects/qbacktest/run_demo.py

<interfaces>
Everything needed is already built — wire, don't reimplement:
```python
CrossSectionalGenerator(...).generate() -> SyntheticPanel                     # 02-01
build_feature_panel(panel) -> features MultiIndex frame                       # 02-02
make_labels(prices_monthly, horizon) / make_forward_returns                   # 02-03
compute_ic_series, icir, newey_west_ic_tstat, ic_decay, factor_attribution    # 02-03
PurgedCVEvaluator(6, 2, 1, 1)                                                 # 02-04
run_model_comparison(X, y, evaluator) -> (table_df, {name: scores_frame})     # 02-06
build_decile_weights(scores_frame) -> weights dict                            # 02-05
run_decile_backtest(ohlcv, weights) -> BacktestResults; summarize_results()   # 02-05
qbacktest.TearsheetRenderer (lazy top-level export)                           # Phase 1
```
Config values come from configs/alpharank_config.yml (created in 02-01). The --quick block in that config defines reduced sizes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReportBuilder — figures and markdown results</name>
  <files>portfolio_projects/alpharank/src/alpharank/report/builder.py, portfolio_projects/alpharank/src/alpharank/report/__init__.py</files>
  <action>
Implement `ReportBuilder(output_dir: Path)` in `src/alpharank/report/builder.py`. Call `matplotlib.use('Agg')` at module top BEFORE importing pyplot (Phase 1 locked headless pattern). Methods, each saving a PNG to `reports/figures/` and returning the path:

1. `fig_ic_comparison(table_df)`: bar chart of mean IC per model in baseline order, NW t-stat annotated above each bar, horizontal line at 0.
2. `fig_ic_decay(decay_by_model: dict[str, pd.DataFrame])`: line plot, one line per model, x=horizon (1,2,3,6), y=mean IC.
3. `fig_monthly_ic(ic_series_by_model)`: monthly IC time series for all four models (thin lines) with a rolling 6-month mean for the LGBM model highlighted.
4. `fig_equity_curves(results_by_model: dict[str, BacktestResults])`: gross vs net equity curves (two lines per model is noisy — plot net equity for all four models in one panel, and a second panel with gross vs net for the LGBM strategy to make the cost drag visible). Build gross equity from `(1 + gross_returns).cumprod() * initial`, net from the results.equity_curve.
5. `write_markdown(path, sections: dict)`: renders the results report: model comparison table (mean IC, ICIR, NW t-stat, p-value), backtest table (gross Sharpe, net Sharpe, Sharpe 95% CI, cost bps, turnover, max drawdown, n_trades per model — QUAL-03's gross-beside-net), attribution table (alpha, alpha t-stat, betas, R-squared), and an honest note restating the planted alpha targets.

Keep figures deterministic (no timestamps in filenames; fixed figsize/dpi).
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/alpharank && ../../quant/bin/python -c "from alpharank.report.builder import ReportBuilder; print('import ok')"</automated>
  </verify>
  <done>ReportBuilder imports headlessly; all five methods implemented and exercised by Task 3's integration test.</done>
</task>

<task type="auto">
  <name>Task 2: run_pipeline.py one-command runner</name>
  <files>portfolio_projects/alpharank/run_pipeline.py</files>
  <action>
Create `run_pipeline.py` at the alpharank project root (argparse: `--quick`, `--seed 42`, `--config configs/alpharank_config.yml`, `--output-dir reports`, `--real-data` hidden behind explicit opt-in). Pipeline steps, each logged with a timed banner:

1. Load YAML config; `--quick` swaps in the quick block (n_assets=20, n_months=36) and reduces LGBM n_estimators to 50 via config_override passed to the model constructor — keep this in the runner, not the model defaults.
2. Data: `CrossSectionalGenerator(**cfg).generate()`. `--real-data` path calls `alpharank.data.loader.load_real_universe` instead — import the loader INSIDE the branch so the default path never touches it (locked isolation).
3. Features + labels: `build_feature_panel(panel)`; month-end closes -> `make_labels(horizon=1)`; stack and inner-join on the feature index to form (X, y).
4. Evaluation: `PurgedCVEvaluator(n_folds=6, n_test_folds=2, purged_size=1, embargo_size=1)`; `run_model_comparison(X, y, evaluator)` -> table + per-model OOS scores.
5. IC decay: `ic_decay(scores_frame, month_end_closes, horizons=(1,2,3,6))` per model.
6. Backtests: for each model in baseline order, `build_decile_weights(scores_frame)` -> `run_decile_backtest(panel.ohlcv, weights)`; collect `summarize_results` dicts.
7. Attribution (ALR-08): compute momentum and value FACTOR portfolio returns — simple monthly decile long-short returns built directly from each single factor's z-scores against next-month returns (no costs; document as frictionless factor-mimicking portfolios); regress the LGBM strategy's monthly net returns (resample net_returns to monthly compound) on these two factor return series via `factor_attribution`.
8. Report: ReportBuilder figures + `write_markdown(reports/RESULTS.md, ...)`; also render a qbacktest tearsheet PNG for the LGBM strategy via `qbacktest.TearsheetRenderer` into reports/figures/.
9. Print the final summary table to stdout; `sys.exit(0)` on success.

No KFold, no accuracy metrics, no hyperparameter search anywhere. Seed flows from --seed into the generator only (models have fixed random_state=42).
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/alpharank && ../../quant/bin/python run_pipeline.py --quick && ls reports/figures/*.png | head -5</automated>
  </verify>
  <done>`run_pipeline.py --quick` exits 0 and writes figures + RESULTS.md; full run path identical code with bigger config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration tests and README research report</name>
  <files>portfolio_projects/alpharank/tests/test_integration.py, portfolio_projects/alpharank/README.md</files>
  <behavior>
    - test_end_to_end: in-process quick pipeline (call the runner's main() with --quick args or a pipeline function it exposes) produces a comparison table with 4 rows, finite gross/net Sharpe for every model's backtest, and attribution dict with r_squared in [0, 1]
    - test_runner_smoke: `subprocess.run([sys.executable, 'run_pipeline.py', '--quick'], cwd=project_root)` exits 0 and reports/figures contains >= 4 PNG files and RESULTS.md exists
  </behavior>
  <action>
1. Replace the W0 stubs in tests/test_integration.py (exact node ids test_end_to_end, test_runner_smoke). Structure run_pipeline.py with a `run(config: dict) -> PipelineResults`-style function so test_end_to_end can call it in-process and inspect outputs; main() is a thin argparse wrapper. Use tmp_path for the in-process test's output dir to keep test runs from clobbering reports/.
2. Write README.md as the research report (QUAL-02), sections:
   - Research question: can ML models recover cross-sectional alpha from lagged factor features, and does it survive transaction costs?
   - Data: synthetic universe with PLANTED alpha — state the exact formula `alpha = IC_target * sigma_noise / sqrt(1 - IC_target^2)`, targets (momentum 0.06, value 0.04), the unplanted negative controls (reversal/vol/quality/liquidity ≈ 0 IC), and delist/survivorship handling. Honesty requirement: a reader must know the models are SUPPOSED to find signal here.
   - Methodology: factor construction with lag discipline, forward-rank labels, CombinatorialPurgedCV(6,2,1,1), baseline model order, Newey-West inference (maxlags=4 at T=60), decile L/S through qbacktest with 5bps spread + 10bps commission, T+1 fills.
   - How to run: install steps (qbacktest editable first, then alpharank), `python run_pipeline.py` / `--quick`, test command.
   - Results: embed the figures from reports/figures/ (relative markdown links) and the comparison/backtest/attribution tables from a real full run — RUN `../../quant/bin/python run_pipeline.py` (full, not quick) once and paste actual numbers.
   - Robustness & limitations: IC decay behavior, gross-vs-net gap, purged-CV vs naive CV rationale, what real data would change, no hyperparameter tuning by design.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/alpharank && ../../quant/bin/python -m pytest tests/test_integration.py -v && ../../quant/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>Both integration tests pass; README contains real full-run numbers and figure links; full suite green.</done>
</task>

</tasks>

<verification>
- `cd portfolio_projects/alpharank && ../../quant/bin/python run_pipeline.py --quick` exits 0 in < ~2 min
- reports/figures/ has IC comparison, decay, monthly IC, equity, tearsheet PNGs
- README.md sections complete with actual results numbers
- Full suite green offline
</verification>

<success_criteria>
- ALR-09: one-command runner produces the research report with figures (question, methodology, results, robustness)
- ALR-08: attribution regression results appear in the final report
- QUAL-03: every backtest table shows net beside gross with bootstrap CI on Sharpe and NW t-stats on IC
</success_criteria>

<output>
After completion, create `.planning/phases/02-alpharank/02-07-SUMMARY.md`
</output>
