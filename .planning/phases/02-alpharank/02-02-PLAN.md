---
phase: 02-alpharank
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - portfolio_projects/alpharank/src/alpharank/features/__init__.py
  - portfolio_projects/alpharank/src/alpharank/features/base.py
  - portfolio_projects/alpharank/src/alpharank/features/factors.py
  - portfolio_projects/alpharank/tests/test_features.py
autonomous: true
requirements: [ALR-02]

must_haves:
  truths:
    - "Every feature value at time t is computed exclusively from data timestamped <= t (shift n >= 1 enforced)"
    - "FeatureLeakageValidator raises AssertionError if any feature's Spearman IC with next-day returns exceeds 0.15"
    - "Six cross-sectional factors (momentum, reversal, volatility, value, quality, liquidity) produce a (date, symbol) feature panel at monthly rebalance dates"
    - "Permutation test: shuffled features do not achieve higher IC than original features"
  artifacts:
    - path: "portfolio_projects/alpharank/src/alpharank/features/base.py"
      provides: "safe_shift guard, cross_sectional_zscore, FeatureLeakageValidator"
      contains: "assert n >= 1"
    - path: "portfolio_projects/alpharank/src/alpharank/features/factors.py"
      provides: "six factor functions + build_feature_panel"
      min_lines: 80
    - path: "portfolio_projects/alpharank/tests/test_features.py"
      provides: "lag correctness, leakage, and permutation tests"
  key_links:
    - from: "portfolio_projects/alpharank/src/alpharank/features/factors.py"
      to: "features/base.py safe_shift"
      via: "every lag goes through safe_shift — no raw negative shift in features/"
      pattern: "safe_shift"
    - from: "portfolio_projects/alpharank/tests/test_features.py"
      to: "alpharank.data.generator"
      via: "small_panel fixture from conftest"
      pattern: "small_panel"
---

<objective>
Build the lag-safe cross-sectional factor feature module with built-in leakage assertions.

Purpose: ALR-02 — momentum, short-term reversal, volatility, value proxy, quality proxy, liquidity features, all strictly lagged, with leakage assertions that the codex audit (QUAL-04) will target.
Output: features/base.py infrastructure, features/factors.py with build_feature_panel, fully implemented test_features.py.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-alpharank/02-RESEARCH.md
@portfolio_projects/alpharank/src/alpharank/data/generator.py

<interfaces>
From plan 02-01 (Wave 1) — generator output consumed here:
```python
class SyntheticPanel:
    ohlcv: dict[str, pd.DataFrame]        # per-symbol [open, high, low, close, volume], business-day index, truncated at delist
    fundamentals: pd.DataFrame            # MultiIndex (month_end, symbol) x [book_to_market, quality]
    monthly_returns: pd.DataFrame         # month_end x symbol
    mom_loading: pd.Series; val_loading: pd.Series
```
Convention: features are computed on daily data with shift(1), then sampled at month-end rebalance dates. Feature at month-end t predicts the t→t+1 month return (label built in plan 02-03).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Feature infrastructure — safe_shift, cross-sectional zscore, FeatureLeakageValidator</name>
  <files>portfolio_projects/alpharank/src/alpharank/features/base.py, portfolio_projects/alpharank/tests/test_features.py</files>
  <behavior>
    - test_cross_sectional_zscore: zscore of a hand-built 3-asset frame has row mean 0 and row std 1; NaN entries are preserved (skipna semantics)
    - test_safe_shift_rejects_negative: safe_shift(df, 0) and safe_shift(df, -1) raise AssertionError with message containing "positive shifts"
    - test_leakage_validator_catches_plant: a deliberately leaked feature (next-day return itself, shift(-1)) FAILS validation; a properly lagged feature PASSES
  </behavior>
  <action>
Implement in `src/alpharank/features/base.py`:

1. `safe_shift(df: pd.DataFrame, n: int) -> pd.DataFrame`: `assert n >= 1, "Only positive shifts in feature construction"`; returns `df.shift(n)`. ALL lags in factors.py must route through this.
2. `cross_sectional_zscore(df: pd.DataFrame) -> pd.DataFrame`: `df.sub(df.mean(axis=1), axis=0).div(df.std(axis=1), axis=0)` — per-time-step normalization, never full-panel (research Pitfall 2). Document WHY full-panel StandardScaler before CV split is forbidden.
3. `FeatureLeakageValidator` with `validate(feature: pd.DataFrame, prices: pd.DataFrame, threshold: float = 0.15) -> None`: computes Spearman IC of each feature column against next-day returns (`prices.pct_change().shift(-1)` — the ONLY permitted negative shift, evaluation-side); asserts `abs(ic) < threshold` per the research Pattern 1 code; raises AssertionError naming the offending feature and its IC. Skip columns with < 10 shared observations.

Write the three failing tests first (replace W0 stubs where names overlap; keep stub node ids that belong to Task 2 untouched), then implement.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/alpharank && ../../quant/bin/python -m pytest tests/test_features.py -v -k "zscore or safe_shift or validator"</automated>
  </verify>
  <done>Infrastructure tests pass; planted leak is caught; lagged feature passes; negative shift impossible through safe_shift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Six factors and build_feature_panel with leakage tests</name>
  <files>portfolio_projects/alpharank/src/alpharank/features/factors.py, portfolio_projects/alpharank/src/alpharank/features/__init__.py, portfolio_projects/alpharank/tests/test_features.py</files>
  <behavior>
    - test_feature_lag_correctness: momentum value at row t computed from a hand-built price series equals the hand-computed (ret_252 - ret_21) using data through t-1 only (verify against shifted manual calc)
    - test_no_feature_uses_future_data: FeatureLeakageValidator passes for ALL six factors on the small_panel fixture (IC vs next-day returns < 0.15)
    - test_permutation_leakage: cross-sectionally shuffling feature values within each date destroys IC — mean |IC| of shuffled momentum vs next-month returns is below the original momentum IC (QUAL-04 audit target)
    - test_panel_shape: build_feature_panel returns MultiIndex (date, symbol) DataFrame with exactly columns [momentum, reversal, volatility, value, quality, liquidity]; only month-end rebalance dates present; no NaN rows after warmup drop
  </behavior>
  <action>
Implement in `src/alpharank/features/factors.py` — each factor takes wide daily frames (close: date x symbol built from SyntheticPanel.ohlcv; volume likewise) and returns a wide daily frame, ALWAYS ending in `safe_shift(..., 1)`:

1. `momentum_12_1(close)`: `close.pct_change(252) - close.pct_change(21)`, then safe_shift(1) (research Pattern 1).
2. `reversal_1m(close)`: `-close.pct_change(21)`, safe_shift(1).
3. `volatility_60d(close)`: `-close.pct_change().rolling(60).std()` (negated: low-vol = high score), safe_shift(1).
4. `value_proxy(fundamentals)`: book_to_market unstacked to wide monthly, shifted ONE MONTH (publication lag — at month-end t use the t-1 fundamental), then forward-filled to daily index. Implement the monthly shift with `.shift(1)` on the monthly frame and comment the publication-lag rationale.
5. `quality_proxy(fundamentals)`: same treatment on quality column.
6. `liquidity(close, volume)`: `-np.log((close * volume).rolling(21).mean())` (negated: illiquid = high expected return premium), safe_shift(1).

`build_feature_panel(panel: SyntheticPanel, rebalance_dates: pd.DatetimeIndex | None = None) -> pd.DataFrame`:
- Build wide close/volume from panel.ohlcv (outer-join indices; delisted assets are NaN after truncation — that is correct).
- Compute all six daily factor frames, sample each at month-end rebalance dates (default: month-ends present in panel.monthly_returns, skipping the warmup period where momentum is NaN, i.e. first 13 months).
- Apply `cross_sectional_zscore` per date to each factor.
- Stack to MultiIndex (date, symbol) rows x 6 factor columns; drop rows with any NaN (delisted/warmup).
- Run `FeatureLeakageValidator.validate` on every daily factor frame before stacking — the module self-asserts on construction.

Export from `features/__init__.py`: build_feature_panel, FeatureLeakageValidator, cross_sectional_zscore, safe_shift.

Replace the remaining W0 stubs in test_features.py with the real tests (failing first, then green). Keep exact node ids test_feature_lag_correctness, test_no_feature_uses_future_data, test_permutation_leakage.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/alpharank && ../../quant/bin/python -m pytest tests/test_features.py -v && ../../quant/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>All test_features.py tests pass; every factor is lag-safe and validator-clean; feature panel has documented shape; full suite still green.</done>
</task>

</tasks>

<verification>
- `grep -rn "shift(-" portfolio_projects/alpharank/src/alpharank/features/` returns ONLY the validator's evaluation-side next-day return line (with explanatory comment)
- `grep -rn "StandardScaler" portfolio_projects/alpharank/src/alpharank/features/` returns nothing
- Full suite green offline
</verification>

<success_criteria>
- ALR-02 satisfied: six lagged cross-sectional factors with leakage assertions wired into construction
- The codex audit targets (positive-shift-only in features/, validator, permutation test) all exist and pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-alpharank/02-02-SUMMARY.md`
</output>
