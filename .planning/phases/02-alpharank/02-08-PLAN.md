---
phase: 02-alpharank
plan: 08
type: execute
wave: 5
depends_on: [02-07]
files_modified:
  - portfolio_projects/alpharank/tests/test_synthetic.py
  - portfolio_projects/alpharank/tests/test_integration.py
  - portfolio_projects/alpharank/src/alpharank/__init__.py
autonomous: true
requirements: [QUAL-01, QUAL-04]

must_haves:
  truths:
    - "Full pytest suite passes under -W error::FutureWarning with ZERO remaining 'W0 stub' skips, fully offline"
    - "Two back-to-back full-suite runs produce identical pass results (determinism)"
    - "codex read-only leakage audit has been executed and every finding is fixed or explicitly triaged"
    - "No KFold, no accuracy metrics, no hyperparameter search exist anywhere in src/alpharank or run_pipeline.py"
  artifacts:
    - path: "portfolio_projects/alpharank/tests/test_synthetic.py"
      provides: "no-network-on-import and no-stub meta tests"
    - path: "portfolio_projects/alpharank/src/alpharank/__init__.py"
      provides: "frozen public API surface for downstream phases (DeFiRegimeNet reuses purged-CV patterns)"
  key_links:
    - from: "codex review findings"
      to: "code fixes or SUMMARY triage rationale"
      via: "every finding numbered and dispositioned before phase completion"
      pattern: "N/A — process link"
---

<objective>
Close out the AlphaRank quality gate: prove the suite is deterministic/offline/stub-free, freeze the public API, and run the locked codex read-only leakage audit with findings triage.

Purpose: QUAL-01 (deterministic offline suite) and QUAL-04 (codex leakage audit) — the phase cannot be marked complete without both. Phase 5 (DeFiRegimeNet) will import AlphaRank's purged-CV pattern, so the API surface is frozen here.
Output: strict full-suite pass x2, codex findings resolved, public API exports finalized.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-alpharank/02-RESEARCH.md
@.planning/phases/01-qbacktest/01-09-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strict suite, determinism proof, API freeze, no-stub guard</name>
  <files>portfolio_projects/alpharank/src/alpharank/__init__.py, portfolio_projects/alpharank/tests/test_synthetic.py, portfolio_projects/alpharank/tests/test_integration.py</files>
  <action>
1. Finalize `src/alpharank/__init__.py` public API with `__all__`: CrossSectionalGenerator, build_feature_panel, FeatureLeakageValidator, cross_sectional_zscore, safe_shift, make_labels, make_forward_returns, PurgedCVEvaluator, BASELINE_ORDER + the four model classes, run_model_comparison, build_decile_weights, PrecomputedWeightsStrategy, run_decile_backtest, compute_ic_series, icir, newey_west_ic_tstat, ic_decay, factor_attribution. Keep matplotlib-importing modules (report.builder) OUT of package init — lazy via `__getattr__` if exported at all (Phase 1 pattern).
2. Add meta tests to test_synthetic.py: (a) `test_no_network_modules_on_import` — subprocess imports alpharank and asserts 'yfinance', 'requests', 'urllib3' not in sys.modules; (b) `test_public_api_complete` — every name in `__all__` imports from top-level alpharank.
3. Add `test_no_skipped_stubs` to test_integration.py — grep tests/ for "W0 stub" and assert zero matches (all stubs replaced by plans 02-02..02-07; fix any straggler NOW by implementing or deleting with rationale).
4. Run the strict full suite TWICE back-to-back and compare pass counts/node outcomes for determinism: `cd portfolio_projects/alpharank && ../../quant/bin/python -m pytest tests/ -v -W error::FutureWarning`.
5. Static anti-feature sweep, all must be empty: `grep -rn "KFold" src/ run_pipeline.py`, `grep -rniE "accuracy_score|f1_score|roc_auc" src/`, `grep -rn "GridSearch|RandomizedSearch|optuna" src/ run_pipeline.py`, `grep -rn "sys.path" src/ tests/ run_pipeline.py`.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/alpharank && ../../quant/bin/python -m pytest tests/ -v -W error::FutureWarning 2>&1 | tail -3 && ! grep -rn "W0 stub" tests/ && ! grep -rn "KFold" src/ run_pipeline.py</automated>
  </verify>
  <done>Strict suite green twice with identical results; zero stubs; API frozen with __all__; anti-feature sweeps clean.</done>
</task>

<task type="auto">
  <name>Task 2: Codex read-only leakage audit with findings triage</name>
  <files>portfolio_projects/alpharank/src/alpharank/ (fixes as needed), portfolio_projects/alpharank/tests/ (fixes as needed)</files>
  <action>
Run the locked external gate:

`codex exec --sandbox read-only "Review portfolio_projects/alpharank for label/feature leakage and CV correctness: (1) verify every shift() in src/alpharank/features/ uses positive lag only, and the only negative shifts live in labels/ and validator evaluation code — trace each one; (2) verify no full-panel normalization (StandardScaler or z-score over time) is fitted before CombinatorialPurgedCV splits — scalers must fit inside training folds only; (3) verify the feature-to-label join uses the feature index and cannot pull future-dated label rows; (4) verify CombinatorialPurgedCV split consumption treats the test side as a list of arrays and that train/test panel rows never overlap; (5) confirm KFold, accuracy metrics, and hyperparameter search are absent; (6) check tests for hidden network access or unseeded randomness. Report findings as a numbered list with severity."`

Capture output. For each finding: fix it (with a regression test if behavioral) or document explicit triage rationale in the SUMMARY (cosmetic/false-positive). Re-run the strict suite after any fix: `cd portfolio_projects/alpharank && ../../quant/bin/python -m pytest tests/ -v -W error::FutureWarning`. If the codex CLI is unavailable or requires authentication, surface this as a blocking checkpoint rather than skipping — QUAL-04 cannot be self-certified.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/alpharank && ../../quant/bin/python -m pytest tests/ -q -W error::FutureWarning 2>&1 | tail -3</automated>
  </verify>
  <done>Codex review executed; every finding fixed or triaged with documented rationale; suite green after fixes.</done>
</task>

</tasks>

<verification>
- `cd portfolio_projects/alpharank && ../../quant/bin/python -m pytest tests/ -v -W error::FutureWarning` → all pass, no skips
- `cd portfolio_projects/alpharank && ../../quant/bin/python run_pipeline.py --quick` exits 0
- Codex findings list dispositioned in 02-08-SUMMARY.md
</verification>

<success_criteria>
- QUAL-01: deterministic, seeded, offline suite passing under -W error::FutureWarning with zero stubs
- QUAL-04: codex read-only leakage audit executed with all findings triaged/resolved before phase completion
- Phase 2 ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/02-alpharank/02-08-SUMMARY.md`
</output>
