---
phase: 02-alpharank
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - portfolio_projects/alpharank/src/alpharank/validation/__init__.py
  - portfolio_projects/alpharank/src/alpharank/validation/purged_cv.py
  - portfolio_projects/alpharank/tests/test_validation.py
autonomous: true
requirements: [ALR-05]

must_haves:
  truths:
    - "CPCV splits have zero train-test overlap at the month level AND the expanded panel-row level"
    - "No training month lies within purged_size=1 of any test month; the month after a test block is embargoed"
    - "CombinatorialPurgedCV(n_folds=6, n_test_folds=2) yields exactly C(6,2)=15 splits"
    - "sklearn KFold is not imported anywhere in src/alpharank — enforced by a test"
    - "Panel expansion maps month indices to row positions by date grouping, robust to variable universe size (delistings)"
  artifacts:
    - path: "portfolio_projects/alpharank/src/alpharank/validation/purged_cv.py"
      provides: "PurgedCVEvaluator wrapping skfolio CombinatorialPurgedCV with panel expansion and generic model evaluation"
      contains: "CombinatorialPurgedCV"
      min_lines: 80
    - path: "portfolio_projects/alpharank/tests/test_validation.py"
      provides: "no-overlap, purge-gap, split-count, and KFold-guard tests"
  key_links:
    - from: "portfolio_projects/alpharank/src/alpharank/validation/purged_cv.py"
      to: "skfolio.model_selection.CombinatorialPurgedCV"
      via: "split() consumed as (train_idx, list[test_idx]) — test is a LIST of arrays"
      pattern: "for .*train.*, .*test_sets.* in .*split"
    - from: "portfolio_projects/alpharank/src/alpharank/validation/purged_cv.py"
      to: "panel MultiIndex dates"
      via: "month -> row positions via groupby on index level 'date', NOT m*n_assets arithmetic"
      pattern: "get_level_values"
---

<objective>
Build the purged/embargoed cross-validation layer: a PurgedCVEvaluator that wraps skfolio CombinatorialPurgedCV at the month level, expands month splits to panel rows, and evaluates any fit/predict model into a per-month OOS IC series.

Purpose: ALR-05 — purged/embargoed walk-forward CV with NO standard KFold anywhere. This is the evaluation backbone plan 02-06 runs all four models through.
Output: validation/purged_cv.py + fully implemented test_validation.py.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-alpharank/02-RESEARCH.md

<interfaces>
Verified skfolio 0.20.1 API (introspected live in research):
```python
from skfolio.model_selection import CombinatorialPurgedCV
cv = CombinatorialPurgedCV(n_folds=6, n_test_folds=2, purged_size=1, embargo_size=1)
# cv.split(X) yields (train_idx: ndarray, tests: list[ndarray])  — CRITICAL: test side is a LIST
```
Consumer contract (plan 02-06 calls this):
```python
class PurgedCVEvaluator:
    def __init__(self, n_folds=6, n_test_folds=2, purged_size=1, embargo_size=1): ...
    def split_months(self, months: pd.DatetimeIndex) -> Iterator[tuple[np.ndarray, np.ndarray]]
        # yields (train_month_positions, test_month_positions) with test sets flattened per split
    def evaluate(self, model, X: pd.DataFrame, y: pd.Series) -> dict
        # X, y MultiIndex (date, symbol); returns {'ic_series': pd.Series (per OOS month, may contain
        #  repeated months across paths — average duplicates), 'oos_scores': pd.Series aligned to X.index,
        #  'n_splits': int}
```
Model contract: anything with `fit(X, y)` and `predict(X) -> ndarray`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PurgedCVEvaluator with date-grouped panel expansion</name>
  <files>portfolio_projects/alpharank/src/alpharank/validation/purged_cv.py, portfolio_projects/alpharank/src/alpharank/validation/__init__.py, portfolio_projects/alpharank/tests/test_validation.py</files>
  <behavior>
    - test_split_count: split_months over 60 months yields exactly 15 splits (C(6,2))
    - test_evaluator_with_dummy_model: a DummyModel whose predict() returns y's true values achieves IC = 1.0 in every OOS month; a DummyModel returning a seeded-random vector achieves mean |IC| < 0.2
    - test_variable_universe: a panel where the last 10 months have fewer symbols (simulated delistings) evaluates without error and oos_scores indices match X.index exactly
  </behavior>
  <action>
Implement `src/alpharank/validation/purged_cv.py`:

1. `PurgedCVEvaluator.__init__` stores locked params (n_folds=6, n_test_folds=2, purged_size=1, embargo_size=1) and constructs `CombinatorialPurgedCV`.
2. `split_months(months)`: run `cv.split(np.zeros((len(months), 1)))`; for each split, flatten the test side with `np.concatenate(test_sets)` — remember test is a list[ndarray] (research Pitfall 3); yield (train_positions, test_positions) as month positions.
3. `evaluate(model, X, y)`:
   - X and y share a MultiIndex (date, symbol). Build `months = X.index.get_level_values('date').unique().sort_values()`.
   - Map month → row positions via `X.index.get_level_values('date')` boolean masks or a precomputed `{month: np.flatnonzero(dates == month)}` dict. Do NOT use `m * n_assets` arithmetic — universe size varies after delistings (this is the critical robustness fix over the research sketch; comment it).
   - Per split: clone the model if it's an sklearn estimator (`sklearn.base.clone` with a try/except fallback to `copy.deepcopy` for non-sklearn models), fit on train rows, predict test rows; record per-OOS-month Spearman IC (scipy.spearmanr of predictions vs y within each test month); accumulate OOS scores (when a month appears in multiple CPCV paths, average the predictions for that row).
   - Return dict per the interface contract; ic_series is a date-indexed pd.Series where duplicate months across paths are averaged.

Export PurgedCVEvaluator from `validation/__init__.py`. Write the behavior tests first against synthetic MultiIndex panels built inline (no dependency on plans 02-02/02-03).
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/alpharank && ../../quant/bin/python -m pytest tests/test_validation.py -v -k "split_count or dummy or variable"</automated>
  </verify>
  <done>15 splits at n_folds=6/n_test_folds=2; perfect model gives IC 1.0 OOS; variable universe handled; oos_scores aligned to X.index.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Purge/embargo property tests and the KFold guard</name>
  <files>portfolio_projects/alpharank/tests/test_validation.py</files>
  <behavior>
    - test_purged_cv_no_train_test_overlap: across all 15 splits, set(train_months) & set(test_months) is empty, and the expanded panel row sets are disjoint
    - test_purge_gap: for every split and every test month position p, positions p-1 and p+1 are absent from the training set (purged_size=1 before, embargo_size=1 after) — assert via month-position arithmetic on split_months output
    - test_no_standard_kfold_anywhere: walk src/alpharank/**/*.py and run_pipeline.py (if it exists yet), assert the string "KFold" appears in none of them (reads file text; also asserts "from sklearn.model_selection import" lines contain no KFold)
  </behavior>
  <action>
Replace the remaining W0 stubs in tests/test_validation.py with the three property tests above (exact node ids: test_purged_cv_no_train_test_overlap, test_purge_gap, test_no_standard_kfold_anywhere).

For test_purge_gap, be careful with CPCV semantics: purged_size removes months adjacent to test blocks from training on the BEFORE side and embargo_size on the AFTER side; assert the weaker, implementation-true property by introspecting actual splits — for each test month t, neither t-1 nor t+1 (when within range) appears in train. If skfolio's exact boundary semantics differ at fold edges, assert the property on interior test blocks and document the boundary behavior found (print the first split's layout in the test docstring).

The KFold guard uses pathlib glob over `src/alpharank` from the tests directory (`Path(__file__).parent.parent / 'src' / 'alpharank'`) — no sys.path manipulation.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/alpharank && ../../quant/bin/python -m pytest tests/test_validation.py -v && ../../quant/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>No-overlap and purge-gap properties hold across all 15 splits; KFold guard passes; full suite green.</done>
</task>

</tasks>

<verification>
- `grep -rn "KFold" portfolio_projects/alpharank/src/alpharank/` returns empty
- `grep -rn "test_sets\|np.concatenate" portfolio_projects/alpharank/src/alpharank/validation/purged_cv.py` shows list-of-arrays handling
- Full suite green offline
</verification>

<success_criteria>
- ALR-05 satisfied: skfolio CombinatorialPurgedCV with locked params (6/2/1/1), panel expansion robust to delistings, no-overlap and purge properties proven by tests, zero KFold anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/02-alpharank/02-04-SUMMARY.md`
</output>
