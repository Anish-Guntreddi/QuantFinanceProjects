---
phase: 02-alpharank
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - portfolio_projects/alpharank/src/alpharank/labels/__init__.py
  - portfolio_projects/alpharank/src/alpharank/labels/forward_returns.py
  - portfolio_projects/alpharank/src/alpharank/analytics/__init__.py
  - portfolio_projects/alpharank/src/alpharank/analytics/ic.py
  - portfolio_projects/alpharank/src/alpharank/analytics/ic_decay.py
  - portfolio_projects/alpharank/src/alpharank/analytics/attribution.py
  - portfolio_projects/alpharank/tests/test_labels.py
  - portfolio_projects/alpharank/tests/test_analytics.py
autonomous: true
requirements: [ALR-03, ALR-06, ALR-08]

must_haves:
  truths:
    - "Forward-return rank labels match hand-computed examples for a 3-asset case"
    - "Label at time t is NaN for the last `horizon` rows — no label exists where no future data exists"
    - "IC, rank-IC, ICIR and Newey-West t-stats match hand-computed/statsmodels reference values"
    - "IC decay is computable at horizons 1, 2, 3, 6 months"
    - "Factor attribution OLS returns alpha, alpha t-stat, betas, and R-squared without error"
  artifacts:
    - path: "portfolio_projects/alpharank/src/alpharank/labels/forward_returns.py"
      provides: "make_labels(prices_monthly, horizon) -> percentile rank labels"
      contains: "rank(axis=1, pct=True)"
    - path: "portfolio_projects/alpharank/src/alpharank/analytics/ic.py"
      provides: "compute_ic_series, icir, newey_west_ic_tstat"
      contains: "HAC"
    - path: "portfolio_projects/alpharank/src/alpharank/analytics/ic_decay.py"
      provides: "ic_decay(scores, monthly_returns, horizons)"
    - path: "portfolio_projects/alpharank/src/alpharank/analytics/attribution.py"
      provides: "factor_attribution OLS regression"
  key_links:
    - from: "portfolio_projects/alpharank/src/alpharank/labels/forward_returns.py"
      to: "pandas shift(-horizon)"
      via: "the ONLY intentional negative shift in the codebase outside validators — commented as label-only"
      pattern: "shift\\(-"
    - from: "portfolio_projects/alpharank/src/alpharank/analytics/ic.py"
      to: "statsmodels get_robustcov_results"
      via: "HAC cov_type with maxlags from floor(4*(T/100)**0.25)"
      pattern: "get_robustcov_results"
---

<objective>
Build the label module (forward-return cross-sectional ranks) and the IC analytics suite (IC/rank-IC/ICIR, Newey-West t-stats, IC decay, factor attribution).

Purpose: ALR-03 (hand-tested rank labels — the highest-leakage-risk step), ALR-06 (IC analytics with HAC inference), ALR-08 (attribution regression used by the final report).
Output: labels/forward_returns.py, analytics/{ic, ic_decay, attribution}.py, fully implemented test_labels.py and test_analytics.py.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-alpharank/02-RESEARCH.md
@portfolio_projects/alpharank/src/alpharank/data/generator.py

<interfaces>
Consumers (plans 02-06, 02-07) will call:
```python
make_labels(prices_monthly: pd.DataFrame, horizon: int = 1) -> pd.DataFrame   # date x symbol, pct ranks 0..1, NaN tail
compute_ic_series(scores: pd.DataFrame, fwd_returns: pd.DataFrame) -> pd.Series  # indexed by date (not bare ndarray — report needs dates)
icir(ic_series) -> float
newey_west_ic_tstat(ic_series) -> tuple[float, float, float]   # (mean_ic, t_stat, p_value)
ic_decay(scores: pd.DataFrame, monthly_returns: pd.DataFrame, horizons=(1, 2, 3, 6)) -> pd.DataFrame  # horizon x [mean_ic, t_stat]
factor_attribution(strategy_rets: pd.Series, factor_rets: pd.DataFrame) -> dict  # alpha, alpha_tstat, alpha_pvalue, betas, r_squared, residual
```
Monthly prices come from SyntheticPanel: use month-end closes (resample('ME').last() of the wide daily close frame) or panel.monthly_returns compounded — pick month-end closes and document.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forward-return cross-sectional rank labels</name>
  <files>portfolio_projects/alpharank/src/alpharank/labels/forward_returns.py, portfolio_projects/alpharank/src/alpharank/labels/__init__.py, portfolio_projects/alpharank/tests/test_labels.py</files>
  <behavior>
    - test_forward_rank_labels_hand_computed: for the research's 3-asset 4-row example, label at t=0 for asset A (highest forward return 2%) equals 1.0; B (-1%, lowest) equals 1/3; C (1%) equals 2/3 — exact to 1e-10
    - test_label_nan_tail: with horizon=h, the last h rows of the label frame are entirely NaN; with horizon=3 exactly the last 3
    - test_label_ignores_delisted: a symbol that is NaN at t+h gets a NaN label at t and does not distort other symbols' ranks
  </behavior>
  <action>
Implement `make_labels(prices: pd.DataFrame, horizon: int = 1) -> pd.DataFrame` in `src/alpharank/labels/forward_returns.py` exactly per research Pattern 2:

```python
fwd_ret = prices.pct_change(horizon).shift(-horizon)  # INTENTIONAL negative shift — labels only
labels = fwd_ret.rank(axis=1, pct=True)
```
Add a prominent comment block: negative shift moves future data to the current row — permitted ONLY in labels/, never in features/. Also provide `make_forward_returns(prices, horizon)` returning the raw fwd_ret frame (the IC analytics and decay module need raw forward returns, not ranks).

Write failing tests first (replace W0 stubs, keep exact node ids), then implement. Use pd.date_range monthly index in tests.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/alpharank && ../../quant/bin/python -m pytest tests/test_labels.py -v</automated>
  </verify>
  <done>Labels match hand-computed ranks to 1e-10; NaN tail of exactly `horizon` rows; delisted symbols handled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IC, ICIR, and Newey-West t-statistics</name>
  <files>portfolio_projects/alpharank/src/alpharank/analytics/ic.py, portfolio_projects/alpharank/src/alpharank/analytics/__init__.py, portfolio_projects/alpharank/tests/test_analytics.py</files>
  <behavior>
    - test_ic_hand_computed: compute_ic_series on a single date with 3 assets equals scipy.stats.spearmanr of the same vectors (hand-verifiable monotonic example gives IC exactly 1.0; an anti-monotonic example gives -1.0)
    - test_icir_formula: icir equals mean/std (ddof=1) for a fixed small array; returns 0.0 for zero-std input
    - test_nw_tstat: for a seeded AR(1) IC series of length 60, newey_west_ic_tstat matches an inline statsmodels HAC reference computation to 1e-10, and the chosen maxlags equals 4 (locked: floor(4*(60/100)**0.25) = 4)
  </behavior>
  <action>
Implement in `src/alpharank/analytics/ic.py` per research Pattern 4 and code examples:

1. `compute_ic_series(scores, fwd_returns) -> pd.Series`: per-date Spearman rank-IC; align columns per date, dropna, require >= 5 shared names else NaN; return a date-indexed Series (dropna'd at the end).
2. `icir(ic_series) -> float`: mean/std with std ddof=1 guard.
3. `newey_west_ic_tstat(ic_series) -> (mean_ic, t_stat, p_value)`: OLS of IC on a constant; `get_robustcov_results(cov_type='HAC', maxlags=int(np.floor(4*(T/100)**0.25)), use_correction=True)`. For T=60 this yields maxlags=4, matching the locked decision. Accept Series or ndarray.

Anti-feature guard: NO accuracy/F1/AUC functions anywhere in analytics (locked). Replace the relevant W0 stubs in test_analytics.py.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/alpharank && ../../quant/bin/python -m pytest tests/test_analytics.py -v -k "ic_hand or icir or nw_tstat"</automated>
  </verify>
  <done>IC math matches scipy reference; ICIR formula exact; NW t-stat matches statsmodels HAC with maxlags=4 at T=60.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: IC decay and factor attribution</name>
  <files>portfolio_projects/alpharank/src/alpharank/analytics/ic_decay.py, portfolio_projects/alpharank/src/alpharank/analytics/attribution.py, portfolio_projects/alpharank/src/alpharank/analytics/__init__.py, portfolio_projects/alpharank/tests/test_analytics.py</files>
  <behavior>
    - test_ic_decay_horizons: ic_decay on the small_panel fixture's momentum-loading scores returns a DataFrame indexed by [1, 2, 3, 6] with columns [mean_ic, t_stat, p_value]; horizon-1 mean IC is positive (planted alpha persists, so decay is gradual — assert h1 > 0)
    - test_factor_attribution: regressing a synthetic series constructed as 0.5*factor1 + 0.2*factor2 + noise recovers betas within 0.1, r_squared > 0.5, and returns all documented keys
  </behavior>
  <action>
1. `src/alpharank/analytics/ic_decay.py` — `ic_decay(scores, monthly_prices_or_returns, horizons=(1, 2, 3, 6)) -> pd.DataFrame`: for each horizon h, build h-month forward returns via `make_forward_returns(prices, h)` (import from alpharank.labels), compute the IC series of the SAME scores against those returns, then mean/NW-tstat per horizon (reuse ic.py functions). Index = horizon, columns = [mean_ic, t_stat, p_value, n_obs].
2. `src/alpharank/analytics/attribution.py` — `factor_attribution(strategy_rets: pd.Series, factor_rets: pd.DataFrame) -> dict` exactly per the research code example: statsmodels OLS with add_constant, reindex factor frame on strategy index, return alpha/alpha_tstat/alpha_pvalue/betas/r_squared/residual.

Export all public functions from `analytics/__init__.py`. Replace remaining W0 stubs in test_analytics.py with the behavior tests above (failing first).
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/alpharank && ../../quant/bin/python -m pytest tests/test_analytics.py tests/test_labels.py -v && ../../quant/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>IC decay produces the 4-horizon table; attribution recovers planted betas; full suite green.</done>
</task>

</tasks>

<verification>
- `grep -rn "shift(-" portfolio_projects/alpharank/src/alpharank/labels/` shows only the documented label shift
- `grep -rniE "accuracy|f1_score|roc_auc" portfolio_projects/alpharank/src/alpharank/` returns nothing
- Full suite green offline
</verification>

<success_criteria>
- ALR-03: labels unit-tested against hand-computed cross-sectional ranks with NaN tail proof
- ALR-06: IC/rank-IC/ICIR with Newey-West HAC t-stats (maxlags=4 at T=60) and IC decay at 1/3/6-month horizons (plus 2)
- ALR-08: factor attribution regression ready for the report
</success_criteria>

<output>
After completion, create `.planning/phases/02-alpharank/02-03-SUMMARY.md`
</output>
