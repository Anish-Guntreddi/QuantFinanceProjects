---
phase: 02-alpharank
plan: 06
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - portfolio_projects/alpharank/src/alpharank/models/__init__.py
  - portfolio_projects/alpharank/src/alpharank/models/base.py
  - portfolio_projects/alpharank/src/alpharank/models/composite.py
  - portfolio_projects/alpharank/src/alpharank/models/linear.py
  - portfolio_projects/alpharank/src/alpharank/models/elastic.py
  - portfolio_projects/alpharank/src/alpharank/models/lgbm.py
  - portfolio_projects/alpharank/src/alpharank/models/comparison.py
  - portfolio_projects/alpharank/tests/test_models.py
autonomous: true
requirements: [ALR-04]

must_haves:
  truths:
    - "Four models exist in strict baseline order: equal-weight factor composite, LinearRegression, ElasticNet, LGBMRegressor (NOT LGBMRanker)"
    - "All four models are evaluated through the SAME PurgedCVEvaluator protocol — no per-model evaluation shortcuts"
    - "Equal-weight composite achieves positive mean OOS IC on the planted-alpha synthetic data"
    - "All hyperparameters are fixed constants — no search, no grid, no optuna anywhere"
  artifacts:
    - path: "portfolio_projects/alpharank/src/alpharank/models/base.py"
      provides: "RankModel interface (fit/predict)"
    - path: "portfolio_projects/alpharank/src/alpharank/models/lgbm.py"
      provides: "LGBMRegressor with fixed deterministic params"
      contains: "LGBMRegressor"
    - path: "portfolio_projects/alpharank/src/alpharank/models/comparison.py"
      provides: "run_model_comparison producing the consistent per-model IC table in baseline order"
  key_links:
    - from: "portfolio_projects/alpharank/src/alpharank/models/comparison.py"
      to: "alpharank.validation.PurgedCVEvaluator"
      via: "evaluator.evaluate(model, X, y) — identical call for all four models"
      pattern: "evaluate\\("
    - from: "portfolio_projects/alpharank/src/alpharank/models/comparison.py"
      to: "alpharank.analytics.ic"
      via: "newey_west_ic_tstat + icir on each model's OOS IC series"
      pattern: "newey_west_ic_tstat"
---

<objective>
Implement the four ranking models in strict baseline order and the comparison harness that evaluates every model through the identical purged-CV protocol.

Purpose: ALR-04 — equal-weight factor composite → LinearRegression → ElasticNet → LGBMRegressor, same evaluation protocol for all, fixed hyperparameters (hyperparameter search is a locked anti-feature).
Output: models/ package + comparison.py + fully implemented test_models.py.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-alpharank/02-RESEARCH.md
@portfolio_projects/alpharank/src/alpharank/validation/purged_cv.py
@portfolio_projects/alpharank/src/alpharank/features/factors.py
@portfolio_projects/alpharank/src/alpharank/labels/forward_returns.py

<interfaces>
From wave-2 plans (consume directly, no exploration needed):
```python
# plan 02-02
build_feature_panel(panel, rebalance_dates=None) -> pd.DataFrame
    # MultiIndex (date, symbol) x [momentum, reversal, volatility, value, quality, liquidity], z-scored per date
# plan 02-03
make_labels(prices_monthly, horizon=1) -> pd.DataFrame   # date x symbol pct ranks
compute_ic_series, icir, newey_west_ic_tstat             # alpharank.analytics
# plan 02-04
PurgedCVEvaluator(n_folds=6, n_test_folds=2, purged_size=1, embargo_size=1)
    .evaluate(model, X, y) -> {'ic_series': pd.Series, 'oos_scores': pd.Series, 'n_splits': int}
    # model contract: fit(X, y), predict(X) -> ndarray
```
To build (X, y): stack the label frame to a MultiIndex Series and inner-join with the feature panel index so every row has both features and a label. Join on the FEATURE index (left side = features) — never the label index (research audit point 5).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Four models with fixed parameters in baseline order</name>
  <files>portfolio_projects/alpharank/src/alpharank/models/base.py, portfolio_projects/alpharank/src/alpharank/models/composite.py, portfolio_projects/alpharank/src/alpharank/models/linear.py, portfolio_projects/alpharank/src/alpharank/models/elastic.py, portfolio_projects/alpharank/src/alpharank/models/lgbm.py, portfolio_projects/alpharank/src/alpharank/models/__init__.py, portfolio_projects/alpharank/tests/test_models.py</files>
  <behavior>
    - test_composite_positive_ic: EqualWeightComposite evaluated via PurgedCVEvaluator on a full-size synthetic panel (n_assets=50, n_months=60, seed=42) achieves mean OOS IC > 0.0 with NW t-stat computable (planted alpha is recoverable through the pipeline)
    - test_composite_needs_no_fit: composite.predict gives identical output before and after fit (fit is a no-op)
    - test_models_fixed_params: each model class exposes its params; assert LGBM params equal the fixed constants and random_state == 42; assert no model module imports GridSearchCV/RandomizedSearchCV/optuna
  </behavior>
  <action>
1. `models/base.py`: `RankModel` ABC — `fit(self, X: pd.DataFrame, y: pd.Series) -> "RankModel"`, `predict(self, X: pd.DataFrame) -> np.ndarray`, plus `name: str` class attr. Also `get_params/set_params` passthrough so sklearn.base.clone works where applicable (simplest: make wrappers hold an inner sklearn estimator and implement `__deepcopy__`-friendly state; PurgedCVEvaluator falls back to deepcopy for non-sklearn models).
2. `models/composite.py`: `EqualWeightComposite(RankModel)` — fit is a no-op; predict = row-mean of the (already z-scored) factor columns. Factor SIGNS are already oriented "high = good" in plan 02-02 (reversal/vol/liquidity negated there) so a plain mean is the baseline. name="equal_weight_composite".
3. `models/linear.py`: `LinearRankModel` wrapping `Pipeline(StandardScaler(), LinearRegression())`. Scaler is fitted INSIDE CV training folds only (the evaluator fits the whole pipeline per fold) — leak-safe; comment this.
4. `models/elastic.py`: `ElasticNetRankModel` wrapping `Pipeline(StandardScaler(), ElasticNet(alpha=0.001, l1_ratio=0.5, max_iter=5000, random_state=42))`. Fixed params, documented as deliberately untuned.
5. `models/lgbm.py`: `LGBMRankModel` wrapping `LGBMRegressor(n_estimators=200, learning_rate=0.05, num_leaves=15, max_depth=3, min_child_samples=20, subsample=0.9, subsample_freq=1, colsample_bytree=0.9, random_state=42, deterministic=True, force_row_wise=True, verbosity=-1)`. NOT LGBMRanker (locked — research Pitfall 5 documents the relevance-tier failure; cite in docstring).
6. `models/__init__.py`: export `BASELINE_ORDER = [EqualWeightComposite, LinearRankModel, ElasticNetRankModel, LGBMRankModel]` — the strict order is data, not convention.

Write the behavior tests first (replace W0 stub test_composite_positive_ic; add the two new tests). Building (X, y) for the integration-style test: generator → build_feature_panel → make_labels on month-end closes → stack labels → inner-join on feature index.
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/alpharank && ../../quant/bin/python -m pytest tests/test_models.py -v -k "composite or fixed_params"</automated>
  </verify>
  <done>Composite recovers positive OOS IC through the full purged-CV protocol; all params fixed and deterministic; baseline order exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Model comparison harness with identical protocol</name>
  <files>portfolio_projects/alpharank/src/alpharank/models/comparison.py, portfolio_projects/alpharank/src/alpharank/models/__init__.py, portfolio_projects/alpharank/tests/test_models.py</files>
  <behavior>
    - test_all_models_positive_ic: run_model_comparison on the full-size synthetic panel (seed=42) returns a 4-row DataFrame in exact baseline order with columns [model, mean_ic, icir, nw_tstat, p_value, n_months]; every model's mean_ic > 0.0 (all recover the planted linear alpha; strict cross-model ordering is NOT asserted — with linear planted alpha LinearRegression may legitimately beat LGBM, document this in the test docstring)
    - test_comparison_returns_oos_scores: the harness also returns per-model OOS score frames (date x symbol) suitable for build_decile_weights — index/columns match the label frame's
  </behavior>
  <action>
Implement `models/comparison.py` — `run_model_comparison(X: pd.DataFrame, y: pd.Series, evaluator: PurgedCVEvaluator) -> tuple[pd.DataFrame, dict[str, pd.DataFrame]]`:

1. Iterate `BASELINE_ORDER`, instantiate each model fresh, call `evaluator.evaluate(model, X, y)` — one code path, zero model-specific branches (ALR-04's "same protocol" is structural here).
2. Per model compute mean_ic, icir, NW t-stat/p-value (alpharank.analytics) on the OOS ic_series; assemble the comparison DataFrame in baseline order.
3. Unstack each model's `oos_scores` (MultiIndex Series) to a (date x symbol) frame for downstream decile construction; return `{model_name: scores_frame}`.
4. Log (print or logging) the table — run_pipeline will reuse it verbatim.

Replace the remaining W0 stub (test_all_models_positive_ic) and add test_comparison_returns_oos_scores. Use a module-scoped fixture for the expensive full-size panel evaluation so the two tests share one computation; keep total runtime under ~60s (if LGBM is slow on 15 splits x 60 months x 50 assets, drop to n_assets=30, n_months=48 in the fixture — planted IC tolerance still holds; note the choice).
  </action>
  <verify>
    <automated>cd /Users/anishguntreddi/Documents/QuantFinanceProjects/portfolio_projects/alpharank && ../../quant/bin/python -m pytest tests/test_models.py -v && ../../quant/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>Comparison table covers all four models in baseline order with IC/ICIR/NW stats; OOS scores exportable per model; full suite green.</done>
</task>

</tasks>

<verification>
- `grep -rn "LGBMRanker\|GridSearch\|RandomizedSearch\|optuna" portfolio_projects/alpharank/src/alpharank/` returns empty
- `grep -rn "KFold" portfolio_projects/alpharank/src/alpharank/` still empty
- Full suite green offline, runtime < ~90s
</verification>

<success_criteria>
- ALR-04: four models in strict baseline order, identical purged-CV evaluation protocol, fixed hyperparameters, all recovering positive OOS IC on planted-alpha data
</success_criteria>

<output>
After completion, create `.planning/phases/02-alpharank/02-06-SUMMARY.md`
</output>
