{
  "mode": "yolo",
  "granularity": "standard",
  "parallelization": true,
  "commit_docs": true,
  "model_profile": "balanced",
  "workflow": {
    "research": true,
    "plan_check": true,
    "verifier": true,
    "nyquist_validation": true,
    "auto_advance": true,
    "_auto_chain_active": true
  },
  "external_validation": {
    "tool": "codex",
    "command": "codex exec --sandbox read-only",
    "when": "after each phase verification, before phase is marked complete"
  }
}
